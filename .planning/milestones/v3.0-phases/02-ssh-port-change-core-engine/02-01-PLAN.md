---
phase: 02-ssh-port-change-core-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gui-pro/package.json
  - gui-pro/package-lock.json
  - gui-pro/src/shared/lib/cn.ts
  - gui-pro/src/shared/ui/Button.tsx
  - gui-pro/src/shared/ui/Button.test.tsx
  - gui-pro/src/shared/ui/Button.stories.tsx
  - gui-pro/src/shared/ui/Badge.tsx
  - gui-pro/src/shared/ui/Badge.test.tsx
  - gui-pro/src/shared/ui/Badge.stories.tsx
  - gui-pro/src/shared/ui/ErrorBanner.tsx
  - gui-pro/src/shared/ui/ErrorBanner.test.tsx
  - gui-pro/src/shared/ui/ErrorBanner.stories.tsx
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-13, COMP-14, SB-04, SB-05]

must_haves:
  truths:
    - "CVA, tailwind-merge, clsx are installed as dependencies in gui-pro"
    - "cn() utility function exists and correctly merges Tailwind classes"
    - "Button renders with exactly 4 variants (primary, danger, ghost, icon) and 3 sizes (sm, md, lg) via CVA"
    - "Badge renders with exactly 5 variants (success, warning, danger, neutral, dot) via CVA"
    - "ErrorBanner renders with 3 severity variants (error, warning, info) via CVA"
    - "Zero imports from colors.ts in Button, Badge, or ErrorBanner"
    - "All three components have Storybook stories showing all variants, sizes, and states"
  artifacts:
    - path: "gui-pro/src/shared/lib/cn.ts"
      provides: "cn() utility combining clsx + tailwind-merge"
      exports: ["cn"]
    - path: "gui-pro/src/shared/ui/Button.tsx"
      provides: "CVA-based Button with forwardRef"
      exports: ["Button", "buttonVariants"]
    - path: "gui-pro/src/shared/ui/Badge.tsx"
      provides: "CVA-based Badge"
      exports: ["Badge", "badgeVariants"]
    - path: "gui-pro/src/shared/ui/ErrorBanner.tsx"
      provides: "CVA-based ErrorBanner with severity"
      exports: ["ErrorBanner", "errorBannerVariants"]
  key_links:
    - from: "gui-pro/src/shared/ui/Button.tsx"
      to: "gui-pro/src/shared/lib/cn.ts"
      via: "import { cn }"
      pattern: "import.*cn.*from.*shared/lib/cn"
    - from: "gui-pro/src/shared/ui/Button.tsx"
      to: "class-variance-authority"
      via: "import { cva }"
      pattern: "import.*cva.*from.*class-variance-authority"
---

<objective>
Install CVA infrastructure and redesign the three CVA-variant components (Button, Badge, ErrorBanner) with full Storybook stories.

Purpose: These three components define the CVA pattern that all other components follow. Button is used by ConfirmDialog and IconButton. Badge pattern informs StatusBadge. ErrorBanner establishes the severity variant pattern.
Output: cn() utility, 3 fully redesigned CVA components, 3 story files, updated tests.
</objective>

<execution_context>
@C:/Users/naska/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/naska/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ssh-port-change-core-engine/02-CONTEXT.md
@.planning/phases/02-ssh-port-change-core-engine/02-RESEARCH.md
@.planning/phases/02-ssh-port-change-core-engine/02-UI-SPEC.md

<interfaces>
<!-- Phase 1 token system (executor must use these tokens, not hardcoded hex) -->
From gui-pro/src/shared/styles/tokens.css:
- Accent: --color-accent-interactive, --color-accent-hover, --color-accent-active
- Destructive: --color-destructive (#e05545 dark / #b03020 light)
- Status: --color-status-connected/connecting/error/info + -bg + -border variants
- Text: --color-text-primary, --color-text-secondary, --color-text-muted, --color-text-inverse
- BG: --color-bg-primary, --color-bg-surface, --color-bg-elevated, --color-bg-hover, --color-bg-active
- Border: --color-border
- Focus: --focus-ring (box-shadow: 0 0 0 2px var(--color-bg-primary), 0 0 0 4px var(--color-accent-interactive))
- Sizing: --font-size-xs(11px), --font-size-sm(12px), --font-size-md(14px), --font-size-lg(16px)
- Weights: --font-weight-normal(400), --font-weight-semibold(600)
- Spacing: --space-1(4px) through --space-8(40px)
- Radius: --radius-sm(4px), --radius-md(6px), --radius-lg(8px), --radius-full(9999px)
- Motion: --transition-fast(150ms), --transition-normal(200ms), --ease-out, --ease-in-out
- Opacity: --opacity-disabled(0.4)
- Tracking: --tracking-tight(-0.01em), --tracking-normal(0), --tracking-wide(0.02em)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install CVA dependencies and create cn() utility</name>
  <files>gui-pro/package.json, gui-pro/src/shared/lib/cn.ts</files>
  <read_first>
    - gui-pro/package.json
    - gui-pro/src/shared/ui/Button.tsx
    - gui-pro/src/shared/ui/Badge.tsx
    - gui-pro/src/shared/ui/ErrorBanner.tsx
    - gui-pro/src/shared/styles/tokens.css
  </read_first>
  <action>
1. Run `cd gui-pro && npm install class-variance-authority tailwind-merge clsx` to install:
   - class-variance-authority@0.7.1 — CVA variant system
   - tailwind-merge@3.5.0 — safe Tailwind class merging
   - clsx@2.1.1 — conditional class concatenation

2. Create directory `gui-pro/src/shared/lib/` if it does not exist.

3. Create `gui-pro/src/shared/lib/cn.ts` with exact content:
```ts
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

This cn() function is the standard shadcn/ui pattern: clsx handles conditional classes, twMerge resolves Tailwind conflicts so consumer className overrides work correctly.
  </action>
  <verify>
    <automated>cd gui-pro && node -e "const {cn} = require('./src/shared/lib/cn.ts'); console.log('FAIL: cn loaded as CJS')" 2>/dev/null; grep -q "class-variance-authority" package.json && grep -q "tailwind-merge" package.json && grep -q "clsx" package.json && echo "DEPS_OK" || echo "DEPS_MISSING"; test -f src/shared/lib/cn.ts && echo "CN_OK" || echo "CN_MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - gui-pro/package.json contains "class-variance-authority" in dependencies
    - gui-pro/package.json contains "tailwind-merge" in dependencies
    - gui-pro/package.json contains "clsx" in dependencies
    - gui-pro/src/shared/lib/cn.ts exists and contains `export function cn(`
    - gui-pro/src/shared/lib/cn.ts imports from "clsx" and "tailwind-merge"
  </acceptance_criteria>
  <done>CVA, tailwind-merge, clsx installed. cn() utility created and exports correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Button with CVA + create story + update tests</name>
  <files>gui-pro/src/shared/ui/Button.tsx, gui-pro/src/shared/ui/Button.stories.tsx, gui-pro/src/shared/ui/Button.test.tsx</files>
  <read_first>
    - gui-pro/src/shared/ui/Button.tsx
    - gui-pro/src/shared/ui/Button.test.tsx
    - gui-pro/src/shared/styles/tokens.css
    - .planning/phases/02-ssh-port-change-core-engine/02-UI-SPEC.md
    - .planning/phases/02-ssh-port-change-core-engine/02-RESEARCH.md
  </read_first>
  <action>
**Button.tsx — full rewrite per D-01, D-04, D-05, D-06, UI-SPEC:**

Rewrite Button.tsx completely using CVA. The component must:
- Use `cva()` from class-variance-authority with `cn()` from shared/lib/cn
- Use `forwardRef` wrapping HTMLButtonElement
- Export both `Button` and `buttonVariants` (named exports)
- Define exactly 4 variants: primary, danger, ghost, icon (per D-06). REMOVE all other variants (secondary, danger-outline, success, warning)
- Define exactly 3 sizes: sm, md, lg (per D-06)
- Default: variant="primary", size="md"
- Support props: loading (boolean, shows Loader2 spinner), fullWidth (boolean), className (merged last via cn())
- All colors via CSS custom properties — zero hardcoded hex values

Variant class mappings (from 02-RESEARCH.md verified pattern):
- primary: bg-[var(--color-accent-interactive)] hover:bg-[var(--color-accent-hover)] active:bg-[var(--color-accent-active)] text-[var(--color-text-inverse)] border border-transparent
- danger: bg-[var(--color-destructive)] hover:opacity-90 text-white border border-transparent
- ghost: bg-transparent hover:bg-[var(--color-bg-hover)] active:bg-[var(--color-bg-active)] text-[var(--color-text-secondary)] border border-transparent
- icon: bg-transparent hover:bg-[var(--color-bg-hover)] active:bg-[var(--color-bg-active)] text-[var(--color-text-muted)] border border-transparent

Size class mappings:
- sm: h-8 px-3 text-[var(--font-size-sm)] gap-1.5
- md: h-8 px-4 text-[var(--font-size-sm)] gap-2
- lg: h-9 px-5 text-[var(--font-size-md)] gap-2

Base classes: inline-flex items-center justify-center font-[var(--font-weight-semibold)] rounded-[var(--radius-lg)] transition-all duration-[var(--transition-fast)] ease-[var(--ease-out)] active:scale-[0.97] disabled:opacity-[var(--opacity-disabled)] disabled:cursor-not-allowed disabled:pointer-events-none focus-visible:shadow-[var(--focus-ring)] outline-none

Loading state: show `<Loader2 className="w-4 h-4 animate-spin" />` replacing children, set disabled.

**Button.stories.tsx — per D-16, D-17, D-18, SB-04, SB-05, UI-SPEC Storybook Contract:**

Create co-located story file with:
- meta: title "Primitives/Button", component Button, tags: ["autodocs"]
- argTypes: variant (select: primary/danger/ghost/icon), size (select: sm/md/lg), loading (boolean), disabled (boolean), fullWidth (boolean)
- Stories: Default, Variants (render all 4 in a row), Sizes (render all 3 in a row), States (default/disabled/loading), IconVariant (with Lucide icon + aria-label)
- All stories use Tailwind gap/flex for layout inside render function

**Button.test.tsx — update for new API:**

Read existing tests. Remove tests for deleted variants (secondary, danger-outline, success, warning). Update remaining tests to work with new CVA prop API. Ensure:
- Renders with default variant/size
- Renders each variant (primary, danger, ghost, icon)
- Disabled state applies correctly
- Loading state shows spinner
- className prop merges correctly
  </action>
  <verify>
    <automated>cd gui-pro && npx vitest run src/shared/ui/Button.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - gui-pro/src/shared/ui/Button.tsx contains `import { cva` from "class-variance-authority"
    - gui-pro/src/shared/ui/Button.tsx contains `import { cn }` from "../lib/cn"
    - gui-pro/src/shared/ui/Button.tsx contains `forwardRef`
    - gui-pro/src/shared/ui/Button.tsx contains `export const buttonVariants = cva(`
    - gui-pro/src/shared/ui/Button.tsx contains variant keys: primary, danger, ghost, icon
    - gui-pro/src/shared/ui/Button.tsx does NOT contain "secondary" or "danger-outline" or "success" or "warning" as variant keys
    - gui-pro/src/shared/ui/Button.tsx contains size keys: sm, md, lg
    - gui-pro/src/shared/ui/Button.tsx does NOT contain any hardcoded hex color (#)
    - gui-pro/src/shared/ui/Button.tsx does NOT import from "./colors"
    - gui-pro/src/shared/ui/Button.stories.tsx exists and contains `title: "Primitives/Button"`
    - gui-pro/src/shared/ui/Button.stories.tsx contains `tags: ["autodocs"]`
    - gui-pro/src/shared/ui/Button.stories.tsx exports: Default, Variants, Sizes, States
    - gui-pro/src/shared/ui/Button.test.tsx passes (exit code 0)
  </acceptance_criteria>
  <done>Button fully redesigned with CVA, 4 variants, 3 sizes, story with all states, tests passing.</done>
</task>

<task type="auto">
  <name>Task 3: Redesign Badge and ErrorBanner with CVA + stories + tests</name>
  <files>gui-pro/src/shared/ui/Badge.tsx, gui-pro/src/shared/ui/Badge.test.tsx, gui-pro/src/shared/ui/Badge.stories.tsx, gui-pro/src/shared/ui/ErrorBanner.tsx, gui-pro/src/shared/ui/ErrorBanner.test.tsx, gui-pro/src/shared/ui/ErrorBanner.stories.tsx</files>
  <read_first>
    - gui-pro/src/shared/ui/Badge.tsx
    - gui-pro/src/shared/ui/Badge.test.tsx
    - gui-pro/src/shared/ui/ErrorBanner.tsx
    - gui-pro/src/shared/ui/ErrorBanner.test.tsx
    - gui-pro/src/shared/ui/colors.ts
    - gui-pro/src/shared/styles/tokens.css
    - .planning/phases/02-ssh-port-change-core-engine/02-UI-SPEC.md
  </read_first>
  <action>
**Badge.tsx — full rewrite per D-01, D-04, D-05, D-07:**

Rewrite Badge.tsx using CVA. The component must:
- Use cva() + cn() pattern, forwardRef
- Export both `Badge` and `badgeVariants`
- Define exactly 5 variants: success, warning, danger, neutral, dot (per D-07). REMOVE `accent` variant, replace with `neutral`.
- REMOVE `import { colors } from "./colors"` — zero colors.ts dependency
- All colors via status tokens from tokens.css

Variant class mappings:
- success: bg-[var(--color-status-connected-bg)] text-[var(--color-status-connected)] border border-[var(--color-status-connected-border)]
- warning: bg-[var(--color-status-connecting-bg)] text-[var(--color-status-connecting)] border border-[var(--color-status-connecting-border)]
- danger: bg-[var(--color-status-error-bg)] text-[var(--color-status-error)] border border-[var(--color-status-error-border)]
- neutral: bg-[var(--color-bg-elevated)] text-[var(--color-text-secondary)] border border-[var(--color-border)]
- dot: (dot-only indicator, no background, just a colored dot before text)

Base classes: inline-flex items-center gap-1.5 px-2 py-0.5 rounded-[var(--radius-full)] text-[var(--font-size-xs)] font-[var(--font-weight-semibold)] uppercase tracking-[var(--tracking-wide)]

Default: variant="neutral"

**ErrorBanner.tsx — full rewrite per D-01, D-04, D-05, D-08:**

Rewrite ErrorBanner.tsx using CVA. The component must:
- Use cva() + cn(), forwardRef
- Export both `ErrorBanner` and `errorBannerVariants`
- Define severity variants: error, warning, info (per D-08)
- REMOVE `import { colors } from "./colors"` — zero colors.ts dependency
- Support props: message (string), onDismiss (optional callback), severity (default "error")
- Use Lucide AlertTriangle for error/warning, Info icon for info
- Include dismiss X button

Severity class mappings:
- error: bg-[var(--color-status-error-bg)] text-[var(--color-status-error)] border border-[var(--color-status-error-border)]
- warning: bg-[var(--color-status-connecting-bg)] text-[var(--color-status-connecting)] border border-[var(--color-status-connecting-border)]
- info: bg-[var(--color-status-info-bg)] text-[var(--color-status-info)] border border-[var(--color-status-info-border)]

Base classes: flex items-center gap-2 px-[var(--space-3)] py-[var(--space-2)] rounded-[var(--radius-md)] text-[var(--font-size-sm)]

**Badge.stories.tsx and ErrorBanner.stories.tsx — per SB-04, SB-05:**

Each story file follows Storybook Contract from UI-SPEC:
- title: "Primitives/Badge" or "Primitives/ErrorBanner"
- tags: ["autodocs"]
- argTypes exposing CVA variant props
- Stories: Default, Variants (all in a row), States (if applicable)
- Badge stories: show all 5 variants including dot indicator
- ErrorBanner stories: Default (error), AllSeverities, Dismissible, WithLongMessage

**Test updates:**

Badge.test.tsx: Remove rgba assertions (e.g., `style.backgroundColor === "rgba(16, 185, 129, 0.15)"`), replace with class-based assertions checking for var(--color-status-*) presence. Remove `accent` variant tests, add `neutral`.

ErrorBanner.test.tsx: Remove hardcoded rgba assertions, add severity variant tests.
  </action>
  <verify>
    <automated>cd gui-pro && npx vitest run src/shared/ui/Badge.test.tsx src/shared/ui/ErrorBanner.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - gui-pro/src/shared/ui/Badge.tsx contains `import { cva` and does NOT contain `import { colors }`
    - gui-pro/src/shared/ui/Badge.tsx contains variant keys: success, warning, danger, neutral, dot
    - gui-pro/src/shared/ui/Badge.tsx does NOT contain "accent" as variant key
    - gui-pro/src/shared/ui/Badge.tsx does NOT contain hardcoded hex or rgba values
    - gui-pro/src/shared/ui/ErrorBanner.tsx contains `import { cva` and does NOT contain `import { colors }`
    - gui-pro/src/shared/ui/ErrorBanner.tsx contains severity variants: error, warning, info
    - gui-pro/src/shared/ui/ErrorBanner.tsx does NOT contain hardcoded hex or rgba values
    - gui-pro/src/shared/ui/Badge.stories.tsx exists with `title: "Primitives/Badge"` and `tags: ["autodocs"]`
    - gui-pro/src/shared/ui/ErrorBanner.stories.tsx exists with `title: "Primitives/ErrorBanner"` and `tags: ["autodocs"]`
    - Badge.test.tsx and ErrorBanner.test.tsx pass (exit code 0)
  </acceptance_criteria>
  <done>Badge (5 variants) and ErrorBanner (3 severity variants) fully redesigned, zero colors.ts imports, stories and tests complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> package.json | Third-party packages installed from npm |
| User className prop -> component rendering | Consumer can inject arbitrary Tailwind classes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | npm packages (CVA, tailwind-merge, clsx) | mitigate | Pin exact versions in package-lock.json; all three are established packages with >10M weekly downloads |
| T-02-01-02 | Information Disclosure | className prop injection | accept | className only affects CSS classes (visual), no data exposure; standard React pattern |
</threat_model>

<verification>
1. `cd gui-pro && grep -c "class-variance-authority" package.json` returns 1
2. `cd gui-pro && grep -rn "import.*colors" src/shared/ui/Button.tsx src/shared/ui/Badge.tsx src/shared/ui/ErrorBanner.tsx` returns empty (zero colors.ts imports)
3. `cd gui-pro && npx vitest run src/shared/ui/Button.test.tsx src/shared/ui/Badge.test.tsx src/shared/ui/ErrorBanner.test.tsx` all pass
4. `ls gui-pro/src/shared/ui/Button.stories.tsx gui-pro/src/shared/ui/Badge.stories.tsx gui-pro/src/shared/ui/ErrorBanner.stories.tsx` all exist
</verification>

<success_criteria>
- CVA, tailwind-merge, clsx installed in gui-pro
- cn() utility exists at shared/lib/cn.ts
- Button: 4 CVA variants + 3 sizes + story + passing tests
- Badge: 5 CVA variants + story + passing tests
- ErrorBanner: 3 severity CVA variants + story + passing tests
- Zero colors.ts imports in any of the three components
- Zero hardcoded hex/rgba in any of the three components
</success_criteria>

<output>
After completion, create `.planning/phases/02-ssh-port-change-core-engine/02-01-SUMMARY.md`
</output>
