---
phase: 02-ssh-port-change-core-engine
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - gui-app/src/shared/ui/Modal.tsx
  - gui-app/src/shared/ui/Modal.test.tsx
  - gui-app/src/shared/ui/Modal.stories.tsx
  - gui-app/src/shared/ui/ConfirmDialog.tsx
  - gui-app/src/shared/ui/ConfirmDialog.test.tsx
  - gui-app/src/shared/ui/ConfirmDialog.stories.tsx
  - gui-app/src/shared/ui/Toggle.tsx
  - gui-app/src/shared/ui/Toggle.test.tsx
  - gui-app/src/shared/ui/Toggle.stories.tsx
  - gui-app/src/shared/ui/Card.tsx
  - gui-app/src/shared/ui/Card.test.tsx
  - gui-app/src/shared/ui/Card.stories.tsx
  - gui-app/src/shared/ui/Tooltip.tsx
  - gui-app/src/shared/ui/Tooltip.test.tsx
  - gui-app/src/shared/ui/Tooltip.stories.tsx
autonomous: true
requirements: [COMP-01, COMP-06, COMP-14, SB-04, SB-05]

must_haves:
  truths:
    - "Modal has size variants (sm, md, lg) via CVA and uses --z-modal token instead of hardcoded 9000"
    - "ConfirmDialog uses redesigned Modal + Button internals"
    - "Toggle uses --color-toggle-on/off tokens, not colors.ts"
    - "Card uses --color-bg-surface, no hardcoded colors"
    - "Tooltip uses --z-dropdown token instead of hardcoded 9500"
    - "All 5 components have Storybook stories with all states"
  artifacts:
    - path: "gui-app/src/shared/ui/Modal.tsx"
      provides: "Token-driven Modal with size variants"
      exports: ["Modal"]
    - path: "gui-app/src/shared/ui/Toggle.tsx"
      provides: "Token-driven Toggle"
      exports: ["Toggle"]
    - path: "gui-app/src/shared/ui/Card.tsx"
      provides: "Token-driven Card"
      exports: ["Card", "CardHeader"]
  key_links:
    - from: "gui-app/src/shared/ui/ConfirmDialog.tsx"
      to: "gui-app/src/shared/ui/Modal.tsx"
      via: "imports Modal"
      pattern: "import.*Modal.*from.*Modal"
    - from: "gui-app/src/shared/ui/ConfirmDialog.tsx"
      to: "gui-app/src/shared/ui/Button.tsx"
      via: "imports Button"
      pattern: "import.*Button.*from.*Button"
---

<objective>
Redesign Modal (with size variants), ConfirmDialog, Toggle, Card, and Tooltip with token-driven styling and full Storybook stories.

Purpose: Modal is used by ConfirmDialog and other dialogs in the app. Toggle, Card, and Tooltip are foundational primitives used across multiple screens. Tooltip has a z-index conflict (9500 vs token scale) that must be fixed.
Output: 5 redesigned components, 5 story files, updated tests.
</objective>

<execution_context>
@C:/Users/naska/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/naska/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ssh-port-change-core-engine/02-CONTEXT.md
@.planning/phases/02-ssh-port-change-core-engine/02-RESEARCH.md
@.planning/phases/02-ssh-port-change-core-engine/02-UI-SPEC.md

<interfaces>
<!-- cn() from Plan 01 -->
From gui-app/src/shared/lib/cn.ts:
```ts
export function cn(...inputs: ClassValue[]): string;
```

<!-- Button from Plan 01 (ConfirmDialog uses it) -->
From gui-app/src/shared/ui/Button.tsx:
```ts
export const Button: React.ForwardRefExoticComponent<ButtonProps>;
// variants: primary, danger, ghost, icon; sizes: sm, md, lg
```

<!-- Key tokens -->
From gui-app/src/shared/styles/tokens.css:
- --z-modal: 300 (replaces hardcoded 9000)
- --z-dropdown: 100 (for Tooltip, replaces hardcoded 9500)
- --color-bg-surface (#1c1c1c dark / #f0f0ed light)
- --color-bg-elevated (#222222 dark / #e8e8e5 light)
- --color-toggle-on, --color-toggle-off
- --color-border
- --shadow-md, --shadow-lg
- --transition-normal (200ms) for modal open/close
- --ease-in-out for modal animations
- --radius-lg (8px), --radius-md (6px)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Modal with size variants + ConfirmDialog + stories + tests</name>
  <files>gui-app/src/shared/ui/Modal.tsx, gui-app/src/shared/ui/Modal.test.tsx, gui-app/src/shared/ui/Modal.stories.tsx, gui-app/src/shared/ui/ConfirmDialog.tsx, gui-app/src/shared/ui/ConfirmDialog.test.tsx, gui-app/src/shared/ui/ConfirmDialog.stories.tsx</files>
  <read_first>
    - gui-app/src/shared/ui/Modal.tsx
    - gui-app/src/shared/ui/Modal.test.tsx
    - gui-app/src/shared/ui/ConfirmDialog.tsx
    - gui-app/src/shared/ui/ConfirmDialog.test.tsx
    - gui-app/src/shared/styles/tokens.css
    - .planning/phases/02-ssh-port-change-core-engine/02-UI-SPEC.md
  </read_first>
  <action>
**Modal.tsx — full rewrite per D-01, D-05, COMP-06, UI-SPEC:**

Rewrite Modal.tsx. The component must:
- Use cn() for class merging, forwardRef where applicable
- Add size variants via CVA or simple size prop mapping:
  - sm: max-w-sm (max-width: 24rem / 384px)
  - md: max-w-md (max-width: 28rem / 448px) — default
  - lg: max-w-lg (max-width: 32rem / 512px)
- REMOVE hardcoded `zIndex: 9000` — replace with z-[var(--z-modal)]
- REMOVE hardcoded `backgroundColor: "rgba(0,0,0,0.4)"` for overlay — use bg-black/40 or bg-[var(--color-bg-primary)] with opacity
- Overlay: fixed inset-0 z-[var(--z-modal)] bg-black/40 flex items-center justify-center
- Modal panel: bg-[var(--color-bg-surface)] border border-[var(--color-border)] rounded-[var(--radius-lg)] shadow-[var(--shadow-lg)] p-[var(--space-6)]
- Transition: use --transition-normal (200ms) + --ease-in-out for open/close
- Props: isOpen, onClose, title (optional), size (sm/md/lg), children, className
- Render via createPortal to document.body (preserve existing pattern)
- Close on overlay click (outside modal panel) and Escape key

**ConfirmDialog.tsx — update per D-01, UI-SPEC Copywriting:**

Update ConfirmDialog to use redesigned Modal + Button:
- Use Modal with appropriate size (sm by default for confirm dialogs)
- Confirm button: `<Button variant="danger">` with text from prop (default "Удалить" per UI-SPEC)
- Cancel button: `<Button variant="ghost">` with text "Отмена" (per UI-SPEC)
- Props: isOpen, onConfirm, onCancel, title, message, confirmText (default "Удалить"), cancelText (default "Отмена")
- Use cn() for class merging
- Zero hardcoded colors

**Stories — per SB-04, SB-05:**

Modal.stories.tsx:
- title: "Primitives/Modal", tags: ["autodocs"]
- Stories: Default (md), Small, Large, WithTitle, WithLongContent
- All stories must include a trigger button to open the modal (use useState or args)

ConfirmDialog.stories.tsx:
- title: "Primitives/ConfirmDialog", tags: ["autodocs"]
- Stories: Default, CustomText, WithLongMessage

**Tests:**
- Modal.test.tsx: Update for new API (size prop, z-index token). Test: renders when isOpen=true, closes on overlay click, closes on Escape, size variants render correct max-width classes
- ConfirmDialog.test.tsx: Update for new Button variants. Test: renders confirm/cancel buttons, calls onConfirm/onCancel, default button text is "Удалить"/"Отмена"
  </action>
  <verify>
    <automated>cd gui-app && npx vitest run src/shared/ui/Modal.test.tsx src/shared/ui/ConfirmDialog.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - gui-app/src/shared/ui/Modal.tsx does NOT contain `9000` or `zIndex: 9000`
    - gui-app/src/shared/ui/Modal.tsx contains `var(--z-modal)` or `--z-modal`
    - gui-app/src/shared/ui/Modal.tsx does NOT contain `rgba(0,0,0` as inline style
    - gui-app/src/shared/ui/Modal.tsx accepts `size` prop with values sm, md, lg
    - gui-app/src/shared/ui/ConfirmDialog.tsx contains "Удалить" as default confirm text
    - gui-app/src/shared/ui/ConfirmDialog.tsx contains "Отмена" as default cancel text
    - gui-app/src/shared/ui/Modal.stories.tsx exists with title "Primitives/Modal"
    - gui-app/src/shared/ui/ConfirmDialog.stories.tsx exists with title "Primitives/ConfirmDialog"
    - Modal.test.tsx and ConfirmDialog.test.tsx pass
  </acceptance_criteria>
  <done>Modal redesigned with size variants and z-index token. ConfirmDialog uses redesigned Modal + Button. Stories and tests complete.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Toggle, Card, Tooltip + stories + tests</name>
  <files>gui-app/src/shared/ui/Toggle.tsx, gui-app/src/shared/ui/Toggle.test.tsx, gui-app/src/shared/ui/Toggle.stories.tsx, gui-app/src/shared/ui/Card.tsx, gui-app/src/shared/ui/Card.test.tsx, gui-app/src/shared/ui/Card.stories.tsx, gui-app/src/shared/ui/Tooltip.tsx, gui-app/src/shared/ui/Tooltip.test.tsx, gui-app/src/shared/ui/Tooltip.stories.tsx</files>
  <read_first>
    - gui-app/src/shared/ui/Toggle.tsx
    - gui-app/src/shared/ui/Toggle.test.tsx
    - gui-app/src/shared/ui/Card.tsx
    - gui-app/src/shared/ui/Card.test.tsx
    - gui-app/src/shared/ui/Tooltip.tsx
    - gui-app/src/shared/ui/Tooltip.test.tsx
    - gui-app/src/shared/styles/tokens.css
    - .planning/phases/02-ssh-port-change-core-engine/02-UI-SPEC.md
  </read_first>
  <action>
**Toggle.tsx — full rewrite per D-01, D-05, COMP-01:**

Rewrite Toggle.tsx:
- Use cn() for class merging, forwardRef
- Track: bg-[var(--color-toggle-off)] when unchecked, bg-[var(--color-toggle-on)] when checked
- Knob: white circle that slides left/right
- ADD focus-visible: shadow-[var(--focus-ring)] (currently missing per Research audit)
- Disabled: opacity-[var(--opacity-disabled)] cursor-not-allowed
- Transition: duration-[var(--transition-fast)]
- Props: checked (boolean), onChange (callback), disabled, label (optional), className
- REMOVE any colors.ts import
- Zero hardcoded colors
- Use `role="switch"` + `aria-checked` for accessibility

**Card.tsx — full rewrite per D-01, D-05, COMP-01:**

Rewrite Card.tsx:
- Use cn() for class merging
- bg-[var(--color-bg-surface)] border border-[var(--color-border)] rounded-[var(--radius-lg)] p-[var(--space-4)]
- Shadow: shadow-[var(--shadow-sm)] (subtle)
- CardHeader: flex items-center justify-between, text-[var(--font-size-lg)] font-[var(--font-weight-semibold)]
- Export both Card and CardHeader
- Zero hardcoded colors

**Tooltip.tsx — full rewrite per D-01, D-05, COMP-01, Pitfall 3:**

Rewrite Tooltip.tsx:
- REMOVE hardcoded `z-[9500]` — replace with z-[var(--z-dropdown)]
- bg-[var(--color-bg-elevated)] text-[var(--color-text-primary)] shadow-[var(--shadow-md)] rounded-[var(--radius-sm)]
- Text: text-[var(--font-size-xs)] px-[var(--space-2)] py-1
- Transition: duration-[var(--transition-fast)]
- Preserve existing positioning logic (top/bottom/left/right)
- Use cn() for class merging
- Zero hardcoded colors

**Stories (3 files) — per SB-04, SB-05:**

Toggle.stories.tsx:
- title: "Primitives/Toggle", tags: ["autodocs"]
- Stories: Default, Checked, Disabled, WithLabel, AllStates

Card.stories.tsx:
- title: "Primitives/Card", tags: ["autodocs"]
- Stories: Default, WithHeader, WithContent, Empty

Tooltip.stories.tsx:
- title: "Primitives/Tooltip", tags: ["autodocs"]
- Stories: Default, Positions (top/bottom/left/right), LongText

**Tests:**
- Toggle.test.tsx: Update, ensure focus-visible test, check aria-checked, check CSS var classes
- Card.test.tsx: Update for new token classes
- Tooltip.test.tsx: Update, verify no hardcoded 9500 z-index
  </action>
  <verify>
    <automated>cd gui-app && npx vitest run src/shared/ui/Toggle.test.tsx src/shared/ui/Card.test.tsx src/shared/ui/Tooltip.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - gui-app/src/shared/ui/Toggle.tsx contains `focus-visible` styling
    - gui-app/src/shared/ui/Toggle.tsx contains `role="switch"` and `aria-checked`
    - gui-app/src/shared/ui/Toggle.tsx does NOT import from "./colors"
    - gui-app/src/shared/ui/Card.tsx contains `var(--color-bg-surface)`
    - gui-app/src/shared/ui/Card.tsx does NOT contain hardcoded hex colors
    - gui-app/src/shared/ui/Tooltip.tsx does NOT contain `9500` or `z-[9500]`
    - gui-app/src/shared/ui/Tooltip.tsx contains `var(--z-dropdown)` or `--z-dropdown`
    - gui-app/src/shared/ui/Toggle.stories.tsx exists with title "Primitives/Toggle"
    - gui-app/src/shared/ui/Card.stories.tsx exists with title "Primitives/Card"
    - gui-app/src/shared/ui/Tooltip.stories.tsx exists with title "Primitives/Tooltip"
    - All tests pass
  </acceptance_criteria>
  <done>Toggle (with focus-visible fix), Card, Tooltip (z-index fixed) redesigned. Stories and tests complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| children prop -> Modal content | Arbitrary React content rendered inside modal |
| Tooltip content -> DOM | User-provided tooltip text rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | Modal overlay click bypass | accept | Standard modal pattern; escape key + overlay click to close. No sensitive data in modal itself. |
| T-02-03-02 | Denial of Service | Tooltip rapid hover | accept | Standard CSS transition; no API calls triggered by hover |
</threat_model>

<verification>
1. `cd gui-app && npx vitest run src/shared/ui/Modal.test.tsx src/shared/ui/ConfirmDialog.test.tsx src/shared/ui/Toggle.test.tsx src/shared/ui/Card.test.tsx src/shared/ui/Tooltip.test.tsx` all pass
2. `grep -n "9000\|9500" gui-app/src/shared/ui/Modal.tsx gui-app/src/shared/ui/Tooltip.tsx` returns zero matches
3. 5 .stories.tsx files exist
</verification>

<success_criteria>
- Modal has sm/md/lg size variants, uses --z-modal token
- ConfirmDialog uses redesigned Modal + Button with "Удалить"/"Отмена" defaults
- Toggle has focus-visible, uses --color-toggle-on/off tokens
- Card uses --color-bg-surface
- Tooltip uses --z-dropdown token (not 9500)
- All 5 have stories with autodocs
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-ssh-port-change-core-engine/02-03-SUMMARY.md`
</output>
