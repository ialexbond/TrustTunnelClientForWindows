---
phase: 02-ssh-port-change-core-engine
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - gui-pro/src/shared/ui/Select.tsx
  - gui-pro/src/shared/ui/Select.test.tsx
  - gui-pro/src/shared/ui/Select.stories.tsx
  - gui-pro/src/shared/ui/SnackBar.tsx
  - gui-pro/src/shared/ui/SnackBar.test.tsx
  - gui-pro/src/shared/ui/SnackBar.stories.tsx
  - gui-pro/src/shared/ui/SnackBarContext.tsx
  - gui-pro/src/shared/ui/IconButton.tsx
  - gui-pro/src/shared/ui/IconButton.stories.tsx
  - gui-pro/src/shared/ui/DropOverlay.tsx
  - gui-pro/src/shared/ui/DropOverlay.test.tsx
  - gui-pro/src/shared/ui/DropOverlay.stories.tsx
  - gui-pro/src/shared/ui/PanelErrorBoundary.tsx
  - gui-pro/src/shared/ui/PanelErrorBoundary.stories.tsx
autonomous: true
requirements: [COMP-01, COMP-05, COMP-14, SB-04, SB-05]

must_haves:
  truths:
    - "Select is a custom dropdown (not native) with keyboard navigation (ArrowUp/Down, Enter, Escape)"
    - "Select uses --z-dropdown token, not hardcoded z-index"
    - "Select has zero colors.ts imports"
    - "SnackBar uses --color-status-error token, not var(--color-error) with orange fallback"
    - "IconButton requires aria-label prop (TypeScript enforced)"
    - "DropOverlay uses token vars instead of hardcoded #fff and fontWeight 500"
    - "PanelErrorBoundary uses --color-bg-surface instead of nonexistent --color-bg-tertiary"
    - "All 5 redesigned components have Storybook stories"
  artifacts:
    - path: "gui-pro/src/shared/ui/Select.tsx"
      provides: "Custom dropdown with keyboard navigation"
      exports: ["Select"]
    - path: "gui-pro/src/shared/ui/IconButton.tsx"
      provides: "IconButton with required aria-label"
      exports: ["IconButton"]
    - path: "gui-pro/src/shared/ui/SnackBar.tsx"
      provides: "Token-driven SnackBar"
      exports: ["SnackBar"]
  key_links:
    - from: "gui-pro/src/shared/ui/Select.tsx"
      to: "gui-pro/src/shared/hooks/useDropdownPortal"
      via: "import useDropdownPortal"
      pattern: "useDropdownPortal"
---

<objective>
Redesign Select (custom dropdown with keyboard nav), SnackBar, SnackBarContext, IconButton, DropOverlay, and PanelErrorBoundary with token-driven styling and Storybook stories.

Purpose: These are the remaining existing components. Select is the most complex (keyboard navigation + portal), SnackBar has a broken token reference (--color-error with orange fallback), IconButton needs mandatory aria-label, DropOverlay/PanelErrorBoundary have hardcoded values that must become tokens.
Output: 5 redesigned components (SnackBarContext is provider-only, no visual), 5 story files, updated tests.
</objective>

<execution_context>
@C:/Users/naska/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/naska/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ssh-port-change-core-engine/02-CONTEXT.md
@.planning/phases/02-ssh-port-change-core-engine/02-RESEARCH.md
@.planning/phases/02-ssh-port-change-core-engine/02-UI-SPEC.md

<interfaces>
<!-- cn() from Plan 01 -->
From gui-pro/src/shared/lib/cn.ts:
```ts
export function cn(...inputs: ClassValue[]): string;
```

<!-- Existing hook to preserve -->
From gui-pro/src/shared/hooks/useDropdownPortal:
- useDropdownPortal() — handles dropdown positioning via getBoundingClientRect + createPortal
- MUST be preserved, not rewritten

<!-- Key tokens -->
From gui-pro/src/shared/styles/tokens.css:
- --z-dropdown: 100 (for Select dropdown, Tooltip)
- --z-snackbar: 400 (for SnackBar)
- --color-bg-elevated (#222222 dark / #e8e8e5 light) — for dropdown panels
- --color-bg-surface (#1c1c1c dark / #f0f0ed light) — for PanelErrorBoundary
- --color-status-error — replaces broken var(--color-error) in SnackBar
- --color-text-inverse — replaces hardcoded #fff in DropOverlay
- --shadow-md — for dropdown/tooltip shadows
- --font-size-lg (16px) — replaces hardcoded 18px in DropOverlay
- --font-weight-semibold (600) — replaces hardcoded 500 in DropOverlay
- --color-danger-400 — replaces text-red-500 in PanelErrorBoundary
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Select (custom dropdown with keyboard nav) + story + tests</name>
  <files>gui-pro/src/shared/ui/Select.tsx, gui-pro/src/shared/ui/Select.test.tsx, gui-pro/src/shared/ui/Select.stories.tsx</files>
  <read_first>
    - gui-pro/src/shared/ui/Select.tsx
    - gui-pro/src/shared/ui/Select.test.tsx
    - gui-pro/src/shared/ui/colors.ts
    - gui-pro/src/shared/styles/tokens.css
    - .planning/phases/02-ssh-port-change-core-engine/02-UI-SPEC.md
    - .planning/phases/02-ssh-port-change-core-engine/02-RESEARCH.md
  </read_first>
  <action>
**Select.tsx — full rewrite per D-01, D-05, D-10, COMP-05, UI-SPEC, Pitfall 3:**

This is the most complex component in Phase 2. The existing Select is already custom (uses useDropdownPortal + createPortal). Rewrite it:

1. REMOVE `import { colors } from "./colors"` — zero colors.ts dependency
2. Replace all `colors.dropdownShadow`, `colors.accentBg`, etc. with CSS var tokens
3. Replace hardcoded `color: "var(--color-accent-500)"` (non-semantic!) with `text-[var(--color-accent-interactive)]`
4. Replace inline zIndex values with z-[var(--z-dropdown)]
5. PRESERVE useDropdownPortal hook (do NOT rewrite it — per Research "Don't Hand-Roll")
6. PRESERVE createPortal rendering pattern

Add keyboard navigation (per COMP-05, UI-SPEC):
- ArrowDown: move highlight to next option
- ArrowUp: move highlight to previous option
- Enter: select highlighted option, close dropdown
- Escape: close dropdown without selecting
- Tab: close dropdown, move focus to next element
- Home/End: jump to first/last option

Add ARIA attributes (per UI-SPEC Accessibility):
- Trigger: role="combobox", aria-expanded, aria-haspopup="listbox", aria-activedescendant
- Dropdown: role="listbox"
- Each option: role="option", aria-selected, id for activedescendant reference

Styling:
- Trigger: same base style as Input — h-8 rounded-[var(--radius-md)] border border-[var(--color-input-border)] bg-[var(--color-input-bg)] px-[var(--space-3)] text-[var(--font-size-md)]
- Trigger icon: Lucide ChevronDown, right side, rotates on open
- Dropdown panel: bg-[var(--color-bg-elevated)] border border-[var(--color-border)] rounded-[var(--radius-md)] shadow-[var(--shadow-md)] z-[var(--z-dropdown)]
- Option: px-[var(--space-3)] py-[var(--space-2)] hover:bg-[var(--color-bg-hover)] text-[var(--font-size-md)]
- Selected option: bg-[var(--color-bg-active)] with Lucide Check icon
- Placeholder: "Выберите..." (per UI-SPEC Copywriting, overridable via prop)
- Disabled: opacity-[var(--opacity-disabled)] cursor-not-allowed
- Focus-visible on trigger: shadow-[var(--focus-ring)]

Props: options (array of {value, label}), value, onChange, placeholder (default "Выберите..."), disabled, className

Use cn() for all class merging. forwardRef on the trigger element.

**Select.stories.tsx — per SB-04, SB-05:**
- title: "Primitives/Select", tags: ["autodocs"]
- Stories: Default, WithSelection, WithManyOptions (15+ items to test scroll), Disabled, WithPlaceholder
- Note: Storybook preview.ts uses layout: "fullscreen" which works with createPortal

**Select.test.tsx — update:**
- Remove any colors.ts-based assertions
- Test: renders trigger, opens dropdown on click, selects option, keyboard nav (ArrowDown/Enter), closes on Escape
  </action>
  <verify>
    <automated>cd gui-pro && npx vitest run src/shared/ui/Select.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - gui-pro/src/shared/ui/Select.tsx does NOT contain `import { colors }`
    - gui-pro/src/shared/ui/Select.tsx does NOT contain hardcoded `color: "var(--color-accent-500)"` (must use semantic --color-accent-interactive)
    - gui-pro/src/shared/ui/Select.tsx contains `role="listbox"` or `role="combobox"`
    - gui-pro/src/shared/ui/Select.tsx contains `aria-activedescendant` or `aria-expanded`
    - gui-pro/src/shared/ui/Select.tsx contains keyboard handler for ArrowDown, ArrowUp, Enter, Escape
    - gui-pro/src/shared/ui/Select.tsx contains `var(--z-dropdown)` or `--z-dropdown`
    - gui-pro/src/shared/ui/Select.tsx contains "Выберите..." as default placeholder
    - gui-pro/src/shared/ui/Select.stories.tsx exists with title "Primitives/Select"
    - Select.test.tsx passes
  </acceptance_criteria>
  <done>Select fully rewritten as custom dropdown with keyboard navigation, ARIA attributes, token-driven styling, zero colors.ts dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign SnackBar, IconButton, DropOverlay, PanelErrorBoundary + stories</name>
  <files>gui-pro/src/shared/ui/SnackBar.tsx, gui-pro/src/shared/ui/SnackBar.test.tsx, gui-pro/src/shared/ui/SnackBar.stories.tsx, gui-pro/src/shared/ui/SnackBarContext.tsx, gui-pro/src/shared/ui/IconButton.tsx, gui-pro/src/shared/ui/IconButton.stories.tsx, gui-pro/src/shared/ui/DropOverlay.tsx, gui-pro/src/shared/ui/DropOverlay.test.tsx, gui-pro/src/shared/ui/DropOverlay.stories.tsx, gui-pro/src/shared/ui/PanelErrorBoundary.tsx, gui-pro/src/shared/ui/PanelErrorBoundary.stories.tsx</files>
  <read_first>
    - gui-pro/src/shared/ui/SnackBar.tsx
    - gui-pro/src/shared/ui/SnackBar.test.tsx
    - gui-pro/src/shared/ui/SnackBarContext.tsx
    - gui-pro/src/shared/ui/IconButton.tsx
    - gui-pro/src/shared/ui/DropOverlay.tsx
    - gui-pro/src/shared/ui/DropOverlay.test.tsx
    - gui-pro/src/shared/ui/PanelErrorBoundary.tsx
    - gui-pro/src/shared/styles/tokens.css
    - .planning/phases/02-ssh-port-change-core-engine/02-UI-SPEC.md
    - .planning/phases/02-ssh-port-change-core-engine/02-RESEARCH.md
  </read_first>
  <action>
**SnackBar.tsx — rewrite per D-01, D-05, COMP-01, Pitfall 2:**

Key fix: Replace `var(--color-error, #f97316)` (nonexistent token with ORANGE fallback!) with `var(--color-status-error)` (correct red token).

Full rewrite:
- Use cn(), z-[var(--z-snackbar)] (replaces any hardcoded z-index)
- bg-[var(--color-bg-elevated)] text-[var(--color-text-primary)] shadow-[var(--shadow-lg)]
- Error state: border-l-2 border-[var(--color-status-error)] or colored left border
- Position: fixed bottom-4 right-4 (or however currently positioned)
- Transition: duration-[var(--transition-fast)]
- Zero hardcoded colors/hex

**SnackBarContext.tsx — minor update:**
- This is a context provider with no visual output. Just verify it does NOT import colors.ts. If it does, remove the import. Otherwise, no changes needed.

**IconButton.tsx — rewrite per D-01, COMP-01, UI-SPEC Accessibility:**

Key change: `aria-label` prop is REQUIRED (TypeScript enforced):
```ts
interface IconButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  "aria-label": string;  // REQUIRED per UI-SPEC
  icon: ReactNode;
  // ... other props
}
```

- If aria-label is absent in dev mode, log `console.error("IconButton requires aria-label for accessibility")`
- Align styling with Button variant="icon" pattern: bg-transparent hover:bg-[var(--color-bg-hover)] active:bg-[var(--color-bg-active)]
- Size: h-8 w-8 (32x32px minimum per UI-SPEC)
- focus-visible: shadow-[var(--focus-ring)]
- Use cn(), forwardRef
- Zero hardcoded colors

**DropOverlay.tsx — rewrite per D-01, D-05, COMP-01, Research audit:**

Replace hardcoded values:
- `color: "#fff"` -> text-[var(--color-text-inverse)]
- `fontSize: "18px"` -> text-[var(--font-size-lg)]
- `fontWeight: 500` -> font-[var(--font-weight-semibold)]
- All inline styles converted to Tailwind bracket syntax with token vars
- Use cn() for class merging
- Zero hardcoded colors, zero inline style for colors/typography

**PanelErrorBoundary.tsx — rewrite per D-01, D-05, COMP-01, Pitfall 1:**

Replace broken references:
- `text-red-500` -> text-[var(--color-danger-400)]
- `var(--color-bg-tertiary)` (DOES NOT EXIST in tokens.css!) -> bg-[var(--color-bg-surface)]
- Use cn() for class merging
- Error fallback UI: centered layout with error icon, error message text, "Retry" button using Button component
- Zero hardcoded colors

**Stories (4 files, no story for SnackBarContext) — per SB-04, SB-05:**

SnackBar.stories.tsx:
- title: "Primitives/SnackBar", tags: ["autodocs"]
- Stories: Default, Error, Success, AutoDismiss

IconButton.stories.tsx:
- title: "Primitives/IconButton", tags: ["autodocs"]
- Stories: Default (with aria-label), Disabled, HoverState
- ALL stories must include aria-label prop (per UI-SPEC)

DropOverlay.stories.tsx:
- title: "Primitives/DropOverlay", tags: ["autodocs"]
- Stories: Default, Active (showing drag state)

PanelErrorBoundary.stories.tsx:
- title: "Primitives/PanelErrorBoundary", tags: ["autodocs"]
- Stories: Default (showing error fallback), WithRetry

**Tests:**
- SnackBar.test.tsx: Update for new tokens, verify no orange fallback
- DropOverlay.test.tsx: Update for new token classes
  </action>
  <verify>
    <automated>cd gui-pro && npx vitest run src/shared/ui/SnackBar.test.tsx src/shared/ui/DropOverlay.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - gui-pro/src/shared/ui/SnackBar.tsx does NOT contain `var(--color-error` (the broken nonexistent token)
    - gui-pro/src/shared/ui/SnackBar.tsx contains `var(--color-status-error)` or `--color-status-error`
    - gui-pro/src/shared/ui/SnackBar.tsx contains `var(--z-snackbar)` or `--z-snackbar`
    - gui-pro/src/shared/ui/IconButton.tsx contains `"aria-label": string` as required prop
    - gui-pro/src/shared/ui/IconButton.tsx contains `console.error` for missing aria-label (dev warning)
    - gui-pro/src/shared/ui/DropOverlay.tsx does NOT contain `"#fff"` or `"#FFF"` hardcoded
    - gui-pro/src/shared/ui/DropOverlay.tsx does NOT contain `fontSize: "18px"` inline style
    - gui-pro/src/shared/ui/PanelErrorBoundary.tsx does NOT contain `text-red-500`
    - gui-pro/src/shared/ui/PanelErrorBoundary.tsx does NOT contain `--color-bg-tertiary`
    - gui-pro/src/shared/ui/PanelErrorBoundary.tsx contains `var(--color-bg-surface)`
    - 4 story files exist: SnackBar.stories.tsx, IconButton.stories.tsx, DropOverlay.stories.tsx, PanelErrorBoundary.stories.tsx
    - All stories contain `tags: ["autodocs"]`
    - Tests pass
  </acceptance_criteria>
  <done>SnackBar (fixed broken token), IconButton (required aria-label), DropOverlay (token migration), PanelErrorBoundary (fixed nonexistent token) all redesigned with stories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Select options -> DOM rendering | Options array rendered as dropdown items |
| SnackBar message -> DOM | Consumer message displayed to user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Spoofing | Select keyboard events | accept | Keyboard handlers only navigate options list; no data mutation |
| T-02-04-02 | Information Disclosure | SnackBar auto-dismiss | accept | SnackBar shows consumer-provided messages, no secrets exposed |
| T-02-04-03 | Tampering | PanelErrorBoundary error display | accept | Error boundary catches React errors; componentStack shown only in dev mode |
</threat_model>

<verification>
1. `grep -n "import.*colors" gui-pro/src/shared/ui/Select.tsx gui-pro/src/shared/ui/SnackBar.tsx` returns zero matches
2. `grep -n "color-error\|#f97316" gui-pro/src/shared/ui/SnackBar.tsx` returns zero matches
3. `grep -n "color-bg-tertiary\|text-red-500" gui-pro/src/shared/ui/PanelErrorBoundary.tsx` returns zero matches
4. `grep -n "9500\|#fff\|fontWeight.*500\|fontSize.*18" gui-pro/src/shared/ui/Select.tsx gui-pro/src/shared/ui/DropOverlay.tsx` returns zero matches
5. All tests pass, all story files exist
</verification>

<success_criteria>
- Select: custom dropdown with keyboard nav, ARIA, zero colors.ts
- SnackBar: uses correct --color-status-error token (not broken --color-error)
- IconButton: aria-label is TypeScript-required, dev warning on missing
- DropOverlay: all hardcoded values replaced with token vars
- PanelErrorBoundary: --color-bg-tertiary replaced with --color-bg-surface, text-red-500 replaced
- All have stories, tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-ssh-port-change-core-engine/02-04-SUMMARY.md`
</output>
