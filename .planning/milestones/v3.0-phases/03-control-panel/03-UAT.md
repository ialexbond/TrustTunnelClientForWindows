---
status: testing
phase: 03-control-panel
source: [03-01-SUMMARY.md, 03-02-SUMMARY.md, 03-03-SUMMARY.md]
started: 2026-04-14T16:10:00+05:00
updated: 2026-04-14T16:10:00+05:00
---

## Current Test

number: 1
name: Sidebar отображается с пустым списком серверов
expected: |
  При запуске приложения без сохранённых credentials слева видна панель "СЕРВЕРЫ" с иконкой и текстом "Нет серверов". Внизу кнопка "+ Добавить сервер".
awaiting: user response

## Tests

### 1. Sidebar отображается с пустым списком серверов
expected: При запуске без credentials слева панель "СЕРВЕРЫ" с "Нет серверов" и кнопка "+ Добавить сервер" внизу.
result: [pending]

### 2. Форма подключения встроена в правую панель
expected: Справа от sidebar — форма с заголовком "ПОДКЛЮЧЕНИЕ К СЕРВЕРУ", поля IP, порт, имя пользователя, переключатель авторизации. Нет отдельной карточки по центру экрана.
result: [pending]

### 3. Переключатель авторизации — 2 сегмента
expected: Под полями ввода — переключатель "Пароль | SSH-ключ" в виде двух сегментов в одной полоске. Активный сегмент подсвечен цветом.
result: [pending]

### 4. SSH-ключ: файл + разделитель "или" + вставка
expected: При выборе "SSH-ключ" появляется выбор файла ключа, затем разделитель с надписью "или", затем поле для вставки ключа. Оба варианта видны одновременно.
result: [pending]

### 5. Подключение к серверу работает
expected: После ввода корректных данных (IP, порт, логин, пароль) и нажатия "Подключиться" — приложение подключается, в sidebar появляется сервер с зелёной точкой.
result: [pending]

### 6. Табы вместо скролла секций
expected: После подключения к серверу справа — строка табов (Статус, Пользователи, Конфигурация, Безопасность, Инструменты, Опасная зона). Клик по табу показывает соответствующую секцию. Не нужно скроллить через все 9 секций.
result: [pending]

### 7. Header подключённого сервера
expected: Над табами — компактная строка с зелёной точкой, IP:порт сервера и кнопкой "Сменить сервер".
result: [pending]

### 8. Кнопка "Сменить сервер" отключает и возвращает к форме
expected: При нажатии "Сменить сервер" — сервер отключается, sidebar снова показывает "Нет серверов", справа форма подключения.
result: [pending]

### 9. VPN ошибка показывает ErrorBanner
expected: Если VPN выдаёт ошибку — под строкой статуса появляется красный блок с текстом ошибки и кнопкой закрытия (X). Блок можно закрыть.
result: [pending]

### 10. Storybook — все компоненты рендерятся
expected: В Storybook (npx storybook dev -p 6006) в секции Screens все три компонента (ControlPanelPage, StatusPanel, SshConnectForm) рендерятся без ошибок. StatusPanel показывает все 6 состояний.
result: [pending]

## Summary

total: 10
passed: 0
issues: 0
pending: 10
skipped: 0
blocked: 0

## Gaps
