---
phase: 04-application-shell
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - gui-pro/src/shared/types.ts
  - gui-pro/src/shared/i18n/locales/en.json
  - gui-pro/src/shared/i18n/locales/ru.json
  - gui-pro/src/shared/hooks/useKeyboardShortcuts.ts
  - gui-pro/src/components/Header.tsx
  - gui-pro/src/components/Header.test.tsx
autonomous: true
requirements:
  - SCR-07
  - DOC-04

must_haves:
  truths:
    - "AppTab type is the new 5-value union exported from shared/types.ts"
    - "Old SidebarPage consumers can use AppTab without breakage"
    - "i18n keys for all 5 new tabs exist in both ru.json and en.json"
    - "Keyboard shortcuts navigate to 5 tabs instead of 8 pages"
    - "Header.tsx dead code is removed"
    - "ROADMAP.md reflects the scope change from Remaining Panels to Application Shell"
  artifacts:
    - path: "gui-pro/src/shared/types.ts"
      provides: "AppTab 5-value union type"
      contains: "export type AppTab"
    - path: "gui-pro/src/shared/i18n/locales/ru.json"
      provides: "Russian i18n keys for tabs"
      contains: "tabs.connection"
    - path: "gui-pro/src/shared/i18n/locales/en.json"
      provides: "English i18n keys for tabs"
      contains: "tabs.connection"
  key_links:
    - from: "gui-pro/src/shared/types.ts"
      to: "gui-pro/src/App.tsx"
      via: "AppTab import"
      pattern: "import.*AppTab.*from.*shared/types"
---

<objective>
Foundation work for the Application Shell phase: define the new AppTab type, add i18n keys for all 5 tabs, update keyboard shortcuts, clean up dead code (Header.tsx), and update ROADMAP.md to reflect the scope change per D-19.

Purpose: Establishes the type contracts and i18n strings that all subsequent plans depend on. Without this, TitleBar, TabNavigation, and App.tsx refactor cannot proceed with correct types and labels.

Output: Updated shared/types.ts with new AppTab, i18n files with tab keys, updated keyboard shortcuts, Header.tsx removed, ROADMAP.md updated.
</objective>

<execution_context>
@C:\Users\naska\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\naska\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-mtproto-proxy/04-CONTEXT.md
@.planning/phases/04-mtproto-proxy/04-RESEARCH.md
@.planning/phases/04-mtproto-proxy/04-UI-SPEC.md

<interfaces>
<!-- Current shared/types.ts — AppTab will be replaced -->
From gui-pro/src/shared/types.ts:
```typescript
/** @deprecated Use SidebarPage instead — kept for backward compat with Header.tsx */
export type AppTab = "setup" | "settings" | "routing" | "about";
export type ThemeMode = "system" | "dark" | "light";
```

From gui-pro/src/components/layout/Sidebar.tsx:
```typescript
export type SidebarPage =
  | "server"
  | "control"
  | "settings"
  | "dashboard"
  | "routing"
  | "logs"
  | "about"
  | "appSettings";
```

From gui-pro/src/shared/hooks/useKeyboardShortcuts.ts:
```typescript
const pages = ["dashboard", "server", "control", "settings", "routing", "logs", "appSettings", "about"];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define AppTab type + update useKeyboardShortcuts</name>
  <files>gui-pro/src/shared/types.ts, gui-pro/src/shared/hooks/useKeyboardShortcuts.ts</files>
  <read_first>
    gui-pro/src/shared/types.ts
    gui-pro/src/shared/hooks/useKeyboardShortcuts.ts
    gui-pro/src/App.tsx
    .planning/phases/04-mtproto-proxy/04-UI-SPEC.md
  </read_first>
  <action>
1. In `gui-pro/src/shared/types.ts`:
   - Remove the deprecated AppTab type (`"setup" | "settings" | "routing" | "about"`) and its @deprecated JSDoc comment (lines 30-31).
   - Add the new AppTab type: `export type AppTab = "control" | "connection" | "routing" | "settings" | "about";`
   - Keep all other exports (VpnStatus, UpdateInfo, VpnConfig, LogEntry, ThemeMode) unchanged.
   - Per D-03: the 5 tabs are control (Панель управления), connection (Подключение), routing (Маршрутизация), settings (Настройки), about (О программе).

2. In `gui-pro/src/shared/hooks/useKeyboardShortcuts.ts`:
   - Replace the `pages` array on line 19 from `["dashboard", "server", "control", "settings", "routing", "logs", "appSettings", "about"]` (8 items) to `["control", "connection", "routing", "settings", "about"]` (5 items, matching AppTab order per D-03).
   - This means Ctrl+1=control, Ctrl+2=connection, Ctrl+3=routing, Ctrl+4=settings, Ctrl+5=about.
   - The `num <= pages.length` check already handles the count change (was 8, now 5).
  </action>
  <verify>
    <automated>cd gui-pro && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - gui-pro/src/shared/types.ts contains `export type AppTab = "control" | "connection" | "routing" | "settings" | "about"`
    - gui-pro/src/shared/types.ts does NOT contain `@deprecated`
    - gui-pro/src/shared/types.ts does NOT contain `"setup"`
    - gui-pro/src/shared/hooks/useKeyboardShortcuts.ts contains `["control", "connection", "routing", "settings", "about"]`
    - gui-pro/src/shared/hooks/useKeyboardShortcuts.ts does NOT contain `"dashboard"` or `"logs"` or `"appSettings"` or `"server"`
  </acceptance_criteria>
  <done>AppTab is the new 5-value union. Keyboard shortcuts navigate to 5 tabs. TypeScript compiles (note: App.tsx will have type errors until Plan 03 refactors it — that is expected).</done>
</task>

<task type="auto">
  <name>Task 2: Add i18n keys for new tabs + sidebar + remove Header.tsx dead code</name>
  <files>gui-pro/src/shared/i18n/locales/en.json, gui-pro/src/shared/i18n/locales/ru.json, gui-pro/src/components/Header.tsx, gui-pro/src/components/Header.test.tsx</files>
  <read_first>
    gui-pro/src/shared/i18n/locales/en.json
    gui-pro/src/shared/i18n/locales/ru.json
    gui-pro/src/components/Header.tsx
    gui-pro/src/components/Header.test.tsx
    .planning/phases/04-mtproto-proxy/04-UI-SPEC.md
  </read_first>
  <action>
1. In `gui-pro/src/shared/i18n/locales/en.json`, update the `"tabs"` object:
   - Add `"connection": "Connection"` — new key per UI-SPEC Copywriting Contract
   - Add `"requires_config": "Set up connection first"` — disabled tab tooltip per UI-SPEC
   - Keep existing keys (controlPanel, routing, about) unchanged
   - The old keys (server, serverSetup, installation, dashboard, logs, setup) can remain for now — they do no harm and may still be referenced by non-shell components. They will be cleaned up in Phase 6.

2. In `gui-pro/src/shared/i18n/locales/en.json`, add to the `"sidebar"` object (create if not exists):
   - `"servers": "Servers"` — ServerSidebar section header
   - `"add_server": "Add server"` — ServerSidebar add button
   - `"no_servers": "No servers"` — ServerSidebar empty state heading
   - `"no_servers_hint": "Configure connection in Control Panel"` — ServerSidebar empty state body

3. In `gui-pro/src/shared/i18n/locales/ru.json`, update the `"tabs"` object:
   - Add `"connection": "Подключение"` — per D-03
   - Add `"requires_config": "Сначала настройте подключение"` — per UI-SPEC

4. In `gui-pro/src/shared/i18n/locales/ru.json`, add to the `"sidebar"` object (create if not exists):
   - `"servers": "Серверы"` — per UI-SPEC
   - `"add_server": "Добавить сервер"`
   - `"no_servers": "Нет серверов"`
   - `"no_servers_hint": "Настройте подключение в «Панель управления»"`

5. Delete `gui-pro/src/components/Header.tsx` — dead code, not imported by App.tsx, only imported by its own test file. Uses the old AppTab type that no longer exists.

6. Delete `gui-pro/src/components/Header.test.tsx` — tests for deleted Header.tsx.
  </action>
  <verify>
    <automated>cd gui-pro && node -e "const en=require('./src/shared/i18n/locales/en.json'); const ru=require('./src/shared/i18n/locales/ru.json'); console.log('en.tabs.connection:', en.tabs.connection); console.log('ru.tabs.connection:', ru.tabs.connection); console.log('en.tabs.requires_config:', en.tabs.requires_config); console.log('en.sidebar.servers:', en.sidebar?.servers); console.log('ru.sidebar.no_servers_hint:', ru.sidebar?.no_servers_hint); const fs=require('fs'); console.log('Header.tsx exists:', fs.existsSync('src/components/Header.tsx')); console.log('Header.test.tsx exists:', fs.existsSync('src/components/Header.test.tsx'));"</automated>
  </verify>
  <acceptance_criteria>
    - en.json contains key `tabs.connection` with value `"Connection"`
    - ru.json contains key `tabs.connection` with value `"Подключение"`
    - en.json contains key `tabs.requires_config` with value `"Set up connection first"`
    - ru.json contains key `tabs.requires_config` with value `"Сначала настройте подключение"`
    - en.json contains key `sidebar.servers` with value `"Servers"`
    - ru.json contains key `sidebar.no_servers_hint` with value starting with `"Настройте подключение"`
    - gui-pro/src/components/Header.tsx does NOT exist
    - gui-pro/src/components/Header.test.tsx does NOT exist
  </acceptance_criteria>
  <done>All i18n keys for Phase 4 tabs and ServerSidebar are present in both locales. Header.tsx dead code is removed.</done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md for scope change</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/phases/04-mtproto-proxy/04-CONTEXT.md
  </read_first>
  <action>
Per D-17/D-18/D-19, update ROADMAP.md Phase 4 entry:

1. Change Phase 4 title from `**Phase 4: Remaining Panels**` to `**Phase 4: Application Shell**`
2. Change Goal from "All remaining application screens..." to:
   `Application shell is complete: TitleBar (custom title bar with logo + WindowControls), TabNavigation (5 horizontal tabs replacing sidebar), ServerSidebar redesigned with tokens, and App.tsx routing refactored from 8 SidebarPage to 5 AppTab`
3. Update Requirements to note the scope change:
   `**Requirements**: SCR-07 (partial — Dashboard disbanded), DOC-04 (partial — shell use cases), DOC-05 (partial — shell test cases). SCR-03, SCR-04, SCR-05, SCR-06, SCR-08, SCR-09 deferred to future screen phases.`
4. Update Success Criteria:
   ```
   1. TitleBar renders seamlessly (no border-bottom) with logo + drag region + WindowControls in both themes
   2. TabNavigation shows 5 tabs with correct disabled states when hasConfig=false, ARIA tablist semantics
   3. ServerSidebar uses only design token vars for status dots and hover states — zero hardcoded Tailwind colors
   4. App.tsx routing uses AppTab 5-tab union, old localStorage page IDs migrate correctly
   5. Sidebar.tsx is deleted, all navigation goes through TabNavigation
   ```
5. Update Plans section:
   ```
   **Plans**: 4 plans
   Plans:
   - [ ] 04-03-PLAN.md — Foundation: AppTab type + i18n keys + keyboard shortcuts + ROADMAP update
   - [ ] 04-04-PLAN.md — New components: TitleBar + TabNavigation + WindowControls redesign
   - [ ] 04-05-PLAN.md — Integration: App.tsx refactor + ServerSidebar redesign + Sidebar deletion
   - [ ] 04-06-PLAN.md — Polish: Storybook stories + documentation + visual checkpoint
   ```

Keep Phase 5 (Layout Shell) and Phase 6 (Cleanup) unchanged.

NOTE: Do NOT modify any other phase entries. Do NOT add new phase numbers for deferred screen migrations — that will be done when those phases are discussed.
  </action>
  <verify>
    <automated>cd "C:/Users/naska/Documents/TrustTunnelClient/.claude/worktrees/naughty-hermann" && node -e "const fs=require('fs'); const r=fs.readFileSync('.planning/ROADMAP.md','utf8'); console.log('Has Application Shell:', r.includes('Application Shell')); console.log('Has 04-03-PLAN:', r.includes('04-03-PLAN')); console.log('Has 4 plans:', r.includes('4 plans')); console.log('Has deferred note:', r.includes('deferred'));"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 4 title contains "Application Shell" not "Remaining Panels"
    - ROADMAP.md Phase 4 Plans section lists 04-03 through 04-06 PLAN.md files
    - ROADMAP.md Phase 4 contains note about SCR-03 through SCR-09 being deferred
    - Phase 5 and Phase 6 entries are unchanged
  </acceptance_criteria>
  <done>ROADMAP.md accurately reflects the Phase 4 scope change per D-19. Deferred requirements are documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 4 Plan 03 modifies only static types, i18n JSON, and documentation — no runtime trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | i18n JSON files | accept | JSON files are bundled at build time, no runtime loading from untrusted sources |
| T-04-02 | Information Disclosure | Header.tsx deletion | accept | Dead code removal reduces attack surface |
</threat_model>

<verification>
1. `npx tsc --noEmit` — TypeScript compiles (may show App.tsx errors until Plan 05 refactors it; types.ts and useKeyboardShortcuts.ts should be clean)
2. i18n keys are present in both locale files
3. Header.tsx and Header.test.tsx no longer exist
4. ROADMAP.md has updated Phase 4 entry
</verification>

<success_criteria>
- AppTab type is `"control" | "connection" | "routing" | "settings" | "about"` in shared/types.ts
- Keyboard shortcuts use 5-tab array
- Both en.json and ru.json have tabs.connection, tabs.requires_config, sidebar.servers, sidebar.no_servers_hint
- Header.tsx dead code removed
- ROADMAP.md reflects Application Shell scope
</success_criteria>

<output>
After completion, create `.planning/phases/04-mtproto-proxy/04-03-SUMMARY.md`
</output>
