---
phase: 04-application-shell
plan: 04
type: execute
wave: 2
depends_on: ["04-03"]
files_modified:
  - gui-pro/src/components/layout/TitleBar.tsx
  - gui-pro/src/components/layout/TitleBar.test.tsx
  - gui-pro/src/components/layout/TabNavigation.tsx
  - gui-pro/src/components/layout/TabNavigation.test.tsx
  - gui-pro/src/components/layout/WindowControls.tsx
autonomous: true
requirements:
  - SCR-03
  - SCR-04
  - SCR-05
  - SCR-06

must_haves:
  truths:
    - "TitleBar renders Shield icon + TrustTunnel text + PRO badge + drag region + WindowControls"
    - "TabNavigation renders 5 tabs with correct icons, labels via i18n, and disabled state for config-required tabs"
    - "Active tab has a centered 60%-width accent underline, not a background fill"
    - "WindowControls uses token vars for hover, platform red for close button"
    - "Title bar is seamless (no border-bottom) per D-09"
    - "Tab bar uses ARIA tablist/tab/aria-selected pattern"
  artifacts:
    - path: "gui-pro/src/components/layout/TitleBar.tsx"
      provides: "Title bar component with logo + drag region + WindowControls"
      exports: ["TitleBar"]
    - path: "gui-pro/src/components/layout/TitleBar.test.tsx"
      provides: "Tests for TitleBar"
      min_lines: 20
    - path: "gui-pro/src/components/layout/TabNavigation.tsx"
      provides: "5-tab horizontal nav replacing Sidebar"
      exports: ["TabNavigation"]
    - path: "gui-pro/src/components/layout/TabNavigation.test.tsx"
      provides: "Tests for TabNavigation"
      min_lines: 50
    - path: "gui-pro/src/components/layout/WindowControls.tsx"
      provides: "Redesigned WindowControls with token vars"
      exports: ["WindowControls"]
  key_links:
    - from: "gui-pro/src/components/layout/TitleBar.tsx"
      to: "gui-pro/src/components/layout/WindowControls.tsx"
      via: "import and render"
      pattern: "import.*WindowControls"
    - from: "gui-pro/src/components/layout/TabNavigation.tsx"
      to: "gui-pro/src/shared/types.ts"
      via: "AppTab type import"
      pattern: "import.*AppTab.*from.*shared/types"
---

<objective>
Create the two new shell layout components (TitleBar and TabNavigation) and redesign WindowControls with design tokens. These are the visual building blocks that Plan 05 will wire into App.tsx.

Purpose: Delivers the core UI components of the application shell — the chrome that frames all content. TitleBar provides the seamless title bar with logo and window controls. TabNavigation replaces the sidebar with 5 horizontal tabs. WindowControls migrates from hardcoded RGBA to token vars.

Output: Three production-ready components in layout/ with tests for TitleBar and TabNavigation.
</objective>

<execution_context>
@C:\Users\naska\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\naska\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/04-mtproto-proxy/04-CONTEXT.md
@.planning/phases/04-mtproto-proxy/04-RESEARCH.md
@.planning/phases/04-mtproto-proxy/04-UI-SPEC.md
@.planning/phases/04-mtproto-proxy/04-VALIDATION.md

<interfaces>
<!-- AppTab type from Plan 03 -->
From gui-pro/src/shared/types.ts (after Plan 03):
```typescript
export type AppTab = "control" | "connection" | "routing" | "settings" | "about";
```

<!-- Tab definitions from UI-SPEC -->
Tab array (canonical from 04-UI-SPEC.md):
```typescript
import { Monitor, Settings, GitBranch, SlidersHorizontal, Info } from "lucide-react";

interface TabDef {
  id: AppTab;
  labelKey: string;
  icon: ReactNode;
  requiresConfig: boolean;
}

const TABS: TabDef[] = [
  { id: "control",    labelKey: "tabs.controlPanel", icon: <Monitor className="w-4 h-4" />,           requiresConfig: false },
  { id: "connection", labelKey: "tabs.connection",   icon: <Settings className="w-4 h-4" />,           requiresConfig: true },
  { id: "routing",    labelKey: "tabs.routing",      icon: <GitBranch className="w-4 h-4" />,          requiresConfig: true },
  { id: "settings",   labelKey: "tabs.settings",     icon: <SlidersHorizontal className="w-4 h-4" />,  requiresConfig: true },
  { id: "about",      labelKey: "tabs.about",        icon: <Info className="w-4 h-4" />,               requiresConfig: false },
];
```

<!-- WindowControls current interface -->
From gui-pro/src/components/layout/WindowControls.tsx:
```typescript
export function WindowControls() { ... }
// Uses: getCurrentWindow() from @tauri-apps/api/window
// Buttons: minimize, maximize, close
// aria-labels: "Minimize", "Maximize", "Close"
```

<!-- cn() utility -->
From gui-pro/src/shared/lib/cn.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TitleBar component + TabNavigation component with tests</name>
  <files>gui-pro/src/components/layout/TitleBar.tsx, gui-pro/src/components/layout/TitleBar.test.tsx, gui-pro/src/components/layout/TabNavigation.tsx, gui-pro/src/components/layout/TabNavigation.test.tsx</files>
  <read_first>
    gui-pro/src/components/layout/WindowControls.tsx
    gui-pro/src/shared/types.ts
    gui-pro/src/shared/lib/cn.ts
    gui-pro/src/shared/i18n/locales/en.json
    gui-pro/src/App.tsx
    .planning/phases/04-mtproto-proxy/04-UI-SPEC.md
  </read_first>
  <behavior>
    TabNavigation tests (RED first):
    - renders 5 tab buttons with role="tab" inside role="tablist"
    - active tab has aria-selected="true", others have aria-selected="false"
    - clicking a tab calls onTabChange with the tab id
    - when hasConfig=false, 3 tabs (connection, routing, settings) have aria-disabled="true"
    - clicking a disabled tab does NOT call onTabChange
    - active tab has an indicator element (the 3px accent underline)

    TitleBar tests (RED first):
    - renders Shield icon (lucide-react Shield or ShieldCheck)
    - renders "TrustTunnel" text
    - renders "PRO" badge
    - does NOT have border-bottom style
    - imports and renders WindowControls
  </behavior>
  <action>
**TitleBar.tsx** — Create `gui-pro/src/components/layout/TitleBar.tsx`:

```typescript
interface TitleBarProps {
  hasUpdate?: boolean;
}
export function TitleBar({ hasUpdate }: TitleBarProps) { ... }
```

Structure (per UI-SPEC Layout Architecture):
- Root div: `className="flex items-center shrink-0 h-8"`, `data-tauri-drag-region`, `style={{ backgroundColor: "var(--color-bg-primary)" }}`. NO border-bottom (D-09 seamless).
- Logo group (left): `div` with `data-tauri-drag-region`, `className="flex items-center gap-[var(--space-2)] pl-[var(--space-4)]"`
  - Shield icon container: 28x28px rounded `--radius-md`, background `var(--color-bg-elevated)`, flex center
  - Shield icon: 16x16, `color: var(--color-accent-interactive)`, from lucide-react
  - App name span: `className="text-[14px] font-semibold"`, `style={{ letterSpacing: "-0.01em", color: "var(--color-text-primary)" }}`, text "TrustTunnel"
  - PRO badge span: `className="text-[11px] font-bold px-[var(--space-2)] py-[2px] rounded-[var(--radius-sm)]"`, `style={{ backgroundColor: "var(--color-bg-elevated)", color: "var(--color-accent-interactive)" }}`, text "PRO"
- Drag spacer (center): `div` with `className="flex-1"`, `data-tauri-drag-region`
- WindowControls (right): `<WindowControls />` (imported from same directory). No data-tauri-drag-region on the WindowControls wrapper — already handled inside the component.

**TitleBar.test.tsx** — Create `gui-pro/src/components/layout/TitleBar.test.tsx`:
- Write tests FIRST (RED), then implement component (GREEN).
- Mock WindowControls as a simple div with `vi.mock("./WindowControls", ...)` to isolate TitleBar tests.
- Test the 5 behaviors listed in the TitleBar behavior block above.
- Verify that TitleBar renders text "TrustTunnel" and "PRO".
- Verify no `border-bottom` or `borderBottom` in rendered output (check computed styles or snapshot absence).

**TabNavigation.tsx** — Create `gui-pro/src/components/layout/TabNavigation.tsx`:

```typescript
interface TabNavigationProps {
  activeTab: AppTab;
  onTabChange: (tab: AppTab) => void;
  hasConfig: boolean;
  hasUpdate?: boolean;
}
export function TabNavigation({ activeTab, onTabChange, hasConfig, hasUpdate }: TabNavigationProps) { ... }
```

Structure (per UI-SPEC Component Inventory — TabNavigation):
- Root div: `role="tablist"`, `className="flex w-full shrink-0"`, height 40px, `style={{ backgroundColor: "var(--color-bg-primary)" }}`. NO border-bottom.
- Define TABS array inside the file (see interfaces block above for exact values).
- Map TABS to buttons:
  - Each button: `role="tab"`, `aria-selected={activeTab === tab.id}`, `aria-disabled={tab.requiresConfig && !hasConfig}`
  - className via `cn()`:
    - Base: `"relative flex-1 flex items-center justify-center gap-[var(--space-1)] transition-colors duration-[var(--transition-fast)]"`
    - Disabled: `tab.requiresConfig && !hasConfig && "opacity-40 cursor-not-allowed"`
    - Not disabled: `"cursor-pointer"`
  - onClick: `() => { if (!(tab.requiresConfig && !hasConfig)) onTabChange(tab.id); }`
  - Hover style: use `onMouseEnter/onMouseLeave` + `useState` to track hovered tab ID. When hovered and not active and not disabled: `backgroundColor: "var(--color-bg-hover)"`.
  - Icon: `tab.icon` rendered inline, color via style: `active ? "var(--color-text-primary)" : "var(--color-text-muted)"`
  - Label: `<span>` with `t(tab.labelKey)`, `className="text-[14px]"`, font-weight: active = 600 (semibold), inactive = 400 (normal). Color: active = `--color-text-primary`, inactive = `--color-text-muted`, hover (not active) = `--color-text-secondary`.
  - Active indicator: when `activeTab === tab.id`, render a `<span>` inside the button: `className="absolute bottom-0 left-1/2 -translate-x-1/2 w-[60%] h-[3px] rounded-full"`, `style={{ backgroundColor: "var(--color-accent-interactive)" }}`
  - Focus ring: `className` includes `"focus-visible:shadow-[var(--focus-ring)] outline-none"`
- For "about" tab: if `hasUpdate` is true, render a small green dot (8px circle, `--color-status-connected`, with `box-shadow: 0 0 0 2px var(--color-bg-primary)` halo) positioned absolute on the icon area.
- Arrow key navigation: add `onKeyDown` handler on each tab button. If `ArrowRight`, focus next tab. If `ArrowLeft`, focus previous tab. Wrap around. Use `tabIndex={activeTab === tab.id ? 0 : -1}` for roving tabindex pattern.

**TabNavigation.test.tsx** — Create tests using vitest + @testing-library/react:
- Write tests FIRST (RED), then implement component (GREEN).
- Import i18n from `../../shared/i18n` and set language to "en" in beforeEach.
- Test the 6 behaviors listed in the TabNavigation behavior block above.
  </action>
  <verify>
    <automated>cd gui-pro && npx vitest run TabNavigation TitleBar --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - gui-pro/src/components/layout/TitleBar.tsx exists and exports `TitleBar`
    - TitleBar.tsx contains `data-tauri-drag-region` attribute
    - TitleBar.tsx contains text "TrustTunnel" and text "PRO"
    - TitleBar.tsx does NOT contain `border-bottom` or `borderBottom`
    - TitleBar.tsx contains `import { WindowControls }`
    - gui-pro/src/components/layout/TitleBar.test.tsx exists with at least 3 test cases
    - All TitleBar tests pass
    - gui-pro/src/components/layout/TabNavigation.tsx exists and exports `TabNavigation`
    - TabNavigation.tsx contains `role="tablist"`
    - TabNavigation.tsx contains `role="tab"`
    - TabNavigation.tsx contains `aria-selected`
    - TabNavigation.tsx contains `aria-disabled`
    - TabNavigation.tsx contains `--color-accent-interactive`
    - TabNavigation.tsx contains `w-[60%] h-[3px]` (active indicator)
    - TabNavigation.tsx contains `import type { AppTab }` from shared/types
    - TabNavigation.tsx contains all 5 tab IDs: "control", "connection", "routing", "settings", "about"
    - gui-pro/src/components/layout/TabNavigation.test.tsx exists with at least 5 test cases
    - All TabNavigation tests pass
  </acceptance_criteria>
  <done>TitleBar and TabNavigation are production-ready components with ARIA semantics, token-based styling, and passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign WindowControls with design tokens</name>
  <files>gui-pro/src/components/layout/WindowControls.tsx</files>
  <read_first>
    gui-pro/src/components/layout/WindowControls.tsx
    gui-pro/src/shared/styles/tokens.css
    .planning/phases/04-mtproto-proxy/04-UI-SPEC.md
  </read_first>
  <action>
Rewrite `gui-pro/src/components/layout/WindowControls.tsx` to use design tokens instead of hardcoded RGBA:

1. Replace inline style hover background for minimize/maximize buttons:
   - OLD: `background: hovered === "min" ? "rgba(255,255,255,0.08)" : "transparent"`
   - NEW: Use `cn()` utility with Tailwind token classes: `cn("h-full w-10 flex items-center justify-center transition-colors duration-[var(--transition-fast)]", hovered === "min" ? "bg-[var(--color-bg-hover)]" : "bg-transparent")`

2. Same pattern for maximize button (replace `"rgba(255,255,255,0.08)"` with `bg-[var(--color-bg-hover)]`).

3. Close button — keep platform exception:
   - Hover: `backgroundColor: "#e81123"`, `color: "#ffffff"` — Windows platform convention, NOT from tokens.
   - Add pressed state: when `pressed === "close"` (add `onMouseDown`/`onMouseUp` tracking): `backgroundColor: "#c50f1f"`, `color: "#ffffff"`.
   - Add code comment: `/* Windows platform convention: close button hover = #e81123, pressed = #c50f1f — NOT from design tokens */`

4. Remove the `window-control-btn` and `window-control-close` CSS classes from button classNames. Replace with full Tailwind token classes:
   - Base: `"h-full w-10 flex items-center justify-center transition-colors duration-[var(--transition-fast)]"`
   - Text color (default): `style={{ color: "var(--color-text-secondary)" }}`
   - Close override on hover: `style={{ color: "#ffffff" }}`

5. Add `import { cn } from "../../shared/lib/cn"` at the top.

6. Keep all functional logic unchanged: `getCurrentWindow()`, `appWindow.minimize()`, `appWindow.toggleMaximize()`, `appWindow.close()`, `aria-label` attributes, SVG icons.
  </action>
  <verify>
    <automated>cd gui-pro && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - WindowControls.tsx does NOT contain `rgba(255,255,255,0.08)` (old hardcoded hover)
    - WindowControls.tsx contains `var(--color-bg-hover)` (new token-based hover)
    - WindowControls.tsx contains `#e81123` with a code comment mentioning "Windows platform convention"
    - WindowControls.tsx contains `#c50f1f` (pressed state for close)
    - WindowControls.tsx contains `import { cn }` from shared/lib/cn
    - WindowControls.tsx contains `var(--color-text-secondary)` for default icon color
    - WindowControls.tsx contains `var(--transition-fast)` for transition duration
    - WindowControls.tsx still contains `aria-label="Minimize"`, `aria-label="Maximize"`, `aria-label="Close"`
    - TypeScript compiles without errors in this file
  </acceptance_criteria>
  <done>WindowControls uses design token vars for hover states. Close button retains Windows platform red with documented exception. All three buttons maintain correct aria-labels and functionality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> Tab navigation | Tab click handler receives only typed AppTab values — no free string input |
| Title bar -> Window actions | WindowControls calls Tauri API (minimize/maximize/close) — existing trust boundary, unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Spoofing | TabNavigation disabled tabs | mitigate | aria-disabled + onClick guard prevents navigation to tabs requiring config when hasConfig=false |
| T-04-04 | Denial of Service | data-tauri-drag-region | accept | Drag region on non-interactive elements only — WindowControls wrapper explicitly has WebkitAppRegion: "no-drag" to prevent click interception |
| T-04-05 | Tampering | WindowControls close | accept | close() calls Tauri API which handles OS-level window close — no custom confirmation needed per UI-SPEC |
</threat_model>

<verification>
1. `npx vitest run TabNavigation TitleBar` — all tests pass
2. `npx tsc --noEmit` — TypeScript compiles for new components
3. Visual inspection: TitleBar has no border-bottom, WindowControls hover uses token vars
</verification>

<success_criteria>
- TitleBar component renders logo + drag region + WindowControls seamlessly
- TabNavigation renders 5 tabs with ARIA tablist pattern, disabled states, and accent underline
- WindowControls hover uses --color-bg-hover token instead of hardcoded RGBA
- All TitleBar and TabNavigation tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-mtproto-proxy/04-04-SUMMARY.md`
</output>
