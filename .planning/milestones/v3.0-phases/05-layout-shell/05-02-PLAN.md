---
phase: 05-layout-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gui-pro/src/components/server/Fail2banSection.tsx
  - gui-pro/src/components/server/CertSection.tsx
  - gui-pro/src/components/server/ServerStatusSection.tsx
  - gui-pro/src/components/server/DiagnosticsSection.tsx
  - gui-pro/src/components/server/LogsSection.tsx
  - gui-pro/src/components/server/FirewallSection.tsx
  - gui-pro/src/components/server/UsersSection.tsx
  - gui-pro/src/components/server/VersionSection.tsx
autonomous: true
requirements: [SCR-10]
must_haves:
  truths:
    - "No Button component uses variant='secondary' or variant='success' anywhere in server/ directory"
    - "No Badge component uses variant='default' or variant='accent' anywhere in server/ directory"
    - "Auth action buttons have correct primary variant and visible contrast on light theme"
    - "TypeScript compiles with zero errors after all variant replacements"
  artifacts:
    - path: "gui-pro/src/components/server/Fail2banSection.tsx"
      provides: "Corrected Button variants (secondary->ghost)"
      contains: "variant=\"ghost\""
    - path: "gui-pro/src/components/server/CertSection.tsx"
      provides: "Corrected Badge variants (default->neutral) and Button variants (secondary->ghost)"
      contains: "variant=\"neutral\""
    - path: "gui-pro/src/components/server/ServerStatusSection.tsx"
      provides: "Corrected Button/Badge variants"
      contains: "variant=\"ghost\""
    - path: "gui-pro/src/components/server/VersionSection.tsx"
      provides: "Corrected Badge variants (accent->neutral)"
      contains: "variant=\"neutral\""
    - path: "gui-pro/src/components/server/UsersSection.tsx"
      provides: "Corrected Button variants (success->primary)"
      contains: "variant=\"primary\""
  key_links:
    - from: "gui-pro/src/components/server/*.tsx"
      to: "gui-pro/src/shared/ui/Button.tsx"
      via: "CVA variant prop"
      pattern: "variant=\"(primary|danger|ghost|icon)\""
    - from: "gui-pro/src/components/server/*.tsx"
      to: "gui-pro/src/shared/ui/Badge.tsx"
      via: "CVA variant prop"
      pattern: "variant=\"(success|warning|danger|neutral|dot)\""
---

<objective>
Fix all invalid Button and Badge CVA variants across ServerPanel sections, and fix auth button color contrast on light theme.

Purpose: Eliminates runtime TypeScript warnings and potential rendering issues from non-existent CVA variants (D-12), fixes auth button contrast (D-15).
Output: All 8 server section files use only valid CVA variants.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-layout-shell/05-CONTEXT.md
@.planning/phases/05-layout-shell/05-PATTERNS.md

<interfaces>
<!-- Valid CVA variants — these are the ONLY valid values -->

From gui-pro/src/shared/ui/Button.tsx (CVA variants):
```typescript
variant: {
  primary: "bg-[var(--color-accent-interactive)] hover:bg-[var(--color-accent-hover)] active:bg-[var(--color-accent-active)] text-white border border-transparent",
  danger: "bg-[var(--color-destructive)] hover:opacity-90 text-white border border-transparent",
  ghost: "bg-transparent hover:bg-[var(--color-bg-hover)] active:bg-[var(--color-bg-active)] text-[var(--color-text-secondary)] border border-transparent",
  icon: "bg-transparent hover:bg-[var(--color-bg-hover)] active:bg-[var(--color-bg-active)] text-[var(--color-text-muted)] border border-transparent",
}
// DOES NOT EXIST: "secondary", "success", "danger-outline", "default"
```

From gui-pro/src/shared/ui/Badge.tsx (CVA variants):
```typescript
variant: {
  success: "bg-[var(--color-status-connected-bg)] text-[var(--color-status-connected)] ...",
  warning: "bg-[var(--color-status-connecting-bg)] text-[var(--color-status-connecting)] ...",
  danger: "bg-[var(--color-status-error-bg)] text-[var(--color-status-error)] ...",
  neutral: "bg-[var(--color-bg-elevated)] text-[var(--color-text-secondary)] ...",
  dot: "...",
}
// DOES NOT EXIST: "default", "accent"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Button variants in all server sections (D-12)</name>
  <files>
    gui-pro/src/components/server/Fail2banSection.tsx,
    gui-pro/src/components/server/CertSection.tsx,
    gui-pro/src/components/server/ServerStatusSection.tsx,
    gui-pro/src/components/server/DiagnosticsSection.tsx,
    gui-pro/src/components/server/LogsSection.tsx,
    gui-pro/src/components/server/FirewallSection.tsx,
    gui-pro/src/components/server/UsersSection.tsx
  </files>
  <read_first>
    - gui-pro/src/components/server/Fail2banSection.tsx
    - gui-pro/src/components/server/CertSection.tsx
    - gui-pro/src/components/server/ServerStatusSection.tsx
    - gui-pro/src/components/server/DiagnosticsSection.tsx
    - gui-pro/src/components/server/LogsSection.tsx
    - gui-pro/src/components/server/FirewallSection.tsx
    - gui-pro/src/components/server/UsersSection.tsx
    - gui-pro/src/shared/ui/Button.tsx (confirm valid variants)
  </read_first>
  <action>
Apply this exact replacement table for Button `variant` props. Each line shows file, approximate line, old value, new value:

**Fail2banSection.tsx:**
- Line ~42: `variant="secondary"` -> `variant="ghost"` (stop fail2ban button)
- Line ~110: `variant="secondary"` -> `variant="ghost"` (unban button)

**CertSection.tsx:**
- Line ~258: `variant="secondary"` -> `variant="ghost"` (renew cert button)

**ServerStatusSection.tsx:**
- Line ~183: `variant="secondary"` -> `variant="ghost"` (restart service button)
- Line ~207: `variant="success"` -> `variant="primary"` (start service button)

**DiagnosticsSection.tsx:**
- Line ~57: `variant="secondary"` -> `variant="ghost"` (run diagnostics button)
- Line ~69: `variant="secondary"` -> `variant="ghost"` (copy results button)

**LogsSection.tsx:**
- Line ~85: `variant="secondary"` -> `variant="ghost"` (refresh logs button)

**FirewallSection.tsx:**
- Line ~51: `variant="secondary"` -> `variant="ghost"` (stop firewall button)

**UsersSection.tsx:**
- Line ~274: `variant={selectedUser ? "primary" : "secondary"}` -> `variant={selectedUser ? "primary" : "ghost"}` (conditional variant)
- Line ~334: `variant="success"` -> `variant="primary"` (add user / positive action button)

**Rule:** `variant="secondary"` always becomes `variant="ghost"` (secondary actions). `variant="success"` on Button always becomes `variant="primary"` (positive/affirmative actions). Badge `variant="success"` stays unchanged (it is a valid Badge variant).
  </action>
  <verify>
    <automated>cd gui-pro && grep -rn 'variant="secondary"\|variant="success"' src/components/server/*.tsx | grep -i button | head -20; echo "---"; npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'variant="secondary"' gui-pro/src/components/server/*.tsx` returns zero matches
    - `grep -rn 'variant="success"' gui-pro/src/components/server/*.tsx` returns ONLY Badge usages (CertSection Badge, ServerStatusSection Badge), NOT Button usages
    - Fail2banSection.tsx contains `variant="ghost"` in both button locations
    - CertSection.tsx line ~258 contains `variant="ghost"`
    - ServerStatusSection.tsx line ~183 contains `variant="ghost"`, line ~207 contains `variant="primary"`
    - UsersSection.tsx line ~334 contains `variant="primary"`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All Button components in server/ use only valid CVA variants: primary, danger, ghost, or icon. Zero "secondary" or "success" on Button.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Badge variants + auth buttons color + ServerStatusSection type cast (D-12, D-15)</name>
  <files>
    gui-pro/src/components/server/CertSection.tsx,
    gui-pro/src/components/server/ServerStatusSection.tsx,
    gui-pro/src/components/server/VersionSection.tsx
  </files>
  <read_first>
    - gui-pro/src/components/server/CertSection.tsx
    - gui-pro/src/components/server/ServerStatusSection.tsx
    - gui-pro/src/components/server/VersionSection.tsx
    - gui-pro/src/shared/ui/Badge.tsx (confirm valid variants)
  </read_first>
  <action>
Apply this replacement table for Badge `variant` props:

**CertSection.tsx:**
- Line ~193: `<Badge variant="success"` -> LEAVE AS IS (success is valid for Badge)
- Line ~197: `variant="default"` -> `variant="neutral"`
- Line ~225: `variant={... "default"}` -> replace every `"default"` in the ternary with `"neutral"`. The full ternary should become: `variant={certInfo.certType === "lets_encrypt" ? "success" : certInfo.certType === "self_signed" ? "warning" : "neutral"}`
- Line ~251: `variant={certInfo.autoRenew ? "success" : "default"}` -> `variant={certInfo.autoRenew ? "success" : "neutral"}`

**ServerStatusSection.tsx:**
- Line ~165: Type cast `variant={pingVariant as "success" | "warning" | "danger" | "default"}` -> fix to `variant={pingVariant as "success" | "warning" | "danger" | "neutral"}`. Also find where `pingVariant` is computed and change any `"default"` return value to `"neutral"`.
- Line ~171: `<Badge variant="default"` -> `<Badge variant="neutral"`

**D-15 auth buttons color fix:** Search ServerStatusSection.tsx for any Button with inline white background styles or non-standard color overrides that make buttons too white on light theme. If found, remove inline color overrides and ensure `variant="primary"` is used (which provides correct accent bg with white text from Button.tsx CVA). The `primary` variant already uses `--color-accent-interactive` bg which has proper contrast on both themes.

**VersionSection.tsx:**
- Line ~84: `<Badge variant="accent"` -> `<Badge variant="neutral">` (accent does not exist for Badge)
- Line ~86: `<Badge variant="success"` -> LEAVE AS IS (success is valid for Badge)

**Rule:** Badge `variant="default"` -> `variant="neutral"`. Badge `variant="accent"` -> `variant="neutral"`. Badge `variant="success"` stays. All type casts referencing `"default"` must become `"neutral"`.
  </action>
  <verify>
    <automated>cd gui-pro && grep -rn 'variant="default"\|variant="accent"\|as.*"default"' src/components/server/*.tsx | head -20; echo "---"; npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'variant="default"' gui-pro/src/components/server/*.tsx` returns zero matches
    - `grep -rn 'variant="accent"' gui-pro/src/components/server/*.tsx` returns zero matches
    - CertSection.tsx contains `variant="neutral"` in at least 3 places
    - ServerStatusSection.tsx contains `variant="neutral"` instead of `variant="default"`
    - ServerStatusSection.tsx type cast uses `"neutral"` not `"default"`
    - VersionSection.tsx line ~84 contains `variant="neutral"`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All Badge components use only valid CVA variants. No "default" or "accent" Badge variants remain. Auth button color is correct on light theme via proper primary variant. TypeScript type casts updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server data -> Badge variants | pingVariant computed from server ping data determines Badge variant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T (Tampering) | pingVariant computation | accept | Badge variant is visual-only (color/style). Invalid variant falls back to CVA default. No security impact. |
| T-05-05 | R (Repudiation) | Button variant changes | accept | Visual-only changes. No user actions or audit trail affected. |
</threat_model>

<verification>
1. `cd gui-pro && npx tsc --noEmit` — zero type errors
2. `grep -rn 'variant="secondary"' gui-pro/src/components/server/` returns 0 matches
3. `grep -rn 'variant="default"' gui-pro/src/components/server/` returns 0 matches
4. `grep -rn 'variant="accent"' gui-pro/src/components/server/` returns 0 matches
5. Button `variant="success"` does not appear in server/ (only Badge `variant="success"` is valid)
</verification>

<success_criteria>
- All Button variants are valid: primary, danger, ghost, or icon (D-12)
- All Badge variants are valid: success, warning, danger, neutral, or dot (D-12)
- Auth buttons have correct color contrast on light theme (D-15)
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-layout-shell/05-02-SUMMARY.md`
</output>
