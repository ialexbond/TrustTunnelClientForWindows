---
phase: 06-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - SCR-12
  - QA-02

must_haves:
  truths:
    - "All 8 Claude memory todo files are verified — resolved ones deleted, unresolved ones annotated"
    - "Final verification confirms prerelease passes and zero legacy artifacts remain"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Updated state reflecting Phase 6 completion"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/06-cleanup/"
      via: "Phase 6 plan list and status"
---

<objective>
Verify and close todo files (D-07..D-09), run final quality verification across the entire cleanup phase, and update project state.

Purpose: Closes the Phase 6 cleanup loop by verifying that all accumulated todos from earlier phases are either resolved (and deleted) or explicitly kept as future work. Runs the final prerelease gate to confirm the codebase is clean.
Output: Clean todo state, passing prerelease, updated STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cleanup/06-CONTEXT.md
@.planning/phases/06-cleanup/06-01-SUMMARY.md
@.planning/phases/06-cleanup/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and close todo files (D-07..D-09)</name>
  <files>Claude project memory todo files (8 files to audit)</files>
  <action>
Per D-07, D-08, D-09: check each of the 8 Claude project memory todo files against the current codebase state. For each, grep/inspect the codebase to determine if the issue is resolved.

**The 8 todo files to audit** (located in Claude project memory at `~/.claude/projects/C--Users-naska-Documents-TrustTunnelClient/memory/`):

1. **todo_control_panel_ux.md** — "Скрыть ServerSidebar когда нет серверов"
   - Check: Is ServerSidebar conditionally rendered? This is a multi-server feature — likely UNRESOLVED (tied to project_multi_server_planned.md).
   - Verification: `grep -n "ServerSidebar" gui-pro/src/components/ControlPanelPage.tsx`

2. **todo_logging_and_sanitize_fix.md** — "sanitize() multi-occurrence fix"
   - Check: Was the sanitize loop+search_from pattern implemented? Phase 5 STATE.md says "D-18 closed: sanitize() loop+search_from pattern verified by all 6 logging tests".
   - Verification: `grep -n "search_from\|while.*let.*Some" gui-pro/src-tauri/src/sanitize.rs` (or wherever sanitize lives)
   - If confirmed fixed: DELETE this todo file.

3. **todo_phase3_uat_issues.md** — "UAT фазы 3 — визуал, UX, баги"
   - Check each item:
     - "Угловатость UI" -> Phase 5 removed all hard borders, added soft shadows. RESOLVED.
     - "Белые кнопки auth" -> Phase 5 fixed auth button colors. RESOLVED.
     - "Табы — underline" -> Phase 5 changed tab indicator. RESOLVED.
     - "Кнопки серверных секций" -> Phase 5 fixed CVA variants. RESOLVED.
     - "IP дублирование" -> multi-server feature. UNRESOLVED (see todo_server_naming_dedup).
     - "Credentials persist" -> multi-server feature. UNRESOLVED (see todo_server_credentials_persist).
     - "Таб перерендер" -> Phase 5 added ServerTabs caching. RESOLVED.
     - "Кнопка Добавить сервер" -> Phase 5 fixed onAddServer. RESOLVED.
   - If mostly resolved: DELETE, note remaining items are tracked in their own dedicated todos.

4. **todo_server_credentials_persist.md** — "Сохранять credentials при смене сервера"
   - This is explicitly tied to multi-server architecture (project_multi_server_planned.md).
   - KEEP — future milestone work.

5. **todo_server_naming_dedup.md** — "Дедупликация IP + переименование серверов"
   - This is explicitly tied to multi-server architecture.
   - KEEP — future milestone work.

6. **todo_sidebar_status_dots.md** — "Редизайн статус-точек сервера"
   - Check: Is ServerSidebar even used currently? Phase 5 STATE says ServerSidebar is not imported from ControlPanelPage.
   - If ServerSidebar is unused: The status dots issue is moot until multi-server.
   - KEEP — future milestone work (tied to multi-server).

7. **todo_ui_review_improvements.md** — "UI-аудит: underline tab, logo box, font sizes, roving focus"
   - Check each item against Phase 5 work:
     - "Active tab: bg-pill to accent underline" -> Phase 5 plan 01 specifically did this. RESOLVED.
     - "TitleBar: app name + PRO badge font sizes" -> Check current TitleBar.tsx font sizes.
     - "Logo container: 28x28 rounded box" -> Check current TitleBar.tsx.
     - "Arrow-key roving focus in tablist" -> Phase 5 plan 01 specifically did roving focus. RESOLVED.
   - Verification: `grep -n "font-size\|roving\|tabIndex\|onKeyDown" gui-pro/src/components/TitleBar.tsx gui-pro/src/components/TabNavigation.tsx`
   - If 3-4 of 4 items resolved: DELETE. If 1-2 remain, update file to reflect only unresolved items.

8. **todo_visual_softness.md** — "Убрать угловатость UI"
   - Phase 5 plan 01 specifically addressed this: removed borders, sidebar bg-secondary, tab max-width.
   - Check: `grep -n "border-\[var" gui-pro/src/components/TabNavigation.tsx gui-pro/src/components/ServerSidebar.tsx`
   - If borders are gone and soft styling applied: DELETE.

**For each RESOLVED todo:** Delete the file from Claude project memory using the appropriate method (since these are Claude memory files, note in the summary which files should be deleted — the executor should delete them).

**IMPORTANT:** These files are in Claude's project memory directory (`~/.claude/projects/.../memory/`), NOT in the git repo. They do not appear in git. The executor should delete resolved files and update MEMORY.md index to remove their entries.

**Also update MEMORY.md:** After determining which todos are resolved, edit MEMORY.md to remove the entries for deleted todo files. Keep entries for todos that remain unresolved.
  </action>
  <verify>
    <automated>ls ~/.claude/projects/C--Users-naska-Documents-TrustTunnelClient/memory/todo_* 2>/dev/null | wc -l</automated>
  </verify>
  <done>Each of the 8 todo files has been verified against current codebase. Resolved todos are deleted from Claude project memory. MEMORY.md index is updated. Only genuinely unresolved todos (multi-server related) remain — expected: 3-4 files (server_credentials_persist, server_naming_dedup, sidebar_status_dots, possibly control_panel_ux).</done>
</task>

<task type="auto">
  <name>Task 2: Final quality gate and state update</name>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <action>
**1. Run full prerelease check:**
```
cd gui-pro && npm run prerelease
```
This runs typecheck + lint + test + rust:check + build. All must pass.

**2. Verify cleanup completeness:**
Run these verification commands and confirm zero violations:
- `grep -rn "rgba(" gui-pro/src/components/ --include="*.tsx" | wc -l` -> must be 0
- `grep -c "!important" gui-pro/src/index.css` -> must be 0
- `grep -c "surface" gui-pro/tailwind.config.js` -> must be 0
- `ls gui-pro/src/shared/styles/colors.ts 2>/dev/null` -> must not exist

**3. Update ROADMAP.md:**
- Mark Phase 6 plans as complete:
```
Plans:
- [x] 06-01-PLAN.md — Tokenize inline rgba() values into CSS custom properties
- [x] 06-02-PLAN.md — Remove legacy artifacts + documentation update
- [x] 06-03-PLAN.md — Todo closure + final quality gate
```
- Update Plans count: `**Plans:** 3 plans`
- Update progress table: Phase 6 row should show `3/3 | Complete | {date}`

**4. Update STATE.md:**
- Set `status: complete` (or `verifying` if that's the convention before user sign-off)
- Update `stopped_at` to reflect Phase 6 completion
- Update `progress.completed_phases: 6`
- Update `Current focus` to "Phase 06 — Cleanup complete"
- Add decision entry: "[Phase 06-cleanup]: All legacy artifacts removed — colors.ts, surface palette, !important overrides, inline rgba(). Todo files audited."

**5. Note on QA-02 and SCR-12:**
- QA-02 (vitest-axe): Deferred per CONTEXT.md to separate phase. Note in STATE.md decisions: "QA-02 deferred — vitest-axe a11y tests planned for post-v3.0"
- SCR-12 (every screen has Storybook story): Verify by listing existing story files. Run `ls gui-pro/src/components/**/*.stories.tsx gui-pro/src/shared/ui/*.stories.tsx 2>/dev/null | wc -l`. Note coverage in summary.
  </action>
  <verify>
    <automated>cd gui-pro && npm run prerelease 2>&1 | tail -5</automated>
  </verify>
  <done>prerelease passes cleanly. ROADMAP.md shows Phase 6 complete (3/3 plans). STATE.md reflects Phase 6 completion with cleanup decisions logged. All verification commands return expected zero counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — state management and verification only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | I (Information Disclosure) | todo files | accept | Todo files are development notes, no secrets. Resolved todos are deleted to reduce noise. |
</threat_model>

<verification>
1. `cd gui-pro && npm run prerelease` passes (typecheck + lint + test + clippy + build)
2. Zero rgba() in components, zero !important in index.css, zero surface in tailwind config
3. colors.ts does not exist
4. ROADMAP.md shows Phase 6: 3/3 Complete
5. Todo files: only multi-server related todos remain (3-4 files)
</verification>

<success_criteria>
- Full prerelease suite passes
- All cleanup verification checks return zero violations
- Resolved todo files deleted, unresolved ones kept
- ROADMAP.md and STATE.md updated to reflect Phase 6 completion
- QA-02 and SCR-12 status documented (deferred / verified)
</success_criteria>

<output>
After completion, create `.planning/phases/06-cleanup/06-03-SUMMARY.md`
</output>
