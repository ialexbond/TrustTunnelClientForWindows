---
phase: 01-connectivity-bypass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gui-app/src-tauri/Cargo.toml
  - gui-app/src-tauri/tauri.conf.json
  - gui-app/package.json
  - gui-light/src-tauri/Cargo.toml
  - gui-light/src-tauri/tauri.conf.json
  - gui-light/package.json
  - gui-app/src-tauri/src/connectivity.rs
autonomous: true
requirements: [CONN-01, CONN-02, CONN-03, CONN-04, CONN-05]

must_haves:
  truths:
    - "gui-app connectivity checks bind to the physical adapter IP via socket2"
    - "gui-app HTTP checks use reqwest with local_address set to physical adapter IP"
    - "When no physical adapter found, gui-app falls back to default routing without error"
    - "gui-app uses 4-consecutive-failure threshold before declaring offline"
    - "Verbose file logging shows adapter IP, bind result, and endpoint responses each cycle"
  artifacts:
    - path: "gui-app/src-tauri/src/connectivity.rs"
      provides: "socket2-bound connectivity checks with find_physical_adapter_ip()"
      contains: "use socket2"
    - path: "gui-app/src-tauri/Cargo.toml"
      provides: "socket2 and ipconfig dependencies"
      contains: "socket2"
  key_links:
    - from: "gui-app/src-tauri/src/connectivity.rs"
      to: "socket2 crate"
      via: "use socket2::{Socket, Domain, Type, Protocol, SockAddr}"
      pattern: "use socket2"
    - from: "gui-app/src-tauri/src/connectivity.rs"
      to: "ipconfig crate"
      via: "ipconfig::get_adapters()"
      pattern: "ipconfig::get_adapters"
    - from: "gui-app/src-tauri/src/connectivity.rs"
      to: "reqwest local_address"
      via: "ClientBuilder::local_address(physical_ip)"
      pattern: "local_address\\(physical_ip\\)"
---

<objective>
Restore socket2/ipconfig-based connectivity bypass in gui-app: version bump, add dependencies, and replace connectivity.rs with the 798ce8e7 version enhanced with verbose file logging.

Purpose: VPN connectivity checks must route through the physical network adapter (not VPN tunnel) to prevent false "offline" detections and unnecessary VPN reconnects.
Output: gui-app with working socket2-bound connectivity monitoring + verbose logging.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-connectivity-bypass/01-CONTEXT.md
@.planning/phases/01-connectivity-bypass/01-RESEARCH.md

<interfaces>
<!-- gui-app logging API (both editions have identical signature) -->
From gui-app/src-tauri/src/logging.rs:
```rust
pub fn log_app(level: &str, message: &str);
```

<!-- Current gui-app connectivity.rs public API (must be preserved) -->
From gui-app/src-tauri/src/connectivity.rs:
```rust
pub fn start_monitor(app: tauri::AppHandle, is_connected: Arc<Mutex<bool>>);
```

<!-- Target version from git history -->
Retrieve with: git show 798ce8e7:gui-app/src-tauri/src/connectivity.rs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version bump 2.3.0 -> 2.5.0 and add socket2/ipconfig dependencies</name>
  <read_first>
    - gui-app/src-tauri/Cargo.toml
    - gui-light/src-tauri/Cargo.toml
    - gui-app/src-tauri/tauri.conf.json
    - gui-light/src-tauri/tauri.conf.json
    - gui-app/package.json
    - gui-light/package.json
  </read_first>
  <files>
    gui-app/src-tauri/Cargo.toml,
    gui-app/src-tauri/tauri.conf.json,
    gui-app/package.json,
    gui-light/src-tauri/Cargo.toml,
    gui-light/src-tauri/tauri.conf.json,
    gui-light/package.json
  </files>
  <action>
1. Version bump in ALL 6 files — change "2.3.0" to "2.5.0":
   - gui-app/src-tauri/Cargo.toml line 3: `version = "2.5.0"`
   - gui-app/src-tauri/tauri.conf.json line 4: `"version": "2.5.0"`
   - gui-app/package.json line 4: `"version": "2.5.0"`
   - gui-light/src-tauri/Cargo.toml line 3: `version = "2.5.0"`
   - gui-light/src-tauri/tauri.conf.json line 4: `"version": "2.5.0"`
   - gui-light/package.json line 4: `"version": "2.5.0"`

2. Add socket2 and ipconfig to gui-app/src-tauri/Cargo.toml [dependencies] section (after the existing `chrono = "0.4"` line):
   ```toml
   ipconfig = "0.3"
   socket2 = { version = "0.5", features = ["all"] }
   ```

3. Add socket2 and ipconfig to gui-light/src-tauri/Cargo.toml [dependencies] section (after the existing `chrono = "0.4"` line, before `[profile.release]`):
   ```toml
   ipconfig = "0.3"
   socket2 = { version = "0.5", features = ["all"] }
   ```

Per D-02: dependencies restored from commit 308583b6 pattern. socket2 MUST have features = ["all"] (enables connect_timeout and SockAddr conversions).
  </action>
  <verify>
    <automated>cd gui-app/src-tauri && grep -c "2.5.0" Cargo.toml tauri.conf.json ../package.json && grep "socket2" Cargo.toml && grep "ipconfig" Cargo.toml && cd ../../gui-light/src-tauri && grep -c "2.5.0" Cargo.toml tauri.conf.json ../package.json && grep "socket2" Cargo.toml && grep "ipconfig" Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files contain "2.5.0" (no remaining "2.3.0" in version fields)
    - gui-app/src-tauri/Cargo.toml contains `ipconfig = "0.3"`
    - gui-app/src-tauri/Cargo.toml contains `socket2 = { version = "0.5", features = ["all"] }`
    - gui-light/src-tauri/Cargo.toml contains `ipconfig = "0.3"`
    - gui-light/src-tauri/Cargo.toml contains `socket2 = { version = "0.5", features = ["all"] }`
  </acceptance_criteria>
  <done>All 6 version files at 2.5.0, socket2 + ipconfig in both Cargo.toml files</done>
</task>

<task type="auto">
  <name>Task 2: Restore gui-app connectivity.rs from 798ce8e7 with verbose logging</name>
  <read_first>
    - gui-app/src-tauri/src/connectivity.rs (current 172-line version to be replaced)
    - Run: git show 798ce8e7:gui-app/src-tauri/src/connectivity.rs (the 253-line target)
    - gui-app/src-tauri/src/logging.rs (verify log_app signature)
  </read_first>
  <files>gui-app/src-tauri/src/connectivity.rs</files>
  <action>
Replace gui-app/src-tauri/src/connectivity.rs with the version from commit 798ce8e7 (253 lines), then enhance with verbose file logging per D-05 and D-06.

Step 1: Restore from git history.
Run `git show 798ce8e7:gui-app/src-tauri/src/connectivity.rs` and use that as the base.

Step 2: Add `use crate::logging::log_app;` to the imports.

Step 3: Add verbose file logging calls (D-06) throughout:

In `find_physical_adapter_ip()`:
- After finding an adapter: `log_app("DEBUG", &format!("[connectivity] Physical adapter found: {}", ip));`
- If no adapter found (at end, before returning None): `log_app("DEBUG", "[connectivity] No physical adapter found");`

In `check_connectivity()`:
- Already has `eprintln!` for adapter IP — keep those AND add log_app:
  - `log_app("DEBUG", &format!("[connectivity] Cycle: physical_ip={:?}", physical_ip));`
- After TCP spawn_blocking result: `log_app("DEBUG", &format!("[connectivity] TCP result: {}", tcp_ok));`
- After each HTTP endpoint attempt (inside the loop), on success: `log_app("DEBUG", &format!("[connectivity] HTTP {} => OK", url));`
- At function return false: `log_app("DEBUG", "[connectivity] All checks failed");`

In `check_adapter_online()`:
- After TCP result: `log_app("DEBUG", &format!("[connectivity] adapter_online TCP: {}", tcp_ok));`
- After HTTP result, on success: `log_app("DEBUG", "[connectivity] adapter_online HTTP => OK");`

In `start_monitor()`:
- At monitor start (after the initial 30s sleep, before the loop): `log_app("INFO", "[connectivity] Monitor started");`
- On state transition to offline: `log_app("WARN", "[connectivity] Declaring offline after 4 failures");`
- On state transition to online (restored): `log_app("INFO", "[connectivity] Internet restored");`
- On adapter recovery: `log_app("INFO", &format!("[connectivity] Adapter recovered after {} checks", adapter_wait));`
- On give_up: `log_app("WARN", "[connectivity] Gave up waiting for adapter after 5 minutes");`

Key constraints per decisions:
- D-01: Base is commit 798ce8e7, not a rewrite
- D-04: find_physical_adapter_ip() called every cycle (NOT cached) — already the case in 798ce8e7
- D-07: 4-consecutive-failure threshold preserved (already in 798ce8e7)
- D-08: No extra ipconfig adapter state checks before declaring offline
- D-03: When physical_ip is None, bind is skipped — default routing used silently (already in 798ce8e7 pattern)
  </action>
  <verify>
    <automated>cd gui-app/src-tauri && grep -c "use socket2" src/connectivity.rs && grep -c "find_physical_adapter_ip" src/connectivity.rs && grep -c "spawn_blocking" src/connectivity.rs && grep -c "local_address" src/connectivity.rs && grep -c "log_app" src/connectivity.rs && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - gui-app/src-tauri/src/connectivity.rs contains `use socket2::{Socket, Domain, Type, Protocol, SockAddr};`
    - gui-app/src-tauri/src/connectivity.rs contains `fn find_physical_adapter_ip() -> Option<IpAddr>`
    - gui-app/src-tauri/src/connectivity.rs contains `tokio::task::spawn_blocking`
    - gui-app/src-tauri/src/connectivity.rs contains `.local_address(physical_ip)`
    - gui-app/src-tauri/src/connectivity.rs contains `use crate::logging::log_app;`
    - gui-app/src-tauri/src/connectivity.rs contains at least 6 calls to `log_app(`
    - gui-app/src-tauri/src/connectivity.rs contains `ipconfig::get_adapters()`
    - gui-app/src-tauri/src/connectivity.rs contains `consecutive_failures >= 4` (D-07: 4-failure threshold)
    - `cargo check` in gui-app/src-tauri succeeds with no errors
  </acceptance_criteria>
  <done>gui-app connectivity.rs restored from 798ce8e7 with socket2 binding, ipconfig adapter detection, reqwest local_address, and verbose file logging. cargo check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App -> OS network API | ipconfig reads adapter info (read-only) |
| App -> Internet | Outbound TCP/HTTP to public IPs (1.1.1.1, 8.8.8.8, etc.) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I (Information Disclosure) | find_physical_adapter_ip | accept | Only reads local adapter names/IPs; no PII; logged to local file only |
| T-01-02 | D (Denial of Service) | spawn_blocking TCP checks | accept | 4s connect_timeout per target, max 3 targets = 12s worst case; runs in blocking pool, does not block async runtime |
| T-01-03 | T (Tampering) | Connectivity check results | accept | Checks go to well-known public IPs (1.1.1.1, 8.8.8.8); false results only affect VPN reconnect timing, not security |
</threat_model>

<verification>
1. `cargo check` passes in gui-app/src-tauri
2. grep confirms socket2, ipconfig, find_physical_adapter_ip, spawn_blocking, local_address present in connectivity.rs
3. grep confirms log_app calls present for verbose logging
4. All 6 version files show 2.5.0
</verification>

<success_criteria>
- gui-app connectivity.rs uses socket2 to bind TCP checks to physical adapter
- gui-app connectivity.rs uses reqwest local_address for HTTP checks
- find_physical_adapter_ip() filters out WinTUN/VPN/virtual adapters
- Fallback to default routing when no physical adapter (no crash, no UI error)
- Verbose file logging on every monitoring cycle
- cargo check passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-connectivity-bypass/01-01-SUMMARY.md`
</output>
