---
phase: 01-connectivity-bypass
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - gui-light/src-tauri/src/connectivity.rs
autonomous: true
requirements: [CONN-01, CONN-02, CONN-03, CONN-04, CONN-05]

must_haves:
  truths:
    - "gui-light connectivity checks bind to the physical adapter IP via socket2"
    - "gui-light HTTP checks use reqwest with local_address set to physical adapter IP"
    - "When no physical adapter found, gui-light falls back to default routing without error"
    - "gui-light preserves its lighter timing profile: 15s initial sleep, 15s cycle, 3-failure threshold"
    - "gui-light uses TCP-first checking (new) with HTTP fallback (existing 2 endpoints)"
    - "Both gui-app and gui-light compile successfully with cargo check"
  artifacts:
    - path: "gui-light/src-tauri/src/connectivity.rs"
      provides: "socket2-bound connectivity checks adapted for Light edition"
      contains: "use socket2"
  key_links:
    - from: "gui-light/src-tauri/src/connectivity.rs"
      to: "socket2 crate"
      via: "use socket2::{Socket, Domain, Type, Protocol, SockAddr}"
      pattern: "use socket2"
    - from: "gui-light/src-tauri/src/connectivity.rs"
      to: "ipconfig crate"
      via: "ipconfig::get_adapters()"
      pattern: "ipconfig::get_adapters"
    - from: "gui-light/src-tauri/src/connectivity.rs"
      to: "reqwest local_address"
      via: "ClientBuilder::local_address(physical_ip)"
      pattern: "local_address\\(physical_ip\\)"
---

<objective>
Port socket2/ipconfig connectivity bypass from gui-app to gui-light, preserving Light's timing profile and lighter endpoint set. Then verify both editions compile.

Purpose: gui-light must also route connectivity checks through the physical adapter to prevent false VPN reconnects, completing the full phase requirement across both editions.
Output: gui-light with working socket2-bound connectivity monitoring + verbose logging, both editions passing cargo check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-connectivity-bypass/01-CONTEXT.md
@.planning/phases/01-connectivity-bypass/01-RESEARCH.md
@.planning/phases/01-connectivity-bypass/01-01-SUMMARY.md

<interfaces>
<!-- gui-light logging API -->
From gui-light/src-tauri/src/logging.rs:
```rust
pub fn log_app(level: &str, message: &str);
```

<!-- gui-light connectivity.rs public API (must be preserved) -->
From gui-light/src-tauri/src/connectivity.rs:
```rust
pub fn start_monitor(app: tauri::AppHandle, is_connected: Arc<Mutex<bool>>);
```

<!-- gui-app connectivity.rs (completed in Plan 01) is the pattern reference -->
<!-- Read gui-app/src-tauri/src/connectivity.rs for the socket2 pattern to adapt -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port socket2 connectivity bypass to gui-light with Light timing profile</name>
  <read_first>
    - gui-light/src-tauri/src/connectivity.rs (current 131-line HTTP-only version)
    - gui-app/src-tauri/src/connectivity.rs (completed Plan 01 version — the pattern to adapt)
    - gui-light/src-tauri/src/logging.rs (verify log_app signature matches gui-app)
  </read_first>
  <files>gui-light/src-tauri/src/connectivity.rs</files>
  <action>
Rewrite gui-light/src-tauri/src/connectivity.rs to add socket2 binding, following the gui-app version from Plan 01 but preserving Light's timing profile and lighter endpoint set.

**Imports (replace existing):**
```rust
use socket2::{Socket, Domain, Type, Protocol, SockAddr};
use std::net::{IpAddr, SocketAddr};
use std::sync::{Arc, Mutex};
use std::time::Duration;
use tauri::Emitter;
use crate::logging::log_app;
```

**Add `find_physical_adapter_ip()` — identical to gui-app version:**
```rust
fn find_physical_adapter_ip() -> Option<IpAddr> {
    let adapters = ipconfig::get_adapters().ok()?;
    adapters
        .iter()
        .filter(|a| a.oper_status() == ipconfig::OperStatus::IfOperStatusUp)
        .filter(|a| !a.gateways().is_empty())
        .filter(|a| {
            let if_type = a.if_type();
            if_type == ipconfig::IfType::EthernetCsmacd
                || if_type == ipconfig::IfType::Ieee80211
        })
        .filter(|a| {
            let desc = a.description().to_lowercase();
            !desc.contains("wintun")
                && !desc.contains("vpn")
                && !desc.contains("virtual")
                && !desc.contains("tap-")
        })
        .flat_map(|a| a.ip_addresses().iter().copied())
        .find(|ip| ip.is_ipv4())
}
```

**Rewrite `check_connectivity()` — socket2 TCP first, then HTTP fallback:**
- Use socket2 spawn_blocking TCP check with 2 targets (not 3 like Pro):
  - `SocketAddr::new([1, 1, 1, 1].into(), 443)` (Cloudflare)
  - `SocketAddr::new([8, 8, 8, 8].into(), 443)` (Google)
- Bind to `physical_ip` if available, skip bind if None (D-03/D-04)
- 4s connect_timeout per target (same as Pro)
- HTTP fallback with reqwest `local_address(physical_ip)`: 2 endpoints (Light's existing set):
  - `"https://clients3.google.com/generate_204"`
  - `"https://cp.cloudflare.com"`
- 5s HTTP timeout (same as Pro)

**Rewrite `check_adapter_online()` — socket2 TCP first, then HTTP fallback:**
- TCP to `1.1.1.1:443` via socket2 bound to physical adapter, 3s timeout
- HTTP fallback to `http://clients3.google.com/generate_204` via reqwest with local_address
- Pattern identical to gui-app but single target

**Preserve `start_monitor()` Light timing profile:**
- Initial sleep: `15` seconds (NOT Pro's 30s)
- Cycle interval: `15` seconds (NOT Pro's 20s)
- Failure threshold: `3` consecutive failures (NOT Pro's 4)
- Keep existing monitor structure (VPN state check, emit internet-status, adapter recovery wait)

**Add verbose file logging (D-05, D-06) — same pattern as gui-app Plan 01:**
- `find_physical_adapter_ip()`: log found adapter IP or "no adapter found"
- `check_connectivity()`: log physical_ip, TCP result, HTTP results
- `check_adapter_online()`: log TCP result, HTTP result
- `start_monitor()`: log monitor start, state transitions (online/offline/restored), adapter recovery, give_up

All log calls use `log_app("LEVEL", &format!("[connectivity] ..."))` or `log_app("LEVEL", "[connectivity] ...")`.
  </action>
  <verify>
    <automated>cd gui-light/src-tauri && grep -c "use socket2" src/connectivity.rs && grep -c "find_physical_adapter_ip" src/connectivity.rs && grep -c "spawn_blocking" src/connectivity.rs && grep -c "local_address" src/connectivity.rs && grep -c "log_app" src/connectivity.rs && grep "from_secs(15)" src/connectivity.rs | head -3 && grep "consecutive_failures >= 3" src/connectivity.rs</automated>
  </verify>
  <acceptance_criteria>
    - gui-light/src-tauri/src/connectivity.rs contains `use socket2::{Socket, Domain, Type, Protocol, SockAddr};`
    - gui-light/src-tauri/src/connectivity.rs contains `fn find_physical_adapter_ip() -> Option<IpAddr>`
    - gui-light/src-tauri/src/connectivity.rs contains `tokio::task::spawn_blocking`
    - gui-light/src-tauri/src/connectivity.rs contains `.local_address(physical_ip)`
    - gui-light/src-tauri/src/connectivity.rs contains `use crate::logging::log_app;`
    - gui-light/src-tauri/src/connectivity.rs contains at least 6 calls to `log_app(`
    - gui-light/src-tauri/src/connectivity.rs contains `ipconfig::get_adapters()`
    - gui-light/src-tauri/src/connectivity.rs contains `consecutive_failures >= 3` (Light's 3-failure threshold, NOT Pro's 4)
    - gui-light/src-tauri/src/connectivity.rs contains `Duration::from_secs(15)` for initial sleep and cycle interval
    - gui-light/src-tauri/src/connectivity.rs does NOT contain `Duration::from_secs(30)` (that's Pro's timing)
    - gui-light/src-tauri/src/connectivity.rs does NOT contain `Duration::from_secs(20)` (that's Pro's cycle)
  </acceptance_criteria>
  <done>gui-light connectivity.rs upgraded with socket2 binding, ipconfig adapter detection, reqwest local_address, verbose logging, Light timing profile preserved (15s/15s/3-failure)</done>
</task>

<task type="auto">
  <name>Task 2: Verify both editions compile with cargo check</name>
  <read_first>
    - gui-app/src-tauri/Cargo.toml (verify socket2/ipconfig deps present)
    - gui-light/src-tauri/Cargo.toml (verify socket2/ipconfig deps present)
  </read_first>
  <files>gui-app/src-tauri/src/connectivity.rs, gui-light/src-tauri/src/connectivity.rs</files>
  <action>
Run `cargo check` in both editions to verify everything compiles:

1. `cd gui-app/src-tauri && cargo check 2>&1`
   - Must succeed with no errors
   - Warnings are acceptable but no errors

2. `cd gui-light/src-tauri && cargo check 2>&1`
   - Must succeed with no errors
   - Warnings are acceptable but no errors

If either fails:
- Read the error message carefully
- Common issues (from RESEARCH.md pitfalls):
  - Missing `features = ["all"]` on socket2 => `connect_timeout` not found
  - Wrong ipconfig version => IfType variants not found
  - gui-light logging.rs has different API => adapt log_app calls
- Fix the issue in the failing connectivity.rs and re-run cargo check

Do NOT modify any files if both checks pass. This task is verification-only unless compilation fails.
  </action>
  <verify>
    <automated>cd gui-app/src-tauri && cargo check 2>&1 | tail -3 && cd ../../gui-light/src-tauri && cargo check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check` in gui-app/src-tauri exits with code 0 (output contains "Finished" or no error lines)
    - `cargo check` in gui-light/src-tauri exits with code 0 (output contains "Finished" or no error lines)
    - No "error[E" in either cargo check output
  </acceptance_criteria>
  <done>Both gui-app and gui-light compile successfully with socket2/ipconfig connectivity bypass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App -> OS network API | ipconfig reads adapter info (read-only) |
| App -> Internet | Outbound TCP/HTTP to public IPs (1.1.1.1, 8.8.8.8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I (Information Disclosure) | find_physical_adapter_ip | accept | Same as Plan 01 — reads local adapter names/IPs, logged locally only |
| T-01-02 | D (Denial of Service) | spawn_blocking TCP checks | accept | Same as Plan 01 — 4s timeout, 2 targets max, runs in blocking pool |
</threat_model>

<verification>
1. `cargo check` passes in both gui-app/src-tauri and gui-light/src-tauri
2. gui-light connectivity.rs has socket2, ipconfig, spawn_blocking, local_address
3. gui-light preserves 15s/15s/3-failure timing (not Pro's 30s/20s/4)
4. Both editions have verbose log_app calls in connectivity.rs
</verification>

<success_criteria>
- gui-light connectivity.rs uses socket2 to bind TCP checks to physical adapter
- gui-light connectivity.rs uses reqwest local_address for HTTP checks
- gui-light preserves Light timing: 15s initial, 15s cycle, 3-failure threshold
- Both editions compile with cargo check (no errors)
- Phase complete: CONN-01 through CONN-05 addressed in both gui-app and gui-light
</success_criteria>

<output>
After completion, create `.planning/phases/01-connectivity-bypass/01-02-SUMMARY.md`
</output>
