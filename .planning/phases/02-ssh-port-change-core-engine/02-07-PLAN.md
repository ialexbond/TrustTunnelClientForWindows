---
phase: 02-ssh-port-change-core-engine
plan: 07
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06]
files_modified:
  - gui-app/src/shared/ui/index.ts
  - memory/v3/design-system/components.md
autonomous: false
requirements: [DOC-02]

must_haves:
  truths:
    - "index.ts exports all 26 components (19 redesigned + 7 new)"
    - "memory/v3/design-system/components.md documents every component with props, variants, states, dependencies"
    - "npm test passes (all existing tests still work after full Phase 2 redesign)"
    - "Storybook builds successfully"
    - "User has visually inspected components in Storybook"
  artifacts:
    - path: "gui-app/src/shared/ui/index.ts"
      provides: "Barrel exports for all primitives"
      exports: ["Button", "Badge", "Input", "NumberInput", "PasswordInput", "ActionInput", "ActionPasswordInput", "Select", "Modal", "ConfirmDialog", "Toggle", "Tooltip", "ErrorBanner", "Card", "CardHeader", "SnackBar", "SnackBarContext", "IconButton", "DropOverlay", "PanelErrorBoundary", "Section", "SectionHeader", "FormField", "StatusBadge", "EmptyState", "Separator", "ProgressBar"]
    - path: "memory/v3/design-system/components.md"
      provides: "Component documentation"
  key_links:
    - from: "gui-app/src/shared/ui/index.ts"
      to: "all component files"
      via: "export { X } from './X'"
      pattern: "export.*from"
---

<objective>
Finalize Phase 2: update index.ts barrel exports, write component documentation, run full test suite, and get visual sign-off in Storybook.

Purpose: This is the integration plan. All 26 components are built in Plans 01-06. This plan wires them into the barrel export, creates the documentation (DOC-02), validates everything works together, and gets user approval.
Output: Updated index.ts, component documentation, visual checkpoint.
</objective>

<execution_context>
@C:/Users/naska/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/naska/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ssh-port-change-core-engine/02-CONTEXT.md
@.planning/phases/02-ssh-port-change-core-engine/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update index.ts exports + write component documentation + run full validation</name>
  <files>gui-app/src/shared/ui/index.ts, memory/v3/design-system/components.md</files>
  <read_first>
    - gui-app/src/shared/ui/index.ts
    - gui-app/src/shared/ui/Button.tsx
    - gui-app/src/shared/ui/Section.tsx
    - gui-app/src/shared/ui/StatusBadge.tsx
    - gui-app/src/shared/ui/FormField.tsx
    - gui-app/src/shared/ui/EmptyState.tsx
    - gui-app/src/shared/ui/Separator.tsx
    - gui-app/src/shared/ui/ProgressBar.tsx
    - gui-app/src/shared/ui/Select.tsx
    - memory/v3/design-system/tokens.md
    - .planning/phases/02-ssh-port-change-core-engine/02-UI-SPEC.md
  </read_first>
  <action>
**index.ts — update barrel exports:**

Update gui-app/src/shared/ui/index.ts to export ALL components. The file must contain exactly these exports (sorted alphabetically):

```ts
export { ActionInput } from "./ActionInput";
export { ActionPasswordInput } from "./ActionPasswordInput";
export { Badge, badgeVariants } from "./Badge";
export { Button, buttonVariants } from "./Button";
export { Card, CardHeader } from "./Card";
export { ConfirmDialog } from "./ConfirmDialog";
export { DropOverlay } from "./DropOverlay";
export { EmptyState } from "./EmptyState";
export { ErrorBanner, errorBannerVariants } from "./ErrorBanner";
export { FormField } from "./FormField";
export { IconButton } from "./IconButton";
export { Input } from "./Input";
export { Modal } from "./Modal";
export { NumberInput } from "./NumberInput";
export { PanelErrorBoundary } from "./PanelErrorBoundary";
export { PasswordInput } from "./PasswordInput";
export { ProgressBar } from "./ProgressBar";
export { Section, SectionHeader } from "./Section";
export { Select } from "./Select";
export { Separator } from "./Separator";
export { SnackBar } from "./SnackBar";
export { SnackBarContext } from "./SnackBarContext";
export { StatusBadge, statusBadgeVariants } from "./StatusBadge";
export { Toggle } from "./Toggle";
export { Tooltip } from "./Tooltip";
```

Also export CVA variant functions (buttonVariants, badgeVariants, errorBannerVariants, statusBadgeVariants) so consumers can compose variants.

**memory/v3/design-system/components.md — per DOC-02:**

Create documentation file covering every component. For each component, document:
1. **Name** and file path
2. **Props** — full interface with types and defaults
3. **Variants** (if CVA) — list all variant values
4. **States** — which interaction states are supported (default, hover, focus, active, disabled, error, loading)
5. **Dependencies** — what other components or hooks it uses
6. **Token usage** — which CSS custom properties it references
7. **Usage example** — minimal JSX example

Group components into sections:
- **CVA Components** (Button, Badge, ErrorBanner, StatusBadge, Modal)
- **Input Family** (Input, NumberInput, PasswordInput, ActionInput, ActionPasswordInput)
- **Layout** (Section, FormField, Card, Separator, EmptyState)
- **Interactive** (Toggle, Select, Tooltip, ConfirmDialog, IconButton)
- **Feedback** (SnackBar, ProgressBar, DropOverlay, PanelErrorBoundary)
- **Providers** (SnackBarContext)

**Run full validation:**

After creating both files, run:
1. `cd gui-app && npx tsc --noEmit` — TypeScript compilation must pass (imports resolve)
2. `cd gui-app && npm test` — all existing tests must pass
3. `cd gui-app && npx storybook build --quiet` — Storybook static build must succeed
4. Verify: `grep -c "import.*colors" gui-app/src/shared/ui/*.tsx` — must return 0 for ALL files except colors.ts itself
  </action>
  <verify>
    <automated>cd gui-app && npx tsc --noEmit 2>&1 | tail -10 && npm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - gui-app/src/shared/ui/index.ts contains at least 25 export lines (all components)
    - gui-app/src/shared/ui/index.ts contains exports for: Button, Badge, ErrorBanner, Input, NumberInput, PasswordInput, ActionInput, ActionPasswordInput, Select, Modal, ConfirmDialog, Toggle, Tooltip, Card, SnackBar, IconButton, DropOverlay, PanelErrorBoundary, Section, FormField, StatusBadge, EmptyState, Separator, ProgressBar
    - gui-app/src/shared/ui/index.ts contains CVA variant exports: buttonVariants, badgeVariants, errorBannerVariants, statusBadgeVariants
    - memory/v3/design-system/components.md exists and contains sections for all component groups
    - memory/v3/design-system/components.md contains props documentation for Button, Badge, Input, Select, Modal, Section, FormField, StatusBadge, EmptyState, Separator, ProgressBar
    - `npx tsc --noEmit` exits with code 0 (no TypeScript errors)
    - `npm test` passes (all tests green)
    - `grep -c "import.*colors" gui-app/src/shared/ui/Button.tsx gui-app/src/shared/ui/Badge.tsx gui-app/src/shared/ui/ErrorBanner.tsx gui-app/src/shared/ui/Select.tsx` returns 0 for each file
  </acceptance_criteria>
  <done>index.ts exports all 26 components. Component documentation complete. TypeScript compiles. All tests pass. Zero colors.ts imports in redesigned components.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual sign-off of all components in Storybook</name>
  <files>gui-app/src/shared/ui/*.stories.tsx</files>
  <action>
Start Storybook for visual review. The executor should run `cd gui-app && npm run storybook` and notify the user that Storybook is ready for inspection at http://localhost:6006.
  </action>
  <what-built>
Complete Phase 2 Primitive Redesign: 19 existing components redesigned with CVA + token-driven styling, 7 new components created (Section, FormField, StatusBadge, EmptyState, Separator, ProgressBar, Select custom dropdown). All 26 components have Storybook stories with autodocs.
  </what-built>
  <how-to-verify>
1. Start Storybook: `cd gui-app && npm run storybook`
2. Open http://localhost:6006 in browser
3. Navigate sidebar: Primitives section should list all 26 components
4. For each component, verify:
   - **Button:** 4 variants (primary/danger/ghost/icon), 3 sizes (sm/md/lg), loading spinner
   - **Badge:** 5 variants (success/warning/danger/neutral/dot)
   - **ErrorBanner:** 3 severities (error/warning/info)
   - **StatusBadge:** 4 VPN states (connected/connecting/error/disconnected) with Russian labels
   - **Input family:** clearable X button, helper text, error state
   - **Select:** opens dropdown, keyboard arrows work, placeholder "Выберите..."
   - **Modal:** 3 sizes (sm/md/lg), overlay click closes
   - **Section:** collapsible toggle works
   - **FormField:** required * indicator, error message
   - **EmptyState:** "Ничего нет" default
   - **Separator:** horizontal, vertical, with label
   - **ProgressBar:** filled bar with accent color
5. Toggle dark/light theme via Storybook toolbar — all components should look correct in both themes
6. Check: no broken colors, no missing backgrounds, no invisible text
  </how-to-verify>
  <verify>Visual inspection in Storybook by user</verify>
  <done>User has inspected all 26 components in Storybook, both dark and light themes, and approved the visual quality.</done>
  <resume-signal>Type "approved" if all components look correct in both themes, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index.ts barrel export -> consumers | Public API surface for all UI components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Tampering | index.ts re-exports | accept | Barrel exports only re-export; no logic, no data flow |
</threat_model>

<verification>
1. TypeScript compilation passes
2. All tests pass
3. Storybook builds successfully
4. User visually approves components in Storybook
5. Zero colors.ts imports in redesigned components
</verification>

<success_criteria>
- index.ts exports all 26 components + CVA variant functions
- memory/v3/design-system/components.md documents all components
- TypeScript compiles with zero errors
- All tests pass
- Storybook renders all components correctly
- User approves visual quality in both themes
</success_criteria>

<output>
After completion, create `.planning/phases/02-ssh-port-change-core-engine/02-07-SUMMARY.md`
</output>
