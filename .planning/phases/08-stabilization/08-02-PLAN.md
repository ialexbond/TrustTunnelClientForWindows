---
phase: 08-stabilization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gui-app/src/shared/ui/ConfirmDialog.tsx
  - gui-app/src/shared/ui/ConfirmDialog.test.tsx
  - gui-app/src/shared/i18n/locales/ru.json
  - gui-app/src/shared/i18n/locales/en.json
autonomous: true
requirements: [STAB-01, STAB-04, STAB-10]
must_haves:
  truths:
    - "ConfirmDialog uses t() for default confirm/cancel text, not hardcoded English strings"
    - "ConfirmDialog tests find buttons by i18n-resolved Russian text"
    - "ru.json and en.json have keys for confirmDialog.confirm and confirmDialog.cancel"
  artifacts:
    - path: "gui-app/src/shared/ui/ConfirmDialog.tsx"
      provides: "i18n-powered default button texts"
      contains: "useTranslation"
    - path: "gui-app/src/shared/i18n/locales/ru.json"
      provides: "Russian locale keys for ConfirmDialog"
      contains: "confirmDialog"
    - path: "gui-app/src/shared/i18n/locales/en.json"
      provides: "English locale keys for ConfirmDialog"
      contains: "confirmDialog"
  key_links:
    - from: "gui-app/src/shared/ui/ConfirmDialog.tsx"
      to: "gui-app/src/shared/i18n/locales/ru.json"
      via: "useTranslation t()"
      pattern: "t\\(\"confirmDialog\\."
---

<objective>
Fix ConfirmDialog i18n: replace hardcoded "Confirm"/"Cancel" defaults with t() calls and add i18n keys.

Purpose: Close root cause #3 (i18n hardcoded strings in ConfirmDialog) per D-04, D-06, D-07. Fix 4 ConfirmDialog tests plus downstream tests (FoundStep) that depend on ConfirmDialog rendering Russian text.
Output: ConfirmDialog.tsx with t() defaults, updated test, i18n keys in both locales
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-stabilization/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n keys and fix ConfirmDialog to use t()</name>
  <read_first>
    gui-app/src/shared/ui/ConfirmDialog.tsx
    gui-app/src/shared/i18n/locales/ru.json
    gui-app/src/shared/i18n/locales/en.json
  </read_first>
  <files>gui-app/src/shared/ui/ConfirmDialog.tsx, gui-app/src/shared/i18n/locales/ru.json, gui-app/src/shared/i18n/locales/en.json</files>
  <action>
1. Add i18n keys to `gui-app/src/shared/i18n/locales/ru.json`. Find the existing `"empty"` section (around line 1050) and add a new `"confirmDialog"` section after it (or near other UI component keys):

```json
"confirmDialog": {
  "confirm": "Удалить",
  "cancel": "Отмена"
}
```

2. Add matching keys to `gui-app/src/shared/i18n/locales/en.json`:

```json
"confirmDialog": {
  "confirm": "Confirm",
  "cancel": "Cancel"
}
```

3. In `gui-app/src/shared/ui/ConfirmDialog.tsx` (per D-04, D-06):

Add `useTranslation` import:
```typescript
import { useTranslation } from "react-i18next";
```

Inside the `ConfirmDialog` function body, add `t()` call at the top:
```typescript
const { t } = useTranslation();
```

Replace the hardcoded default props (lines 25, 27):
```typescript
// BEFORE:
confirmText = "Confirm",
cancelText = "Cancel",
```

Remove the default values from destructuring and resolve them in the body:
```typescript
// In destructuring:
confirmText,
cancelText,

// In body (after the const { t } = useTranslation() line):
const resolvedConfirmText = confirmLabel ?? confirmText ?? t("confirmDialog.confirm");
const resolvedCancelText = cancelLabel ?? cancelText ?? t("confirmDialog.cancel");
```

This follows D-06 pattern: `t()` called in body, not in default props. When callers pass explicit `confirmText`/`cancelText`, those take priority. When omitted, i18n provides the default.

NOTE: The existing `resolvedConfirmText` and `resolvedCancelText` lines (around line 35-36) already exist. Replace them with the new versions above that use `t()` as fallback.
  </action>
  <verify>
    <automated>cd gui-app && npx vitest run src/shared/ui/ConfirmDialog.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ConfirmDialog.tsx` contains `import { useTranslation } from "react-i18next"`
    - `ConfirmDialog.tsx` contains `t("confirmDialog.confirm")`
    - `ConfirmDialog.tsx` contains `t("confirmDialog.cancel")`
    - `ConfirmDialog.tsx` does NOT contain `confirmText = "Confirm"` as default
    - `ConfirmDialog.tsx` does NOT contain `cancelText = "Cancel"` as default
    - `ru.json` contains `"confirmDialog"` section with `"confirm": "Удалить"` and `"cancel": "Отмена"`
    - `en.json` contains `"confirmDialog"` section with `"confirm": "Confirm"` and `"cancel": "Cancel"`
  </acceptance_criteria>
  <done>ConfirmDialog uses t() for default texts, i18n keys present in both locales</done>
</task>

<task type="auto">
  <name>Task 2: Fix ConfirmDialog.test.tsx to work with i18n defaults</name>
  <read_first>
    gui-app/src/shared/ui/ConfirmDialog.test.tsx
    gui-app/src/shared/ui/ConfirmDialog.tsx
  </read_first>
  <files>gui-app/src/shared/ui/ConfirmDialog.test.tsx</files>
  <action>
The ConfirmDialog tests expect Russian text ("Удалить", "Отмена") for default button labels. After the i18n fix in Task 1, ConfirmDialog will use `t("confirmDialog.confirm")` and `t("confirmDialog.cancel")`.

The test file currently does NOT call `i18n.changeLanguage("ru")`. The test setup imports `../shared/i18n` via `setup.ts` but the default language may vary.

Add i18n import and `beforeEach` language setting:

1. Add import at the top:
```typescript
import i18n from "../../shared/i18n";
```

2. Add to the existing `beforeEach` block (lines 19-24), add as first line:
```typescript
i18n.changeLanguage("ru");
```

The 4 failing tests will now work:
- "renders default confirm text 'Удалить'" -- `t("confirmDialog.confirm")` returns "Удалить" in ru
- "renders default cancel text 'Отмена'" -- `t("confirmDialog.cancel")` returns "Отмена" in ru
- "calls onConfirm when confirm button clicked" -- finds "Удалить" button
- "calls onCancel when cancel button clicked" -- finds "Отмена" button

No changes to test assertions needed. The tests already look for "Удалить" and "Отмена" which are the Russian translations.
  </action>
  <verify>
    <automated>cd gui-app && npx vitest run src/shared/ui/ConfirmDialog.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ConfirmDialog.test.tsx` contains `import i18n from "../../shared/i18n"`
    - `ConfirmDialog.test.tsx` contains `i18n.changeLanguage("ru")`
    - `npx vitest run src/shared/ui/ConfirmDialog.test.tsx` shows 0 failures, all 12 tests pass
  </acceptance_criteria>
  <done>All 12 ConfirmDialog tests pass. Default confirm/cancel texts resolve via i18n.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n key resolution | User-facing text must resolve correctly in both ru and en |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | I (Information Disclosure) | i18n keys | accept | No sensitive data in button labels |
</threat_model>

<verification>
```bash
cd gui-app && npx vitest run src/shared/ui/ConfirmDialog.test.tsx --reporter=verbose
```
All 12 ConfirmDialog tests pass with 0 failures.
</verification>

<success_criteria>
- 4 previously failing ConfirmDialog tests now pass
- ConfirmDialog renders Russian text by default (via i18n, not hardcoded)
- Custom confirmText/cancelText props still override defaults
- FoundStep "confirm uninstall dialog closes when cancelled" test is unblocked (ConfirmDialog now renders "Отмена" as cancel text)
</success_criteria>

<output>
After completion, create `.planning/phases/08-stabilization/08-02-SUMMARY.md`
</output>
