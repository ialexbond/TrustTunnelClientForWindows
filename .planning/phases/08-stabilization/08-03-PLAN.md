---
phase: 08-stabilization
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - gui-app/src/components/ConfigPanel.test.tsx
  - gui-app/src/components/ControlPanelPage.test.tsx
  - gui-app/src/components/server/SshConnectForm.test.tsx
  - gui-app/src/components/wizard/FoundStep.test.tsx
  - gui-app/src/components/routing/ProcessFilterSection.test.tsx
autonomous: true
requirements: [STAB-01, STAB-04, STAB-05]
must_haves:
  truths:
    - "All 19 previously failing tests pass"
    - "npm test returns 0 failures"
    - "Tests use i18n.changeLanguage('ru') to resolve Russian text"
    - "Tests match actual component output, not stale assumptions"
  artifacts:
    - path: "gui-app/src/components/ConfigPanel.test.tsx"
      provides: "Fixed test with i18n initialization"
      contains: "i18n.changeLanguage"
    - path: "gui-app/src/components/ControlPanelPage.test.tsx"
      provides: "Fixed mock text matching actual i18n output"
      contains: "Сменить сервер"
    - path: "gui-app/src/components/server/SshConnectForm.test.tsx"
      provides: "Fixed label assertion matching actual i18n key"
      contains: "labels.server_address"
    - path: "gui-app/src/components/wizard/FoundStep.test.tsx"
      provides: "Fixed ConfirmDialog cancel button detection"
      contains: "confirmDialog.cancel"
    - path: "gui-app/src/components/routing/ProcessFilterSection.test.tsx"
      provides: "Fixed modal detection after RAF mock"
      contains: "Выберите процессы"
  key_links:
    - from: "gui-app/src/test/setup.ts"
      to: "all test files"
      via: "RAF mock enables Modal rendering"
      pattern: "requestAnimationFrame"
---

<objective>
Fix remaining 14 failing tests across 5 test files: ConfigPanel, ControlPanelPage, SshConnectForm, FoundStep, ProcessFilterSection.

Purpose: With Plan 01 (RAF mock, Section, Button) and Plan 02 (ConfirmDialog i18n) complete, fix the remaining test files that fail due to missing i18n initialization, stale mock text, or wrong i18n key references.
Output: All 19 previously failing tests pass, `npm test` shows 0 failures.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-stabilization/08-CONTEXT.md
@.planning/phases/08-stabilization/08-01-SUMMARY.md
@.planning/phases/08-stabilization/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ConfigPanel.test.tsx and ControlPanelPage.test.tsx</name>
  <read_first>
    gui-app/src/components/ConfigPanel.test.tsx
    gui-app/src/components/ConfigPanel.tsx
    gui-app/src/components/ControlPanelPage.test.tsx
    gui-app/src/components/ControlPanelPage.tsx
    gui-app/src/shared/i18n/locales/ru.json
  </read_first>
  <files>gui-app/src/components/ConfigPanel.test.tsx, gui-app/src/components/ControlPanelPage.test.tsx</files>
  <action>
**ConfigPanel.test.tsx — 6 failures, root cause: missing i18n initialization**

The test never calls `i18n.changeLanguage("ru")`. The component uses `t('configPanel.title')`, `t('configPanel.configFile')`, `t('configPanel.selectFile')`, `t('configPanel.logLevel')`. In ru.json these translate to "Настройки", "Файл конфигурации", "Выбрать файл", "Уровень логирования". Without setting language to "ru", the test gets fallback keys or English.

1. Add import at top:
```typescript
import i18n from "../shared/i18n";
```

2. Add `beforeEach` setup (modify existing `beforeEach` block at line 14):
```typescript
beforeEach(() => {
  vi.clearAllMocks();
  i18n.changeLanguage("ru");
});
```

This fixes all 6 ConfigPanel failures because the test assertions already use the correct Russian strings ("Настройки", "Файл конфигурации", "Выбрать файл", "Уровень логирования").

**ControlPanelPage.test.tsx — 3 failures, root cause: mock text mismatch + missing i18n**

The test expects `screen.getByRole("button", { name: /Сменить сервер/i })` but the mocked ServerPanel renders `<button data-testid="mock-disconnect-btn" onClick={props.onDisconnect}>Disconnect</button>`. The i18n key `control.disconnect` = "Сменить сервер" in Russian but the mock hardcodes "Disconnect".

1. Add import at top (if not already):
```typescript
import i18n from "../shared/i18n";
```

2. Add `i18n.changeLanguage("ru")` to existing `beforeEach` block (line 73).

3. Fix the ServerPanel mock at the top of the file. Change the mock disconnect button from:
```tsx
<button data-testid="mock-disconnect-btn" onClick={props.onDisconnect}>Disconnect</button>
```
to:
```tsx
<button data-testid="mock-disconnect-btn" onClick={props.onDisconnect}>Сменить сервер</button>
```

This matches what the tests expect at lines 117, 132, 243. The mock must render the same text the test looks for.

Alternatively (and better per D-09 pattern): change the 3 failing test assertions to use `data-testid` instead of text matching:
```typescript
// Line 117: change
const btn = screen.getByRole("button", { name: /Сменить сервер/i });
// to:
const btn = screen.getByTestId("mock-disconnect-btn");
```

Apply the same change at lines 132 and 243. This is more robust since it tests the mock callback, not the mock label text.

Choose the `data-testid` approach since the mock is not testing real component rendering. Apply this consistently to all 3 test assertions that use `/Сменить сервер/i`.
  </action>
  <verify>
    <automated>cd gui-app && npx vitest run src/components/ConfigPanel.test.tsx src/components/ControlPanelPage.test.tsx --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `ConfigPanel.test.tsx` contains `i18n.changeLanguage("ru")`
    - `ControlPanelPage.test.tsx` contains `i18n.changeLanguage("ru")`
    - `ControlPanelPage.test.tsx` uses `getByTestId("mock-disconnect-btn")` for disconnect button assertions
    - `npx vitest run src/components/ConfigPanel.test.tsx` shows 0 failures
    - `npx vitest run src/components/ControlPanelPage.test.tsx` shows 0 failures
  </acceptance_criteria>
  <done>ConfigPanel (6 fixed) and ControlPanelPage (3 fixed) all pass</done>
</task>

<task type="auto">
  <name>Task 2: Fix SshConnectForm.test.tsx, FoundStep.test.tsx, ProcessFilterSection.test.tsx</name>
  <read_first>
    gui-app/src/components/server/SshConnectForm.test.tsx
    gui-app/src/components/server/SshConnectForm.tsx
    gui-app/src/components/wizard/FoundStep.test.tsx
    gui-app/src/components/routing/ProcessFilterSection.test.tsx
    gui-app/src/shared/i18n/locales/ru.json
  </read_first>
  <files>gui-app/src/components/server/SshConnectForm.test.tsx, gui-app/src/components/wizard/FoundStep.test.tsx, gui-app/src/components/routing/ProcessFilterSection.test.tsx</files>
  <action>
**SshConnectForm.test.tsx — 1 failure: "renders server IP and port labels"**

Root cause: Test at line 191 checks `i18n.t("labels.server_ip")` = "IP сервера", but the component SshConnectForm.tsx line 137 uses `t("labels.server_address")` = "Адрес сервера". The i18n key changed from `labels.server_ip` to `labels.server_address` during v3.0 redesign but the test was not updated.

Fix line 191 in SshConnectForm.test.tsx:
```typescript
// BEFORE:
expect(screen.getByText(i18n.t("labels.server_ip"))).toBeInTheDocument();
// AFTER:
expect(screen.getByText(i18n.t("labels.server_address"))).toBeInTheDocument();
```

This matches the actual component which renders `t("labels.server_address")` on the input label.

**FoundStep.test.tsx — 1 failure: "confirm uninstall dialog closes when cancelled"**

Root cause: The test at lines 734-739 looks for a button with text matching `i18n.t("buttons.cancel")` or literal "Отмена". After Plan 02 fixes ConfirmDialog to use `t("confirmDialog.cancel")` which returns "Отмена" in Russian, the button text will be "Отмена".

However, the test has a fragile button-finding loop. The issue is that ConfirmDialog previously rendered "Cancel" (hardcoded English), so the loop at line 736 checking for `i18n.t("buttons.cancel")` (which = "Отмена" in ru.json at `buttons.cancel`) OR literal `"Отмена"` never matched "Cancel".

After Plan 02's fix, ConfirmDialog will render "Отмена" (from `t("confirmDialog.cancel")`), so the existing loop logic SHOULD work. But verify: `i18n.t("buttons.cancel")` = "Отмена" (line 49 in ru.json: `"cancel": "Отмена"`).

The fix is already provided by Plan 02 (ConfirmDialog now renders "Отмена"). But ensure the test's `beforeEach` sets `i18n.changeLanguage("ru")` — it already does at line 10.

If the test still fails after Plan 02's ConfirmDialog fix, the issue is the RAF mock timing. With Plan 01's setup.ts RAF mock, the ConfirmDialog's Modal should render synchronously. Verify by running the test.

If the test still has issues with the button-finding loop, simplify the cancel button detection:
```typescript
// Replace lines 732-739 with:
const cancelBtn = screen.getByText(i18n.t("confirmDialog.cancel"));
fireEvent.click(cancelBtn);
```

Note: This requires `i18n.t("confirmDialog.cancel")` to exist after Plan 02 adds the key. The test already imports i18n.

**ProcessFilterSection.test.tsx — 1 failure: "calls onLoadProcesses and opens picker on add click"**

Root cause: After clicking "Добавить процесс", a modal should open showing "Выберите процессы". The modal uses the double-RAF animation pattern. Plan 01's setup.ts RAF mock makes this synchronous, so the modal content renders immediately.

Verify the test passes after Plan 01's RAF mock is in place. If it still fails, the issue might be that `onLoadProcesses` is async (`mockResolvedValue(undefined)`) and the modal opens after the promise resolves.

If the test still fails, wrap the assertion in `waitFor`:
```typescript
// Change line 92 from:
expect(screen.getByText("Выберите процессы")).toBeInTheDocument();
// to:
await waitFor(() => {
  expect(screen.getByText("Выберите процессы")).toBeInTheDocument();
});
```

The test already uses `async` and `userEvent.click` (which is async-aware), so adding `waitFor` for the modal appearance is the correct pattern.
  </action>
  <verify>
    <automated>cd gui-app && npx vitest run src/components/server/SshConnectForm.test.tsx src/components/wizard/FoundStep.test.tsx src/components/routing/ProcessFilterSection.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `SshConnectForm.test.tsx` contains `labels.server_address` instead of `labels.server_ip`
    - `FoundStep.test.tsx` cancel button detection works (finds "Отмена" from ConfirmDialog i18n)
    - `ProcessFilterSection.test.tsx` modal detection works (RAF mock makes it synchronous)
    - `npx vitest run src/components/server/SshConnectForm.test.tsx` shows 0 failures
    - `npx vitest run src/components/wizard/FoundStep.test.tsx` shows 0 failures
    - `npx vitest run src/components/routing/ProcessFilterSection.test.tsx` shows 0 failures
  </acceptance_criteria>
  <done>All 3 remaining test files pass. Combined with Plans 01 and 02, all 19 previously failing tests now pass.</done>
</task>

<task type="auto">
  <name>Task 3: Full test suite verification</name>
  <read_first>
    gui-app/package.json
  </read_first>
  <files></files>
  <action>
Run the complete test suite to verify all 19 fixes work together and no regressions were introduced:

```bash
cd gui-app && npm test
```

Expected result: 0 failures, all 1361+ tests pass.

If any NEW failures appear that were not in the original 19:
1. Check if the RAF mock in setup.ts caused unintended side effects
2. Check if i18n.changeLanguage calls in one test leaked into another
3. Fix regressions before completing

The RAF mock should be safe because it only makes callbacks synchronous (faster, not different behavior). The i18n changes should be safe because each test file sets its own language in beforeEach.
  </action>
  <verify>
    <automated>cd gui-app && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits with code 0
    - Output shows "0 failed" or no failures
    - Test count is >= 1361 (no tests were deleted)
  </acceptance_criteria>
  <done>Full test suite passes with 0 failures. CI is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test assertions | Must match actual component behavior, not stale assumptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | T (Tampering) | test mocks | accept | Mocks are test-only, do not affect production code |
</threat_model>

<verification>
```bash
cd gui-app && npm test
```
Full suite passes with 0 failures.
</verification>

<success_criteria>
- All 19 previously failing tests pass
- `npm test` returns 0 failures
- No regressions in the remaining 1321+ passing tests
- STAB-01 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/08-stabilization/08-03-SUMMARY.md`
</output>
