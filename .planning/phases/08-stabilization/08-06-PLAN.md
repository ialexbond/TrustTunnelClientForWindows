---
phase: 08-stabilization
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - memory/v3/screens/connection.md
  - memory/v3/screens/dashboard.md
  - memory/v3/screens/routing.md
  - memory/v3/screens/settings.md
  - memory/v3/screens/about.md
  - memory/v3/screens/setup-wizard.md
autonomous: true
requirements: [STAB-12]
must_haves:
  truths:
    - "Screen spec exists for every Pro-version screen in memory/v3/screens/"
    - "Each spec follows the established format: overview, architecture, components, states, edge cases, shared/ui used"
    - "Specs reference actual file paths and component names from the codebase"
  artifacts:
    - path: "memory/v3/screens/connection.md"
      provides: "Screen spec for Connection tab (SettingsPanel)"
      contains: "SettingsPanel"
    - path: "memory/v3/screens/dashboard.md"
      provides: "Screen spec for Dashboard (StatusPanel + DashboardPanel)"
      contains: "StatusPanel"
    - path: "memory/v3/screens/routing.md"
      provides: "Screen spec for Routing tab"
      contains: "RoutingPanel"
    - path: "memory/v3/screens/settings.md"
      provides: "Screen spec for Settings tab (AppSettingsPanel)"
      contains: "AppSettingsPanel"
    - path: "memory/v3/screens/about.md"
      provides: "Screen spec for About tab"
      contains: "AboutPanel"
    - path: "memory/v3/screens/setup-wizard.md"
      provides: "Screen spec for Setup Wizard"
      contains: "SetupWizard"
  key_links:
    - from: "memory/v3/screens/"
      to: "gui-pro/src/components/"
      via: "File path references in specs"
      pattern: "gui-pro/src/components"
---

<objective>
Write screen specs for all remaining Pro-version screens.

Purpose: Close STAB-12 (documentation: screen specs for all Pro screens). The existing control-panel.md serves as the format template. Write specs for Connection, Dashboard, Routing, Settings, About, and Setup Wizard.
Output: 6 new screen spec files in memory/v3/screens/
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-stabilization/08-CONTEXT.md

<interfaces>
Existing screen spec format from memory/v3/screens/control-panel.md:
- Overview section
- Architecture diagram (ASCII)
- Components table (component, file, role)
- States (loading, empty, error, active)
- User flows (step-by-step interactions)
- Edge cases
- Shared/ui components used
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write screen specs for Connection, Dashboard, Routing</name>
  <read_first>
    memory/v3/screens/control-panel.md
    gui-pro/src/components/SettingsPanel.tsx
    gui-pro/src/components/DashboardPanel.tsx
    gui-pro/src/components/StatusPanel.tsx
    gui-pro/src/components/RoutingPanel.tsx
    gui-pro/src/App.tsx
  </read_first>
  <files>memory/v3/screens/connection.md, memory/v3/screens/dashboard.md, memory/v3/screens/routing.md</files>
  <action>
Per D-12 and D-13, write complete screen specs following the control-panel.md format.

**For each spec, include:**
1. Title and metadata (screen name, component file, status, last updated)
2. Overview — what the screen does, when it's shown
3. Architecture — ASCII diagram of component tree
4. Components table — component name, file path, role
5. States — what happens in loading/empty/error/active states
6. User flows — step-by-step interaction descriptions
7. Edge cases — what happens in unusual situations
8. Shared/ui components — which shared/ui components are used

**connection.md** — Connection tab (SettingsPanel.tsx when hasConfig=true, EmptyState when no config):
- Read SettingsPanel.tsx to document its sections (VPN mode, config path, reconnect, etc.)
- Document the StatusPanel integration (shown at top when connected)
- States: no-config (shows EmptyState), config-loaded, connected (with StatusPanel), error

**dashboard.md** — StatusPanel + DashboardPanel:
- StatusPanel shows VPN status badge, uptime counter, connect/disconnect button
- DashboardPanel shows connection metrics
- States: disconnected, connecting, connected, error, recovering

**routing.md** — RoutingPanel.tsx:
- Read RoutingPanel.tsx to document routing rules UI
- Sub-components: GeoIP, domain filters, process filters
- States: no-config, loading rules, active rules, editing rules

Read each component file to extract the actual component tree, props, and behavior. Do NOT invent features that don't exist.
  </action>
  <verify>
    <automated>ls memory/v3/screens/connection.md memory/v3/screens/dashboard.md memory/v3/screens/routing.md 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `memory/v3/screens/connection.md` exists and contains "SettingsPanel"
    - `memory/v3/screens/dashboard.md` exists and contains "StatusPanel" and "DashboardPanel"
    - `memory/v3/screens/routing.md` exists and contains "RoutingPanel"
    - Each file has: overview, architecture, components table, states, user flows, edge cases, shared/ui
    - File paths reference actual component paths from the codebase
  </acceptance_criteria>
  <done>Screen specs for Connection, Dashboard, Routing written in established format</done>
</task>

<task type="auto">
  <name>Task 2: Write screen specs for Settings, About, Setup Wizard</name>
  <read_first>
    memory/v3/screens/control-panel.md
    gui-pro/src/components/AppSettingsPanel.tsx
    gui-pro/src/components/AboutPanel.tsx
    gui-pro/src/components/SetupWizard.tsx
    gui-pro/src/components/ConfigPanel.tsx
  </read_first>
  <files>memory/v3/screens/settings.md, memory/v3/screens/about.md, memory/v3/screens/setup-wizard.md</files>
  <action>
Per D-12 and D-13, write complete screen specs following the control-panel.md format.

**settings.md** — Settings tab (AppSettingsPanel.tsx):
- App-level settings: theme, language, auto-connect, auto-start, kill switch
- Read AppSettingsPanel.tsx to extract actual settings sections
- Document ConfigPanel.tsx integration (config file path, log level)
- States: default, saving, error

**about.md** — About tab (AboutPanel.tsx):
- Version info, credits, links, update checker
- Read AboutPanel.tsx to extract actual content
- States: default, update-available

**setup-wizard.md** — Setup Wizard (SetupWizard.tsx):
- Multi-step wizard: welcome, server, found, endpoint, deploy, complete
- Read SetupWizard.tsx to extract wizard steps and flow
- Document FoundStep (complex: installed/not-installed/error states, user management)
- States per step: idle, loading, error, complete

Read each component file thoroughly. Document what EXISTS, not what should exist.
  </action>
  <verify>
    <automated>ls memory/v3/screens/settings.md memory/v3/screens/about.md memory/v3/screens/setup-wizard.md 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `memory/v3/screens/settings.md` exists and contains "AppSettingsPanel"
    - `memory/v3/screens/about.md` exists and contains "AboutPanel"
    - `memory/v3/screens/setup-wizard.md` exists and contains "SetupWizard"
    - Each file has: overview, architecture, components table, states, user flows, edge cases, shared/ui
    - File paths reference actual component paths from the codebase
  </acceptance_criteria>
  <done>Screen specs for Settings, About, Setup Wizard written. All Pro screens documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only plan, no code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | N/A | documentation | accept | No security impact |
</threat_model>

<verification>
```bash
ls memory/v3/screens/
```
Should list: control-panel.md, connection.md, dashboard.md, routing.md, settings.md, about.md, setup-wizard.md (7 total).
</verification>

<success_criteria>
- 6 new screen specs created in memory/v3/screens/
- Combined with existing control-panel.md, all 7 Pro screens are documented
- Each spec follows the established format with component trees, states, flows, and edge cases
- Specs reference actual codebase file paths
- STAB-12 fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/08-stabilization/08-06-SUMMARY.md`
</output>
