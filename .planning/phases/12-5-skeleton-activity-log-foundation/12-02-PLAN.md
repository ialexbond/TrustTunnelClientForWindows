---
phase: 12-5-skeleton-activity-log-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gui-pro/src/components/ServerTabs.tsx
  - gui-pro/src/components/ControlPanelPage.tsx
  - gui-pro/src/shared/i18n/locales/ru.json
  - gui-pro/src/shared/i18n/locales/en.json
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-14

must_haves:
  truths:
    - "ServerTabs отображает 5 табов: overview, users, configuration, security, utilities"
    - "Хедер с IP и кнопкой Отключиться удален полностью"
    - "Иконка LogOut справа от табов с border-l визуально отделена"
    - "Нажатие LogOut открывает ConfirmDialog variant=danger"
    - "Подтверждение disconnect вызывает state.onDisconnect()"
    - "Cross-fade работает для всех 5 панелей"
    - "ServerPanelSkeleton не содержит хедер skeleton"
  artifacts:
    - path: "gui-pro/src/components/ServerTabs.tsx"
      provides: "5-tab server panel with disconnect icon"
      contains: "type TabId"
    - path: "gui-pro/src/components/ControlPanelPage.tsx"
      provides: "Updated ServerPanelSkeleton without header"
      contains: "ServerPanelSkeleton"
  key_links:
    - from: "gui-pro/src/components/ServerTabs.tsx"
      to: "gui-pro/src/shared/ui/ConfirmDialog.tsx"
      via: "ConfirmDialog open={showDisconnectConfirm}"
      pattern: "ConfirmDialog.*showDisconnectConfirm"
    - from: "gui-pro/src/components/ServerTabs.tsx"
      to: "gui-pro/src/components/server/useServerState.ts"
      via: "state.onDisconnect()"
      pattern: "state\\.onDisconnect"
---

<objective>
Переписать ServerTabs.tsx: расширить с 4 до 5 табов (per D-02), удалить хедер с IP (per D-03), добавить иконку disconnect LogOut с ConfirmDialog (per D-04, D-05, D-06). Обновить ServerPanelSkeleton в ControlPanelPage.tsx.

Purpose: Структурная перестройка серверной панели управления — фундамент для миграции контента в Plan 03.
Output: Работающий ServerTabs с 5 табами, хедер удален, disconnect через иконку LogOut + подтверждение.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-5-skeleton-activity-log-foundation/12-CONTEXT.md
@.planning/phases/12-5-skeleton-activity-log-foundation/12-RESEARCH.md
@.planning/phases/12-5-skeleton-activity-log-foundation/12-PATTERNS.md
@.planning/phases/12-5-skeleton-activity-log-foundation/12-UI-SPEC.md

<interfaces>
<!-- ServerTabs current structure -->
From gui-pro/src/components/ServerTabs.tsx:
```typescript
type TabId = "overview" | "users" | "settings" | "service";
interface Tab { id: TabId; labelKey: string; fallback: string; icon: React.ReactNode; }
interface ServerTabsProps { state: ServerState; }
```

From gui-pro/src/components/server/useServerState.ts:
```typescript
// state.onDisconnect is already available via ServerState
// state.host, state.port - used for header (will be removed)
```

From gui-pro/src/shared/ui/ConfirmDialog.tsx:
```typescript
interface ConfirmDialogProps {
  open: boolean;
  variant?: "danger" | "warning";
  title: string;
  message: string;
  confirmLabel: string;
  cancelLabel: string;
  onConfirm: () => void;
  onCancel: () => void;
  loading?: boolean;
}
```

From gui-pro/src/components/ControlPanelPage.tsx (lines 50-79):
```typescript
function ServerPanelSkeleton() {
  // Header skeleton (lines 53-59) - TO BE REMOVED
  // Tab bar skeleton (lines 61-69) - 5 pills already
  // Content skeleton (lines 70-78) - keep
}
```

From gui-pro/src/shared/i18n/locales/ru.json (existing tab keys):
```json
"tabs": {
  "overview": "Обзор",
  "users": "Пользователи",
  "settings_server": "Настройки",   // deprecated after Phase 12
  "service": "Сервис",              // deprecated after Phase 12
  "security": "Безопасность",       // exists!
  "config": "Конфигурация"          // exists but key is tabs.config, not tabs.configuration
}
"control": {
  "disconnect": "Сменить сервер"    // update to "Отключиться от сервера"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n keys для табов и disconnect</name>
  <files>
    gui-pro/src/shared/i18n/locales/ru.json,
    gui-pro/src/shared/i18n/locales/en.json
  </files>
  <read_first>
    gui-pro/src/shared/i18n/locales/ru.json,
    gui-pro/src/shared/i18n/locales/en.json
  </read_first>
  <action>
В `ru.json`:

1. В секцию `"tabs"` (после строки `"service": "Сервис"`, строка 29) добавить:
   ```json
   "configuration": "Конфигурация",
   "utilities": "Утилиты"
   ```
   НЕ удалять существующие `"settings_server"` и `"service"` — оставить для backward compat.

2. В секцию `"control"` (строка 995) изменить значение `"disconnect"`:
   ```json
   "disconnect": "Отключиться от сервера"
   ```
   Было: `"Сменить сервер"`. Хедер где это использовалось удаляется, ключ переиспользуется для aria-label иконки disconnect.

3. В секцию `"server"` добавить вложенные объекты:
   ```json
   "disconnect": {
     "confirm_title": "Отключиться от сервера?",
     "confirm_message": "Сервер продолжит работать. Вы сможете подключиться снова в любое время."
   },
   "config": {
     "port_title": "Порт SSH"
   }
   ```

4. В секцию `"errors"` (после `"retry": "Попробовать снова"`, строка 236) добавить:
   ```json
   "download_logs": "Скачать логи",
   "go_home": "На главную"
   ```
   Ключ `"go_home"` уже используется в PanelErrorBoundary с defaultValue — теперь будет proper key.

В `en.json` — зеркальные изменения:

1. В `"tabs"`:
   ```json
   "configuration": "Configuration",
   "utilities": "Utilities"
   ```

2. В `"control"`:
   ```json
   "disconnect": "Disconnect from server"
   ```

3. В `"server"`:
   ```json
   "disconnect": {
     "confirm_title": "Disconnect from server?",
     "confirm_message": "The server will keep running. You can reconnect at any time."
   },
   "config": {
     "port_title": "SSH Port"
   }
   ```

4. В `"errors"`:
   ```json
   "download_logs": "Download logs",
   "go_home": "Go home"
   ```
  </action>
  <verify>
    <automated>cd gui-pro && node -e "const ru=require('./src/shared/i18n/locales/ru.json'); const en=require('./src/shared/i18n/locales/en.json'); const checks=['tabs.configuration','tabs.utilities','control.disconnect','server.disconnect.confirm_title','server.disconnect.confirm_message','server.config.port_title','errors.download_logs','errors.go_home']; let ok=true; for(const k of checks){const parts=k.split('.'); let r=ru,e=en; for(const p of parts){r=r?.[p]; e=e?.[p];} if(!r||!e){console.log('MISSING:',k,'ru=',r,'en=',e); ok=false;}} if(ok)console.log('ALL KEYS OK'); else process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - `ru.json` содержит ключ `tabs.configuration` со значением `"Конфигурация"`
    - `ru.json` содержит ключ `tabs.utilities` со значением `"Утилиты"`
    - `ru.json` содержит `control.disconnect` со значением `"Отключиться от сервера"` (не `"Сменить сервер"`)
    - `ru.json` содержит `server.disconnect.confirm_title` со значением `"Отключиться от сервера?"`
    - `ru.json` содержит `server.disconnect.confirm_message` со значением содержащим `"Сервер продолжит работать"`
    - `ru.json` содержит `server.config.port_title` со значением `"Порт SSH"`
    - `ru.json` содержит `errors.download_logs` со значением `"Скачать логи"`
    - `ru.json` содержит `errors.go_home` со значением `"На главную"`
    - `en.json` содержит зеркальные ключи на английском
    - Старые ключи `tabs.settings_server` и `tabs.service` НЕ удалены
  </acceptance_criteria>
  <done>Все 8 новых/обновленных i18n ключей добавлены в оба языковых файла. Deprecated ключи сохранены.</done>
</task>

<task type="auto">
  <name>Task 2: ServerTabs 4->5 + header removal + disconnect icon + ControlPanelPage skeleton</name>
  <files>
    gui-pro/src/components/ServerTabs.tsx,
    gui-pro/src/components/ControlPanelPage.tsx
  </files>
  <read_first>
    gui-pro/src/components/ServerTabs.tsx,
    gui-pro/src/components/ControlPanelPage.tsx,
    gui-pro/src/shared/ui/ConfirmDialog.tsx,
    gui-pro/src/components/server/ServiceSection.tsx,
    gui-pro/src/components/server/ServerSettingsSection.tsx
  </read_first>
  <action>
**ServerTabs.tsx — полное переписывание:**

1. Обновить импорты:
   - Добавить `Shield, Wrench` из `lucide-react` (заменить `Terminal` на `Wrench`)
   - Добавить `import { ConfirmDialog } from "../shared/ui/ConfirmDialog";`
   - Заменить `import { ServiceSection }` на `import { SecurityTabSection }` из `"./server/SecurityTabSection"` и `import { UtilitiesTabSection }` из `"./server/UtilitiesTabSection"`
   - ВРЕМЕННО: оставить `import { ServiceSection }` пока Plan 03 не создаст SecurityTabSection/UtilitiesTabSection. Для компиляции в Wave 1 — использовать ServiceSection как placeholder для `security` и `utilities` табов (пустые div или ServiceSection). Plan 03 заменит.

   Итоговые Lucide-импорты:
   ```tsx
   import { LayoutDashboard, Users, SlidersHorizontal, Shield, Wrench, LogOut } from "lucide-react";
   ```

2. Обновить TabId type:
   ```typescript
   type TabId = "overview" | "users" | "configuration" | "security" | "utilities";
   ```

3. Обновить массив tabs:
   ```tsx
   const tabs: Tab[] = [
     { id: "overview",      labelKey: "tabs.overview",       fallback: "Обзор",         icon: <LayoutDashboard className="w-4 h-4" /> },
     { id: "users",         labelKey: "tabs.users",          fallback: "Пользователи",  icon: <Users className="w-4 h-4" /> },
     { id: "configuration", labelKey: "tabs.configuration",  fallback: "Конфигурация",  icon: <SlidersHorizontal className="w-4 h-4" /> },
     { id: "security",      labelKey: "tabs.security",       fallback: "Безопасность",  icon: <Shield className="w-4 h-4" /> },
     { id: "utilities",     labelKey: "tabs.utilities",      fallback: "Утилиты",       icon: <Wrench className="w-4 h-4" /> },
   ];
   ```

4. Добавить state для disconnect dialog:
   ```tsx
   const [showDisconnectConfirm, setShowDisconnectConfirm] = useState(false);
   ```

5. УДАЛИТЬ полностью блок "Server header" (строки 43-60) — весь `<div>` с `state.host` и кнопкой disconnect.

6. В блоке "Tab bar" (строки 62-83), ПОСЛЕ закрывающего `)}` от `tabs.map` добавить disconnect icon:
   ```tsx
   {/* Disconnect icon - visually separated from tabs */}
   <button
     type="button"
     onClick={() => setShowDisconnectConfirm(true)}
     aria-label={t("control.disconnect")}
     title={t("control.disconnect")}
     className={cn(
       "shrink-0 flex items-center justify-center",
       "h-9 w-9 rounded-[var(--radius-md)]",
       "text-[var(--color-text-muted)]",
       "hover:text-[var(--color-text-primary)] hover:bg-[var(--color-bg-hover)]",
       "transition-colors",
       "focus-visible:shadow-[var(--focus-ring)] outline-none",
       "border-l border-[var(--color-border)] ml-2 pl-2"
     )}
   >
     <LogOut className="w-4 h-4" />
   </button>
   ```

7. Обновить cross-fade панели — 5 штук вместо 4:
   - Панель `"overview"` -> `<OverviewSection state={state} />` (без изменений)
   - Панель `"users"` -> `<UsersSection state={state} />` (без изменений)
   - Панель `"configuration"` -> `<ServerSettingsSection state={state} />` (было `"settings"`)
   - Панель `"security"` -> placeholder `<div className="text-sm text-[var(--color-text-muted)]">Security tab (Plan 03)</div>` ВРЕМЕННО — будет заменено на `<SecurityTabSection state={state} />` в Plan 03
   - Панель `"utilities"` -> placeholder `<div className="text-sm text-[var(--color-text-muted)]">Utilities tab (Plan 03)</div>` ВРЕМЕННО — будет заменено на `<UtilitiesTabSection state={state} />` в Plan 03

   Каждая панель использует тот же cross-fade паттерн (visibility+opacity):
   ```tsx
   <div
     className="h-full flex flex-col overflow-hidden scroll-overlay py-4 px-6 space-y-4"
     style={{
       position: activeTab === "TABID" ? "relative" : "absolute",
       inset: activeTab === "TABID" ? undefined : 0,
       opacity: activeTab === "TABID" ? 1 : 0,
       visibility: activeTab === "TABID" ? ("visible" as const) : ("hidden" as const),
       transition: "opacity var(--transition-fast)",
     }}
     aria-hidden={activeTab !== "TABID"}
   >
     <ComponentHere state={state} />
   </div>
   ```

8. После блока cross-fade (перед закрывающим `</div>`) добавить ConfirmDialog:
   ```tsx
   <ConfirmDialog
     open={showDisconnectConfirm}
     variant="danger"
     title={t("server.disconnect.confirm_title")}
     message={t("server.disconnect.confirm_message")}
     confirmLabel={t("buttons.confirm")}
     cancelLabel={t("buttons.cancel")}
     onConfirm={() => {
       setShowDisconnectConfirm(false);
       state.onDisconnect();
     }}
     onCancel={() => setShowDisconnectConfirm(false)}
   />
   ```

**ControlPanelPage.tsx — обновить ServerPanelSkeleton:**

1. УДАЛИТЬ блок "Header skeleton" (строки 53-59):
   ```tsx
   // DELETE: Header skeleton - address bar + disconnect button
   <div className="flex items-center justify-between px-4 py-2 shrink-0" ...>
     <Skeleton variant="line" width="55%" height={14} />
     <Skeleton variant="line" width="18%" height={14} />
   </div>
   ```

2. В блоке "Tab bar skeleton" добавить placeholder для disconnect icon (после 5 pills):
   ```tsx
   <Skeleton variant="card" width={36} height={32} className="ml-2" />
   ```

Итоговая ServerPanelSkeleton:
```tsx
function ServerPanelSkeleton() {
  return (
    <div className="flex-1 flex flex-col overflow-hidden">
      {/* Tab bar skeleton - 5 tab pills + disconnect icon placeholder */}
      <div
        className="flex items-center shrink-0 gap-1 px-2 py-1"
        style={{ borderBottom: "1px solid var(--color-border)" }}
      >
        {[...Array(5)].map((_, i) => (
          <Skeleton key={i} variant="card" className="flex-1" height={32} />
        ))}
        <Skeleton variant="card" width={36} height={32} className="ml-2" />
      </div>
      {/* Content area skeleton */}
      <div className="flex-1 px-6 py-4 space-y-4">
        <Skeleton variant="card" height={80} />
        <Skeleton variant="line" width="40%" height={14} />
        <Skeleton variant="line" width="70%" height={14} />
        <Skeleton variant="line" width="55%" height={14} />
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd gui-pro && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `ServerTabs.tsx` содержит `type TabId = "overview" | "users" | "configuration" | "security" | "utilities"`
    - `ServerTabs.tsx` НЕ содержит `state.host` и `state.port` (хедер удален)
    - `ServerTabs.tsx` содержит `<LogOut className="w-4 h-4"` (disconnect icon)
    - `ServerTabs.tsx` содержит `aria-label={t("control.disconnect")}` на кнопке disconnect
    - `ServerTabs.tsx` содержит `<ConfirmDialog` с `open={showDisconnectConfirm}` и `variant="danger"`
    - `ServerTabs.tsx` содержит `state.onDisconnect()` в onConfirm callback
    - `ServerTabs.tsx` содержит 5 cross-fade панелей с `aria-hidden` для каждого TabId
    - `ControlPanelPage.tsx` НЕ содержит `width="55%"` (header skeleton удален)
    - `ControlPanelPage.tsx` содержит `width={36}` skeleton для disconnect icon placeholder
    - `npm run typecheck` проходит без ошибок
  </acceptance_criteria>
  <done>ServerTabs переписан с 5 табами, хедер удален, disconnect icon с ConfirmDialog добавлен. ServerPanelSkeleton обновлен. Приложение компилируется. Табы security и utilities содержат placeholder — Plan 03 заменит на реальные компоненты.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI state | showDisconnectConfirm - локальный state, no security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Spoofing | Disconnect ConfirmDialog | accept | Локальное UI действие — пользователь подтверждает свой собственный disconnect. ConfirmDialog имеет focus trap (existing Modal). |
</threat_model>

<verification>
- `npm run typecheck` проходит
- ServerTabs отображает 5 табов в правильном порядке
- Хедер с IP не рендерится
- LogOut иконка имеет aria-label и title
- ConfirmDialog появляется при клике на LogOut
- Cross-fade работает для 5 панелей
</verification>

<success_criteria>
1. ServerTabs.tsx содержит TabId с 5 значениями
2. Хедер полностью удален (D-03)
3. Иконка LogOut справа от табов с border-l разделителем (D-04, D-06)
4. ConfirmDialog variant="danger" при нажатии на LogOut (D-05)
5. Cross-fade для 5 панелей с visibility+opacity (D-01)
6. ServerPanelSkeleton без хедера, с placeholder для disconnect icon
7. TypeScript компилируется без ошибок
</success_criteria>

<output>
After completion, create `.planning/phases/12-5-skeleton-activity-log-foundation/12-02-SUMMARY.md`
</output>
