---
phase: 12-5-skeleton-activity-log-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 12-02
files_modified:
  - gui-app/src/components/server/SecurityTabSection.tsx
  - gui-app/src/components/server/UtilitiesTabSection.tsx
  - gui-app/src/components/server/ServerSettingsSection.tsx
  - gui-app/src/components/ServerTabs.tsx
autonomous: true
requirements:
  - D-01
  - D-02

must_haves:
  truths:
    - "SecurityTabSection рендерит SecuritySection + CertSection"
    - "UtilitiesTabSection рендерит Service Controls + BBR + MTProto + LogsSection + DangerZone Accordion"
    - "ServerSettingsSection не содержит BBR toggle и MTProto"
    - "ServerTabs импортирует SecurityTabSection и UtilitiesTabSection вместо placeholder"
    - "ServiceSection.tsx не импортируется ни из одного файла"
  artifacts:
    - path: "gui-app/src/components/server/SecurityTabSection.tsx"
      provides: "Wrapper для таба Безопасность"
      exports: ["SecurityTabSection"]
    - path: "gui-app/src/components/server/UtilitiesTabSection.tsx"
      provides: "Wrapper для таба Утилиты"
      exports: ["UtilitiesTabSection"]
    - path: "gui-app/src/components/server/ServerSettingsSection.tsx"
      provides: "Cleaned up Конфигурация tab content"
      exports: ["ServerSettingsSection"]
  key_links:
    - from: "gui-app/src/components/ServerTabs.tsx"
      to: "gui-app/src/components/server/SecurityTabSection.tsx"
      via: "import + render in security cross-fade panel"
      pattern: "SecurityTabSection"
    - from: "gui-app/src/components/ServerTabs.tsx"
      to: "gui-app/src/components/server/UtilitiesTabSection.tsx"
      via: "import + render in utilities cross-fade panel"
      pattern: "UtilitiesTabSection"
    - from: "gui-app/src/components/server/UtilitiesTabSection.tsx"
      to: "gui-app/src/components/server/useBbrState.ts"
      via: "const bbr = useBbrState(sshParams, state.pushSuccess)"
      pattern: "useBbrState"
---

<objective>
Создать SecurityTabSection и UtilitiesTabSection, очистить ServerSettingsSection от BBR/MTProto, подключить новые секции в ServerTabs. Полная миграция контента per D-01 — все секции функциональны.

Purpose: Завершить реструктуризацию 4->5 табов — каждая функция на своем месте (per D-02), нет дублирования.
Output: SecurityTabSection.tsx, UtilitiesTabSection.tsx созданы; ServerSettingsSection очищен; ServerTabs подключает реальные компоненты.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-5-skeleton-activity-log-foundation/12-CONTEXT.md
@.planning/phases/12-5-skeleton-activity-log-foundation/12-RESEARCH.md
@.planning/phases/12-5-skeleton-activity-log-foundation/12-PATTERNS.md
@.planning/phases/12-5-skeleton-activity-log-foundation/12-UI-SPEC.md
@.planning/phases/12-5-skeleton-activity-log-foundation/12-02-SUMMARY.md

<interfaces>
<!-- ServiceSection.tsx - exact analog for UtilitiesTabSection -->
From gui-app/src/components/server/ServiceSection.tsx:
```typescript
import { useState } from "react";
import { useTranslation } from "react-i18next";
import { invoke } from "@tauri-apps/api/core";
import { RefreshCw, PowerOff, Power, AlertTriangle } from "lucide-react";
import { Card, CardHeader } from "../../shared/ui/Card";
import { Button } from "../../shared/ui/Button";
import { ConfirmDialog } from "../../shared/ui/ConfirmDialog";
import { Accordion } from "../../shared/ui/Accordion";
import type { ServerState } from "./useServerState";
import { SecuritySection } from "./SecuritySection";
import { LogsSection } from "./LogsSection";
import { DangerZoneSection } from "./DangerZoneSection";

interface Props { state: ServerState; }
export function ServiceSection({ state }: Props) {
  const { t } = useTranslation();
  const { serverInfo, actionLoading, sshParams, runAction } = state;
  const [confirmStop, setConfirmStop] = useState(false);
  // Block 1: Service Controls (Card with restart/stop/start buttons + ConfirmDialog for stop)
  // Block 2: SecuritySection in aria-live="polite"
  // Block 3: LogsSection
  // Block 4: DangerZone Accordion
}
```

From gui-app/src/components/server/ServerSettingsSection.tsx:
```typescript
// Imports to REMOVE: useBbrState, useMtProtoState, MtProtoSection, Zap
// Sub-hooks to REMOVE: const bbr = useBbrState(...), const mtproto = useMtProtoState(...)
// Block 2 Network card: REMOVE BBR Toggle, keep SshPortSection only, retitle to "Порт SSH"
// Block 3 Advanced: REMOVE MtProtoSection + its ConfirmDialog
// KEEP: useSecurityState for SshPortSection, Feature Toggles, Save CTA
```

From gui-app/src/components/server/useBbrState.ts:
```typescript
export function useBbrState(sshParams: SshParams, pushSuccess: (msg: string) => void);
// Returns: { enabled, loading, toggle }
```

From gui-app/src/components/server/useMtProtoState.ts:
```typescript
export function useMtProtoState(sshParams: SshParams, pushSuccess: (msg: string) => void);
// Returns: { status, confirm, setConfirm, ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SecurityTabSection + UtilitiesTabSection</name>
  <files>
    gui-app/src/components/server/SecurityTabSection.tsx,
    gui-app/src/components/server/UtilitiesTabSection.tsx
  </files>
  <read_first>
    gui-app/src/components/server/ServiceSection.tsx,
    gui-app/src/components/server/SecuritySection.tsx,
    gui-app/src/components/server/CertSection.tsx,
    gui-app/src/components/server/useBbrState.ts,
    gui-app/src/components/server/useMtProtoState.ts,
    gui-app/src/components/server/MtProtoSection.tsx,
    gui-app/src/components/server/LogsSection.tsx,
    gui-app/src/components/server/DangerZoneSection.tsx,
    gui-app/src/components/server/ServerSettingsSection.tsx,
    gui-app/src/components/server/useServerState.ts
  </read_first>
  <action>
**SecurityTabSection.tsx — CREATE:**

Тонкий wrapper без собственного state (аналог ServiceSection по роли). Содержимое: SecuritySection + CertSection.

```tsx
import type { ServerState } from "./useServerState";
import { SecuritySection } from "./SecuritySection";
import { CertSection } from "./CertSection";

interface Props {
  state: ServerState;
}

export function SecurityTabSection({ state }: Props) {
  return (
    <div className="space-y-4">
      {/* aria-live: SecuritySection делает SSH-запросы, результат объявляется screen reader */}
      <div aria-live="polite">
        <SecuritySection state={state} />
      </div>
      <CertSection state={state} />
    </div>
  );
}
```

**UtilitiesTabSection.tsx — CREATE:**

Содержимое (порядок per CONTEXT.md D-02 и UI-SPEC):
1. Service Controls (Card с кнопками Restart/Stop/Start) — скопировать из ServiceSection Block 1 (строки 27-102)
2. BBR Toggle Card — перенести из ServerSettingsSection Block 2 Network card
3. MTProto section (условный рендер if mtproto.status) — перенести из ServerSettingsSection Advanced Accordion
4. LogsSection — перенести из ServiceSection Block 3
5. DangerZone Accordion — скопировать из ServiceSection Block 4

Структура:
```tsx
import { useState } from "react";
import { useTranslation } from "react-i18next";
import { invoke } from "@tauri-apps/api/core";
import { RefreshCw, PowerOff, Power, AlertTriangle, Zap } from "lucide-react";
import { Card, CardHeader } from "../../shared/ui/Card";
import { Button } from "../../shared/ui/Button";
import { Toggle } from "../../shared/ui/Toggle";
import { ConfirmDialog } from "../../shared/ui/ConfirmDialog";
import { Accordion } from "../../shared/ui/Accordion";
import type { ServerState } from "./useServerState";
import { useBbrState } from "./useBbrState";
import { useMtProtoState } from "./useMtProtoState";
import { MtProtoSection } from "./MtProtoSection";
import { LogsSection } from "./LogsSection";
import { DangerZoneSection } from "./DangerZoneSection";

interface Props {
  state: ServerState;
}

export function UtilitiesTabSection({ state }: Props) {
  const { t } = useTranslation();
  const { serverInfo, actionLoading, sshParams, runAction, pushSuccess } = state;

  const bbr = useBbrState(sshParams, pushSuccess);
  const mtproto = useMtProtoState(sshParams, pushSuccess);
  const [confirmStop, setConfirmStop] = useState(false);

  return (
    <div className="space-y-4">
      {/* Block 1: Service Controls — скопировать из ServiceSection */}
      <Card>
        <CardHeader
          title={t("server.service.controls_title")}
          icon={<RefreshCw className="w-3.5 h-3.5" />}
        />
        <div className="flex flex-wrap gap-2">
          <Button variant="ghost" size="sm" icon={<RefreshCw className="w-3.5 h-3.5" />}
            loading={actionLoading === "restart"}
            onClick={() => runAction("restart", () => invoke("server_restart_service", sshParams), t("server.actions.success_restart"))}
          >{t("server.actions.restart")}</Button>

          {serverInfo?.serviceActive && (
            <Button variant="danger-outline" size="sm" icon={<PowerOff className="w-3.5 h-3.5" />}
              loading={actionLoading === "stop"}
              onClick={() => setConfirmStop(true)}
            >{t("server.actions.stop")}</Button>
          )}

          {serverInfo && !serverInfo.serviceActive && (
            <Button variant="primary" size="sm" icon={<Power className="w-3.5 h-3.5" />}
              loading={actionLoading === "start"}
              onClick={() => runAction("start", () => invoke("server_start_service", sshParams), t("server.actions.success_start"))}
            >{t("server.actions.start")}</Button>
          )}
        </div>

        <ConfirmDialog
          open={confirmStop}
          title={t("server.danger.stop_title")}
          message={t("server.danger.stop_message")}
          confirmLabel={t("buttons.confirm")}
          cancelLabel={t("buttons.cancel")}
          variant="danger"
          loading={actionLoading === "stop"}
          onCancel={() => setConfirmStop(false)}
          onConfirm={() => {
            setConfirmStop(false);
            runAction("stop", () => invoke("server_stop_service", sshParams), t("server.actions.success_stop"));
          }}
        />
      </Card>

      {/* Block 2: BBR Toggle — перенесено из ServerSettingsSection */}
      <Card>
        <CardHeader
          title={t("server.settings.bbr_toggle")}
          icon={<Zap className="w-3.5 h-3.5" />}
        />
        <Toggle
          value={bbr.enabled}
          onChange={() => void bbr.toggle()}
          label={t("server.settings.bbr_toggle")}
          description={bbr.loading ? t("server.settings.bbr_detecting") : t("server.settings.bbr_description")}
          disabled={bbr.loading}
        />
      </Card>

      {/* Block 3: MTProto — перенесено из ServerSettingsSection Advanced Accordion */}
      {mtproto.status && <MtProtoSection state={mtproto} />}
      <ConfirmDialog
        open={!!mtproto.confirm}
        title={mtproto.confirm?.title ?? ""}
        message={mtproto.confirm?.message ?? ""}
        confirmLabel={t("server.settings.mtproto_uninstall")}
        cancelLabel={t("buttons.cancel")}
        variant="warning"
        onCancel={() => mtproto.setConfirm(null)}
        onConfirm={() => mtproto.confirm?.onConfirm()}
      />

      {/* Block 4: Logs — перенесено из ServiceSection */}
      <LogsSection state={state} />

      {/* Block 5: DangerZone Accordion — перенесено из ServiceSection */}
      <Accordion
        defaultOpen={[]}
        items={[
          {
            id: "danger-zone",
            title: (
              <span
                className="flex items-center gap-2 text-sm font-[var(--font-weight-semibold)]"
                style={{ color: "var(--color-danger-500)" }}
              >
                <AlertTriangle className="w-3.5 h-3.5" />
                {t("server.danger.title")}
              </span>
            ),
            content: <DangerZoneSection state={state} />,
          },
        ]}
      />
    </div>
  );
}
```

ВАЖНО при копировании из ServiceSection и ServerSettingsSection:
- Прочитать ТОЧНЫЕ i18n-ключи для BBR toggle (key `server.settings.bbr_toggle`, `server.settings.bbr_detecting`, `server.settings.bbr_description`) — проверить в исходном файле, ключи могут отличаться
- Прочитать ТОЧНЫЕ i18n-ключи для MTProto uninstall confirm
- Убедиться что `pushSuccess` доступен из `state` — проверить в useServerState
- Убедиться что Toggle принимает те же props что в ServerSettingsSection
  </action>
  <verify>
    <automated>cd gui-app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `gui-app/src/components/server/SecurityTabSection.tsx` существует и экспортирует `SecurityTabSection`
    - SecurityTabSection содержит `<SecuritySection state={state} />` и `<CertSection state={state} />`
    - SecurityTabSection содержит `aria-live="polite"` wrapper вокруг SecuritySection
    - `gui-app/src/components/server/UtilitiesTabSection.tsx` существует и экспортирует `UtilitiesTabSection`
    - UtilitiesTabSection содержит `useBbrState` и `useMtProtoState` hook calls
    - UtilitiesTabSection содержит Service Controls (restart/stop/start buttons)
    - UtilitiesTabSection содержит `<LogsSection state={state} />`
    - UtilitiesTabSection содержит `<DangerZoneSection` внутри Accordion
    - TypeScript компилируется без ошибок
  </acceptance_criteria>
  <done>SecurityTabSection и UtilitiesTabSection созданы с полным содержимым. Все секции функциональны — полная миграция контента per D-01.</done>
</task>

<task type="auto">
  <name>Task 2: ServerSettingsSection cleanup + ServerTabs wire-up</name>
  <files>
    gui-app/src/components/server/ServerSettingsSection.tsx,
    gui-app/src/components/ServerTabs.tsx
  </files>
  <read_first>
    gui-app/src/components/server/ServerSettingsSection.tsx,
    gui-app/src/components/ServerTabs.tsx,
    gui-app/src/components/server/SecurityTabSection.tsx,
    gui-app/src/components/server/UtilitiesTabSection.tsx
  </read_first>
  <action>
**ServerSettingsSection.tsx — cleanup:**

1. УДАЛИТЬ импорты:
   - `useBbrState` из `"./useBbrState"`
   - `useMtProtoState` из `"./useMtProtoState"`
   - `MtProtoSection` из `"./MtProtoSection"`
   - `Zap` из `lucide-react`
   - Любые неиспользуемые импорты после удаления

2. УДАЛИТЬ hook-вызовы:
   - `const bbr = useBbrState(sshParams, state.pushSuccess);` (или как он написан)
   - `const mtproto = useMtProtoState(sshParams, state.pushSuccess);` (или как он написан)

3. В Block 2 (Network card):
   - УДАЛИТЬ BBR Toggle блок (Toggle компонент с bbr.enabled)
   - ОСТАВИТЬ SshPortSection
   - Заменить CardHeader title на `{t("server.config.port_title")}` (i18n ключ `server.config.port_title` = "Порт SSH")
   - Обновить icon если нужно (Network icon может остаться)

4. В Block 3 (Advanced Accordion):
   - УДАЛИТЬ `{mtproto.status && <MtProtoSection state={mtproto} />}` внутри content
   - УДАЛИТЬ ConfirmDialog для mtproto: весь блок `<ConfirmDialog open={!!mtproto.confirm} .../>` — он переехал в UtilitiesTabSection

5. ConfirmDialog для security (SshPortSection) — ОСТАВИТЬ. Он нужен для SshPortSection.

6. ОСТАВИТЬ: `useSecurityState` hook — нужен для SshPortSection. Это нормально что 2 экземпляра (здесь и в SecuritySection) — каждый делает свой SSH-запрос.

**ServerTabs.tsx — wire-up новых компонентов:**

1. Обновить импорты:
   - УБРАТЬ `import { ServiceSection }` если есть
   - ДОБАВИТЬ:
     ```tsx
     import { SecurityTabSection } from "./server/SecurityTabSection";
     import { UtilitiesTabSection } from "./server/UtilitiesTabSection";
     ```

2. Заменить placeholder content в cross-fade панелях:
   - Панель `"security"`: заменить placeholder div на `<SecurityTabSection state={state} />`
   - Панель `"utilities"`: заменить placeholder div на `<UtilitiesTabSection state={state} />`

3. ВАЖНО: ServiceSection.tsx НЕ удалять физически — он может использоваться тестами или Storybook stories. Просто убедиться что ServerTabs его НЕ импортирует.
  </action>
  <verify>
    <automated>cd gui-app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `ServerSettingsSection.tsx` НЕ содержит `useBbrState` и `useMtProtoState`
    - `ServerSettingsSection.tsx` НЕ содержит `MtProtoSection`
    - `ServerSettingsSection.tsx` содержит `t("server.config.port_title")` для Network card
    - `ServerSettingsSection.tsx` СОДЕРЖИТ `useSecurityState` (для SshPortSection)
    - `ServerTabs.tsx` содержит `import { SecurityTabSection }` и `import { UtilitiesTabSection }`
    - `ServerTabs.tsx` НЕ содержит `import { ServiceSection }`
    - `ServerTabs.tsx` содержит `<SecurityTabSection state={state} />` в панели security
    - `ServerTabs.tsx` содержит `<UtilitiesTabSection state={state} />` в панели utilities
    - `npm run typecheck` проходит без ошибок
  </acceptance_criteria>
  <done>ServerSettingsSection очищен от BBR/MTProto. ServerTabs подключает SecurityTabSection и UtilitiesTabSection. Полная миграция контента завершена — все 5 табов функциональны.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component data flow | SecurityTabSection и UtilitiesTabSection получают данные через state: ServerState (existing trust boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Elevation of Privilege | UtilitiesTabSection (restart/stop/start) | mitigate | ConfirmDialog для stop service перенесен из ServiceSection as-is. Elevation guard T-11-03 сохранен. |
| T-12-07 | Tampering | DangerZone (reboot/uninstall) | mitigate | DangerZone остается в Accordion (closed by default) с ConfirmDialog для каждого действия — перенесено из ServiceSection as-is. |
</threat_model>

<verification>
- `npm run typecheck` проходит
- ServerTabs рендерит 5 табов с реальными компонентами
- SecurityTabSection содержит SecuritySection + CertSection
- UtilitiesTabSection содержит Service Controls + BBR + MTProto + Logs + DangerZone
- ServerSettingsSection не содержит BBR/MTProto
</verification>

<success_criteria>
1. SecurityTabSection.tsx создан — wrapper для SecuritySection + CertSection с aria-live
2. UtilitiesTabSection.tsx создан — Service Controls + BBR + MTProto + Logs + DangerZone
3. ServerSettingsSection.tsx очищен — нет useBbrState, useMtProtoState, MtProtoSection
4. ServerTabs.tsx импортирует и рендерит SecurityTabSection и UtilitiesTabSection
5. Все 5 табов полностью функциональны (per D-01 — полная миграция, не skeleton-заглушки)
6. TypeScript компилируется без ошибок
</success_criteria>

<output>
After completion, create `.planning/phases/12-5-skeleton-activity-log-foundation/12-03-SUMMARY.md`
</output>
