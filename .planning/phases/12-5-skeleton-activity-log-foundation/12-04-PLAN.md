---
phase: 12-5-skeleton-activity-log-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 12-01
  - 12-02
files_modified:
  - gui-app/src/shared/ui/PanelErrorBoundary.tsx
  - gui-app/src/App.tsx
  - gui-app/src/components/ServerTabs.tsx
autonomous: true
requirements:
  - D-10
  - D-11
  - D-12

must_haves:
  truths:
    - "PanelErrorBoundary показывает 3 кнопки: Попробовать снова, Скачать логи, На главную"
    - "Кнопка Скачать логи вызывает open_logs_folder"
    - "При старте приложения Activity Log записывает app.start version=X.Y.Z"
    - "При переключении таба Activity Log записывает tab.switch target=tabId"
    - "При подтверждении disconnect Activity Log записывает server.disconnect.confirmed"
    - "errors.go_home использует proper i18n key без defaultValue"
  artifacts:
    - path: "gui-app/src/shared/ui/PanelErrorBoundary.tsx"
      provides: "Error boundary с кнопкой Скачать логи"
      contains: "download_logs"
    - path: "gui-app/src/App.tsx"
      provides: "Activity log app.start при монтировании"
      contains: "app.start"
  key_links:
    - from: "gui-app/src/shared/ui/PanelErrorBoundary.tsx"
      to: "gui-app/src-tauri/src/logging.rs"
      via: "invoke('open_logs_folder')"
      pattern: "open_logs_folder"
    - from: "gui-app/src/App.tsx"
      to: "gui-app/src/shared/hooks/useActivityLog.ts"
      via: "useActivityLog().log('STATE', 'app.start ...')"
      pattern: "useActivityLog|app\\.start"
    - from: "gui-app/src/components/ServerTabs.tsx"
      to: "gui-app/src/shared/hooks/useActivityLog.ts"
      via: "useActivityLog().log('USER', 'tab.switch ...')"
      pattern: "tab\\.switch|server\\.disconnect"
---

<objective>
Подключить Activity Log к приложению: добавить кнопку "Скачать логи" в PanelErrorBoundary, логировать app.start в App.tsx, логировать tab.switch и disconnect в ServerTabs. Исправить i18n defaultValue anti-pattern в PanelErrorBoundary.

Purpose: Завершить Activity Log integration — Phase 12 логирует свои действия (per D-07, D-11).
Output: Три точки интеграции Activity Log работают. PanelErrorBoundary имеет кнопку экспорта логов.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-5-skeleton-activity-log-foundation/12-CONTEXT.md
@.planning/phases/12-5-skeleton-activity-log-foundation/12-RESEARCH.md
@.planning/phases/12-5-skeleton-activity-log-foundation/12-PATTERNS.md
@.planning/phases/12-5-skeleton-activity-log-foundation/12-UI-SPEC.md
@.planning/phases/12-5-skeleton-activity-log-foundation/12-01-SUMMARY.md
@.planning/phases/12-5-skeleton-activity-log-foundation/12-02-SUMMARY.md

<interfaces>
<!-- useActivityLog hook (created in Plan 01) -->
From gui-app/src/shared/hooks/useActivityLog.ts:
```typescript
export type ActivityTag = "STATE" | "USER" | "PROTO" | "ERROR";
export function useActivityLog(): { log: (tag: ActivityTag, message: string, details?: string) => void };
```

<!-- PanelErrorBoundary current structure -->
From gui-app/src/shared/ui/PanelErrorBoundary.tsx:
```typescript
// Line 3: import { AlertTriangle, RefreshCw, Home } from "lucide-react";
// Line 18: class PanelErrorBoundaryInner extends React.Component
// Line 46: const { t, panelName } = this.props;
// Line 62-84: div with buttons: "Попробовать снова" + "На главную"
// Line 81: t("errors.go_home", { defaultValue: "На главную" }) <-- FIX: remove defaultValue
// Line 92-98: wrapper function PanelErrorBoundary
```

<!-- ServerTabs (updated in Plan 02) -->
From gui-app/src/components/ServerTabs.tsx (after Plan 02):
```typescript
type TabId = "overview" | "users" | "configuration" | "security" | "utilities";
// setActiveTab(tab.id) in onClick
// state.onDisconnect() in ConfirmDialog onConfirm
```

<!-- Existing command for opening logs folder -->
From gui-app/src-tauri/src/logging.rs (already registered in invoke_handler):
```rust
#[tauri::command]
pub fn open_logs_folder() -> Result<(), String> { ... }
```

<!-- Tauri app version API -->
From @tauri-apps/api/app:
```typescript
export function getVersion(): Promise<string>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PanelErrorBoundary — кнопка "Скачать логи" + fix defaultValue</name>
  <files>
    gui-app/src/shared/ui/PanelErrorBoundary.tsx
  </files>
  <read_first>
    gui-app/src/shared/ui/PanelErrorBoundary.tsx,
    gui-app/src/shared/i18n/locales/ru.json,
    gui-app/src/shared/i18n/locales/en.json
  </read_first>
  <action>
1. Добавить импорты в PanelErrorBoundary.tsx:
   - Добавить `Download` в импорт из `lucide-react`: `import { AlertTriangle, RefreshCw, Home, Download } from "lucide-react";`
   - Добавить `import { invoke } from "@tauri-apps/api/core";`

2. В методе `render()` класса PanelErrorBoundaryInner, в блоке `<div className="flex gap-[var(--space-2)]">` (строка 62), обновить кнопки. Новый порядок: "Попробовать снова" | "Скачать логи" | "На главную":

   После первого `<Button variant="secondary" ...>{t("errors.retry")}</Button>` (строки 63-69), ДОБАВИТЬ вторую кнопку:
   ```tsx
   <Button
     variant="ghost"
     size="md"
     icon={<Download className="w-4 h-4" />}
     onClick={() => {
       invoke("open_logs_folder").catch((e) =>
         console.error("open_logs_folder failed:", e)
       );
     }}
   >
     {t("errors.download_logs")}
   </Button>
   ```

   Решение: используется `open_logs_folder` (уже существует в logging.rs, строка 242 в lib.rs) вместо `export_activity_log` + `open_path`. Это открывает папку logs/ в Explorer — лучше UX для Windows (пользователь видит все логи: app.log + activity.log).

3. ИСПРАВИТЬ строку 81: убрать `{ defaultValue: "На главную" }`:
   - БЫЛО: `{t("errors.go_home", { defaultValue: "На главную" })}`
   - СТАЛО: `{t("errors.go_home")}`
   Ключ `errors.go_home` уже добавлен в оба JSON файла в Plan 02 Task 1.

ВАЖНО: PanelErrorBoundaryInner — class component. invoke вызывается прямо в onClick callback, без хуков.
  </action>
  <verify>
    <automated>cd gui-app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `PanelErrorBoundary.tsx` содержит `import { AlertTriangle, RefreshCw, Home, Download } from "lucide-react"`
    - `PanelErrorBoundary.tsx` содержит `import { invoke } from "@tauri-apps/api/core"`
    - `PanelErrorBoundary.tsx` содержит `invoke("open_logs_folder")` в onClick
    - `PanelErrorBoundary.tsx` содержит `{t("errors.download_logs")}` на кнопке
    - `PanelErrorBoundary.tsx` НЕ содержит `defaultValue: "На главную"` (i18n fix)
    - `PanelErrorBoundary.tsx` содержит 3 кнопки в порядке: retry, download_logs, go_home
    - `npm run typecheck` проходит
  </acceptance_criteria>
  <done>PanelErrorBoundary имеет 3 кнопки: "Попробовать снова", "Скачать логи" (open_logs_folder), "На главную". defaultValue anti-pattern исправлен.</done>
</task>

<task type="auto">
  <name>Task 2: Activity Log integration — app.start + tab.switch + disconnect</name>
  <files>
    gui-app/src/App.tsx,
    gui-app/src/components/ServerTabs.tsx
  </files>
  <read_first>
    gui-app/src/App.tsx,
    gui-app/src/components/ServerTabs.tsx,
    gui-app/src/shared/hooks/useActivityLog.ts
  </read_first>
  <action>
**App.tsx — app.start событие (D-11):**

1. Добавить импорты:
   ```tsx
   import { useActivityLog } from "./shared/hooks/useActivityLog";
   import { getVersion } from "@tauri-apps/api/app";
   ```

2. Внутри компонента App (на верхнем уровне, рядом с другими hooks) добавить:
   ```tsx
   const { log: activityLog } = useActivityLog();
   ```

3. Добавить useEffect для app.start (после существующих useEffect, с пустым dependency array):
   ```tsx
   useEffect(() => {
     getVersion()
       .then((version) => {
         activityLog("STATE", `app.start version=${version}`, "App");
       })
       .catch(() => {
         activityLog("STATE", "app.start version=unknown", "App");
       });
   }, []); // eslint-disable-line react-hooks/exhaustive-deps
   ```

   ВАЖНО: `activityLog` стабилен (useCallback с []), поэтому lint warning о dependency — ложное срабатывание. Добавить eslint-disable-line.

**ServerTabs.tsx — tab.switch + disconnect события (D-11, D-12):**

1. Добавить импорт:
   ```tsx
   import { useActivityLog } from "../shared/hooks/useActivityLog";
   ```

2. В теле компонента ServerTabs добавить:
   ```tsx
   const { log: activityLog } = useActivityLog();
   ```

3. Обновить onClick для tab buttons — добавить логирование ПОСЛЕ setActiveTab:
   ```tsx
   onClick={() => {
     setActiveTab(tab.id);
     activityLog("USER", `tab.switch target="${tab.id}"`, "ServerTabs");
   }}
   ```

4. Обновить onClick для disconnect icon — добавить логирование:
   ```tsx
   onClick={() => {
     setShowDisconnectConfirm(true);
     activityLog("USER", "server.disconnect.initiated", "ServerTabs.LogOutIcon");
   }}
   ```

5. Обновить onConfirm в ConfirmDialog:
   ```tsx
   onConfirm={() => {
     activityLog("USER", "server.disconnect.confirmed", "ConfirmDialog");
     setShowDisconnectConfirm(false);
     state.onDisconnect();
   }}
   ```

6. Обновить onCancel в ConfirmDialog:
   ```tsx
   onCancel={() => {
     activityLog("USER", "server.disconnect.cancelled", "ConfirmDialog");
     setShowDisconnectConfirm(false);
   }}
   ```

Формат логов per D-12: `[TAG] action target="code_name" (ComponentName)` — кодовое имя элемента обязательно для однозначной идентификации.
  </action>
  <verify>
    <automated>cd gui-app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `App.tsx` содержит `import { useActivityLog } from "./shared/hooks/useActivityLog"`
    - `App.tsx` содержит `import { getVersion } from "@tauri-apps/api/app"`
    - `App.tsx` содержит `activityLog("STATE", \`app.start version=` в useEffect
    - `ServerTabs.tsx` содержит `import { useActivityLog }` из `"../shared/hooks/useActivityLog"`
    - `ServerTabs.tsx` содержит `activityLog("USER", \`tab.switch target="` в onClick таба
    - `ServerTabs.tsx` содержит `activityLog("USER", "server.disconnect.initiated"` в onClick disconnect icon
    - `ServerTabs.tsx` содержит `activityLog("USER", "server.disconnect.confirmed"` в onConfirm
    - `ServerTabs.tsx` содержит `activityLog("USER", "server.disconnect.cancelled"` в onCancel
    - Каждый вызов activityLog содержит третий параметр (details) с именем компонента (per D-12)
    - `npm run typecheck` проходит
  </acceptance_criteria>
  <done>Activity Log интегрирован в 3 точках: App.tsx (app.start), ServerTabs (tab.switch + disconnect.initiated/confirmed/cancelled). Формат логов per D-12 — кодовое имя компонента в каждой записи.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React -> Rust (open_logs_folder) | PanelErrorBoundary вызывает existing Tauri command для открытия папки |
| React -> Rust (write_activity_log) | Activity log записи из App.tsx и ServerTabs.tsx |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Information Disclosure | open_logs_folder | accept | Открывает папку logs/ текущего пользователя через Windows Explorer. Стандартное поведение desktop app. |
| T-12-09 | Information Disclosure | app.start version log | accept | Версия приложения — не секретная информация. Не содержит PII. |
| T-12-10 | Information Disclosure | tab.switch + disconnect logs | mitigate | Per D-13: IP замаскирован через sanitize() (host в SENSITIVE_KEYS). Tab names — не секретные. |
</threat_model>

<verification>
- `npm run typecheck` проходит
- PanelErrorBoundary рендерит 3 кнопки в правильном порядке
- App.tsx логирует app.start при монтировании
- ServerTabs логирует tab.switch при переключении
- ServerTabs логирует disconnect events
</verification>

<success_criteria>
1. PanelErrorBoundary имеет кнопку "Скачать логи" вызывающую open_logs_folder (D-10)
2. defaultValue anti-pattern исправлен в errors.go_home
3. App.tsx логирует app.start version=X.Y.Z при старте (D-11)
4. ServerTabs логирует tab.switch target=tabId при переключении (D-11, D-12)
5. ServerTabs логирует server.disconnect.initiated/confirmed/cancelled (D-11, D-12)
6. Каждая запись содержит component name в details (D-12)
7. TypeScript компилируется без ошибок
</success_criteria>

<output>
After completion, create `.planning/phases/12-5-skeleton-activity-log-foundation/12-04-SUMMARY.md`
</output>
