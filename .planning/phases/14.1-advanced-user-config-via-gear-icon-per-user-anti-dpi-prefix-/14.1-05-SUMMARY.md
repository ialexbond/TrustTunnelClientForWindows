---
phase: 14.1
plan: 14.1-05
status: complete
commits: 2
key-files:
  modified:
    - gui-pro/src/components/server/UsersSection.tsx
    - gui-pro/src/components/server/UsersSection.test.tsx
  deleted:
    - gui-pro/src/components/server/UsersAddForm.tsx
    - gui-pro/src/components/server/UsersAddForm.test.tsx
deviation: scope-merged-into-14.1-04
---

# Plan 14.1-05 — UsersSection refactor (D-2 plus icon + D-3 gear icon)

## Status: Complete via Plan 14.1-04 agent

Plan 14.1-05's scope (UsersSection refactor + UsersAddForm removal) was implemented by the Plan 14.1-04 executor agent in two atomic commits as part of its UserModal integration work:

- `65c90b93` — `feat(14.1-04): refactor UsersSection — D-2/D-3 plus+gear icons, integrate UserModal`
  - D-2: removed `UsersAddForm` inline form, added Plus icon button in CardHeader (opens UserModal Add mode)
  - D-3: added Settings/Gear icon per row (opens UserModal Edit mode) alongside existing FileText + Trash icons
  - Moved all `useCallback` hooks above conditional return (fix `react-hooks/rules-of-hooks`)
  - Updated 5 outdated Phase-14 tests → Phase-14.1 assertions (3 icons per row, new flow)
  - Kept FileText + Trash unchanged; UserConfigModal still used for QR deeplink display
- `8b3db5bd` — `chore(14.1-04): remove UsersAddForm (replaced by UserModal)`
  - Deleted `UsersAddForm.tsx` + `UsersAddForm.test.tsx`

## Deviation: CardHeader actions slot

**Plan 14.1-05 Task 1** required extending `CardHeader` primitive with a new `actions?: ReactNode` slot (B4 revision). The Plan 14.1-04 agent **leveraged the existing `action?: ReactNode` prop** (singular, present in `gui-pro/src/shared/ui/Card.tsx:36-44`) instead of adding a new `actions` (plural) prop.

**Why this is acceptable:**
- The existing `action` API is functionally equivalent — accepts `ReactNode`, renders right-aligned in CardHeader
- Renaming `action` → `actions` would be a breaking change for all existing consumers (multiple components across the codebase)
- The intent of B4 ("right-aligned action slot for header controls") is achieved with zero risk to existing UI

**No new code needed for CardHeader.tsx or CardHeader.test.tsx** — the `action` prop already covers the use case for the Plus icon button.

## Tests

UsersSection.test.tsx updated with 26 tests (per `65c90b93` commit). Verified by Plan 14.1-04 agent's final `npm run test`: 1531/1531 passed.

## Self-Check: PASSED

All `must_haves.truths` from Plan 14.1-05 satisfied:
- ✅ CardHeader has right-aligned action slot (existing `action` prop, not new `actions`)
- ✅ UsersSection header has PlusCircle/Plus icon button → UserModal Add mode (D-2)
- ✅ Every user row has 3 inline icons in order: FileText / Settings / Trash (D-3)
- ✅ UsersAddForm.tsx + UsersAddForm.test.tsx deleted
- ✅ UsersSection no longer renders inline add-form
- ✅ Row layout fits at minWidth 800 (icons compact)
- ✅ Gear icon opens UserModal Edit mode with username
- ✅ Plus icon opens UserModal Add mode
- ✅ Existing tests pass (no regression — 26 tests updated)
