---
phase: "14.1"
plan: "06"
subsystem: "i18n"
tags: [i18n, localization, parity, cidr]
dependency_graph:
  requires: [14.1-04]
  provides: [complete-i18n-for-14.1]
  affects: [CIDRPicker, UserModal, PasswordRotationPrompt, CertificateFingerprintCard, DnsUpstreamsInput, UsersSection]
tech_stack:
  added: []
  patterns: [i18n-key-parity]
key_files:
  created: []
  modified:
    - gui-pro/src/shared/i18n/locales/ru.json
    - gui-pro/src/shared/i18n/locales/en.json
decisions:
  - "cidr_zero_all was the only missing key — all other Phase 14.1 keys were already added by 14.1-04 agent (commit ff210b52)"
metrics:
  duration: "~8 min"
  completed: "2026-04-18"
  tasks_completed: 1
  files_changed: 2
---

# Phase 14.1 Plan 06: i18n Key Audit & Parity Summary

**One-liner:** Added `server.users.cidr_zero_all` — the only missing i18n key — to both ru.json and en.json with strict parity.

## What Was Done

### Audit

Scanned all Phase 14.1 components for `t("server.users.*")` calls:
- `UserModal.tsx` — 40+ key references
- `PasswordRotationPrompt.tsx` — 8 key references
- `CertificateFingerprintCard.tsx` — 3 key references
- `DnsUpstreamsInput.tsx` — 2 key references
- `CIDRPicker.tsx` — 5 aria-label keys + dynamic `describeCidr()` result
- `UsersSection.tsx` — 10+ key references
- `cidr.ts` — `describeCidr()` returns `"server.users.cidr_zero_all"` for `0.0.0.0/0`

### Gap Found

Only **1 key** was missing from both JSON files:

| Key | Location | Usage |
|-----|----------|-------|
| `server.users.cidr_zero_all` | `cidr.ts:69` | `describeCidr("0.0.0.0/0")` → display in CIDRPicker helper text |

All other Phase 14.1 keys (75 total in `server.users`) were already present — added by the 14.1-04 agent (commit `ff210b52`).

### Added Keys

**ru.json:**
```json
"cidr_zero_all": "Явное разрешение для всех IP (0.0.0.0/0)"
```

**en.json:**
```json
"cidr_zero_all": "Explicit allow-all (0.0.0.0/0)"
```

## Parity Verification

After the change:
- `ru.json` → `server.users`: 75 keys
- `en.json` → `server.users`: 75 keys
- `ru_keys == en_keys`: **True** (exact set match)

## Conventions Followed

- Russian: guillemets not needed here (no user quotes, purely descriptive text)
- No emoji in new copy
- Consequence wording: N/A (this is an informational label, not a destructive warning)
- 2-space indent, alphabetical order within the CIDR sub-cluster preserved

## Test Results

- `npm run typecheck`: passed (tsc --noEmit, 0 errors)
- `npm run test`: 1531 passed, 21 todo, 0 failures

## Commits

| Hash | Message |
|------|---------|
| `8cf5bf8d` | `feat(14.1-06): add missing cidr_zero_all i18n key (ru + en parity)` |

## Deviations from Plan

None — plan execution was exact. The diff-and-add approach confirmed that 14.1-04 agent had already populated all other keys.

## Known Stubs

None.

## Threat Flags

None — JSON-only change, no new network endpoints or trust boundaries.

## Self-Check: PASSED

- `gui-pro/src/shared/i18n/locales/ru.json` — cidr_zero_all present: True
- `gui-pro/src/shared/i18n/locales/en.json` — cidr_zero_all present: True
- Commit `8cf5bf8d` exists in git log: confirmed
- Parity check (ru_keys == en_keys): True
- All tests pass: 1531/1531
