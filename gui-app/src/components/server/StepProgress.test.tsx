import { describe, it } from "vitest";

describe("StepProgress", () => {
  it.todo("renders all step labels");
  it.todo("shows spinner icon for active step (MTPROTO-02)");
  it.todo("shows check icon for completed steps");
  it.todo("shows X icon for error step");
  it.todo("shows circle icon for pending steps");
  it.todo("renders connector lines between steps");
});
